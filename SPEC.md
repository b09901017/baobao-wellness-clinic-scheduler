# 健康管理師排課系統 — 專案規格書

> 這份文件是給 Claude Code 的完整背景。開始寫任何程式碼前請先讀完。
> 版本：v2（2026-08）。v1 的桌面優先與 Supabase 假設已作廢，見第 1 節與第 9 節。

---

## 1. 這是什麼、為什麼要做

### 使用者

一位在「東森栢馥」任職的**健康管理師**。工作內容是幫購買健康方案的客戶安排各種療程課程，並在公司的預約系統上完成登記。

- 目前**只有她一個人使用**，架構要能在之後開放給同事（多帳號）。
- 這是**私人工具**，不是公司專案。資料含客戶健康資訊。

### ⚠️ 最關鍵的環境限制：公司電腦沒有外網

**公司的電腦無法連外網，所以這個 app 在公司電腦上打不開。**

這導致一個很特別的使用姿勢：

> **她一手拿自己的裝置看 app，一手在公司電腦上操作 Abovee 壓表。**

app 在壓表當下的角色是**「唸稿機」**：她要一眼讀出「下一位排誰、他哪幾天可以、排什麼、幾點、哪間診間、還剩幾次」，照著在公司電腦上輸入，然後回來極快速地記錄結果。

**輸入越少越好** —— 她填完 Abovee 之後回來記錄，時間、治療師、診間一律用選的，不要讓她打字。

### 兩種載具：Android 手機 + iPad，約 50 / 50

**沒有桌機。** 所有畫面都是觸控介面，不能依賴 hover、不能有小到 44px 以下的點擊區。

| | Android 手機（~380–412px） | iPad（直式 ~810 / 橫式 ~1080） |
|---|---|---|
| 情境 | 在院內走動、臨時被問、通勤 | 坐在公司電腦旁、家裡整理資料 |
| 強項 | 隨身、隨手勾一筆 | 看得多、打字快、可雙欄 |
| 適合的事 | 待辦勾選、查客戶剩幾次、複製 LINE 訊息、快速記錄 | 壓表作業、可用性收集打字、主檔設定、資料健檢、匯入、客戶總覽 |

### 裝置分工的兩條鐵則

1. **不做兩套 app。** 同一份程式碼、同一組元件，用容器寬度切版型。iPad 是「同樣的東西看得更多」，不是不同的功能集。
2. **不能有任何功能只有某台裝置做得到。** 她手邊不一定有哪台。iPad 上「比較好做」的事（例如可用性收集要打一段文字），手機上仍然要做得到，只是慢一點。

系統可以在畫面上提示「這件事用 iPad 做比較順」，但不能擋。

### 跨裝置續作

因為她可能 iPad 壓到一半換手機，**壓表作業的進度必須存在雲端**（見第 5 節 `batches`），不能只放在本機。換裝置打開就接著上次的位置繼續。

### 現行工作流程

1. 用 LINE（少數用電話）問客戶下個月方便的時間。
2. 把客戶名字、可用／不可用的時間條件、特殊狀況（例：體內金屬、癌症化療中）寫在**白紙**上。
3. 坐到公司電腦前**壓表**：在 Abovee 上把時段佔掉。一次壓一整個月，同時處理很多位客戶。
4. 壓完後用 LINE 問客戶「這些時間可以嗎」。
5. 客戶確認 → 記到手機 **TimeTree**（工作行事曆，刻意與私人 Google 行事曆分開）+ 記到 **Google 試算表**（每位客戶一張工作表）。
6. 客戶來訪前（**死線是前一天**，但可以提早做）→ 到另外幾個系統完成掛號、並打電話提醒。

### 核心痛點

| 痛點 | 現況 |
|---|---|
| 狀態記在腦袋裡 | 「這個人壓了沒？」「回覆了沒？」沒有地方看 |
| 忘記做系統掛號 | 一次壓十幾個人，每個人後續任務不同，來不及一一記錄 |
| 剩餘次數混亂 | 試算表只能算「已排」，算不出「已完成 / 已排未上 / 剩餘」 |
| 限制條件散落 | 白紙上「8/18 出國八天」「體內金屬」全混在一起，下個月就作廢 |
| 排班要靠記憶判斷優先序 | 誰限制最多、誰快到期、誰是重要客戶，全憑印象 |
| 診間會撞 | 白紙上她自己寫了「（確認房間無人用!!）」兩個驚嘆號 |

### 一句話目標

> **把「決定要排誰、排什麼、他哪天不行、還剩幾次」這件事從腦袋裡搬出來，並且不讓任何一個後續任務被忘記。**

---

## 2. 名詞表

詞彙表在 `CONTEXT.md`，那裡是唯一的一份。寫程式、寫 issue、寫 UI 文案前先讀。

現行白紙的速記寫法（匯入舊資料時才需要）記在 `docs/legacy/README.md`。

---

## 3. 現行試算表結構（要取代的東西）

Google 試算表「寶寶客戶進度總表」，**每位客戶一張工作表**，約 20+ 張且持續增加。

單張工作表結構：

- A2：客戶名稱
- B2：購買名稱（例：`0522 顧客會-8`、`0806 H2U導客 -`）＝ 購買通路／活動
- C 欄：療程項目
- D 欄：應有次數
- E 欄：實際次數
- F 欄以後：每欄一個日期，每格一個勾選框 → 勾起來代表這天排了這個療程
- 下方 `TODO` 區塊：由 Apps Script 依勾選自動產生的系統任務列
- 右方 `FINISH` 區塊：全部任務完成後搬過去
- 空白處：手寫特殊狀況（例：`重大疾病治療中，食慾還可以`）

**現有 Apps Script 的行為**（`onEdit` 觸發）：勾日期 → 產生 TODO 列；TODO 全勾 → 搬到 FINISH；有排序、防重入鎖、防點擊偏移。這些邏輯的**意圖**要保留，但實作方式在新系統裡完全不同（用資料庫狀態，不用儲存格）。

### 試算表 ↔ 方案文宣 名稱對照

| 試算表寫法 | 方案文宣正式名稱 |
|---|---|
| Inbody | 身體組成分析 |
| 復健門診 | 復健科醫師門診 |
| 物理諮詢 | 物理治療師諮詢 |
| 營養諮詢 | 營養師諮詢 |
| 體適能分析 | 體適能檢查分析 |
| 復能(1小時) | 復能三選一(60) |
| ILIB 60mins | ILIB（正式名稱：經皮靜脈雷射） |

---

## 4. 核心設計決策

這些是討論後定案的，實作時請遵守。

### 4.1 預約狀態機

```
佇列狀態（推導，不存欄位）        時段狀態（存在 slot.status 上）
待收集時間 → 待壓表        ──▶   已壓表・待客人確認 → 已確認 → 已完成 / 未到
                            ↑                  |
                            └──── 客人改時間 ────┘

                                  來訪狀態（從時段推出來，也存在 visits 上）
```

**狀態記在時段上，整筆那一個是推導出來的**（2026-09-08，`docs/adr/0081`）。
推法由上往下比、第一個對上的算數：全部取消 → 有一段還沒問過客人 →
有一段談定了 → 有一段做了 → 其餘未到。

推出來的值**照樣寫進文件**，因為索引、Rules、試算表、備份四個地方讀它。
兩邊對不起來時由資料健檢的「來訪的狀態跟它的時段對不起來」列出來。

**來訪的起點是「已壓表・待客人確認」。** 因為使用者是先在 Abovee 壓完表才回 app 記錄，所以 app 裡不存在「還沒壓表的來訪」。

左邊兩個是**佇列狀態**，不是來訪狀態，也不存成欄位：有額度且該月沒有對應來訪 = 待壓表，再加上沒有有效的本輪可用性 = 待收集時間。見 `docs/adr/0001-scheduling-queue-state-is-derived.md`。

- 「**已壓表・待客人確認**」是最危險的狀態：時段已被佔住，但客人還沒回。超過 N 天沒動要跳紅色。
- 客人**改時間**時必須**自動產生「取消舊時段」的任務**，不能默默改日期。壓表登記那一件是無條件的（來訪存在就代表她在 Abovee 或 Examine 上壓過了）；Examine 與耀聖則是已經勾掉的才要收回來。見 `docs/adr/0041`。

### 4.2 次數在「已完成」才扣

現行試算表在「排定」時就扣，導致取消／改期後數字與現實脫節。

新系統一律拆成三個數字顯示：

```
應有 20 ｜ 已完成 8 ｜ 已排未上 3 ｜ 剩餘 9
```

Firestore 沒有便宜的即時聚合，所以會在 entitlement 上維護計數欄位，但**必須搭配對帳機制**（見第 6 節）。計數欄位只能由交易更新，永遠可以從 `visits` 重算驗證。

**「未到」不扣次數，時段還回去。** 客人沒到就是沒用到療程，只有「已完成」才扣。未到會另外計數（`counts()` 的 `noShow`）並顯示出來，讓「這個人常放鴿子」看得見，但它不影響剩餘次數。

**做了幾段就扣幾次。** 客人做了兩段、第三段就走是會發生的（2026-08-20 使用者確認），所以「有沒有做」記在**時段**上（`slot.attended`），不是整筆來訪上。

**取消掉的那一段把次數還回去**（ADR-0081）：`slotOutcome()` 對它回 `null`，
跟整筆取消是同一種答案。整筆的狀態仍然只有一個，講的是這次預約走到哪裡；逐段記的是那天實際發生了什麼。收尾的動線在第 8.1 節，規則在 `domain/entitlements.js` 的 `slotOutcome()`，理由見 `docs/adr/0025-what-happened-is-recorded-per-slot.md`。

### 4.3 限制分兩種，而永久限制底下又分三層

| 類型 | 例子 | 生命週期 |
|---|---|---|
| **永久限制** | 體內金屬、血管難打、固定禮拜五不行 | 跟著客戶，永遠有效 |
| **本輪可用性** | 8/11 不行、8/18 出國八天、9/6 那星期不行、一三下午方便 | 有收集日期 + 有效期，過期自動變灰並提示「該重問了」 |

永久限制底下分兩層，差別在**系統拿它做什麼**
（`docs/adr/0074-a-contraindication-warns-it-does-not-block.md`）：

| 層 | 例子 | 系統做什麼 | 壓表卡片牆上 |
|---|---|---|---|
| **警示** | 體內金屬、血管難打、第一針 | 什麼都不擋 | 畫，每一個字自己挑顏色與填法 |
| **其餘** | 固定禮拜五不行 | 什麼都不擋 | **不畫** |

2026-09-06 之前有三層，最上面那一層是「醫療禁忌」，而它自成一層的唯一理由是
**它會硬性擋掉某個器材**。不擋之後那條界線就不存在了，所以兩層合併。

警示是**有限的一組字**（`config/app/clinicalFlags`，她自己加得了「怕痛」），
所以在編輯畫面上做成可以點的丸子；第二層是她自己的話，留自由輸入。

**警示不給她打字**：打錯一個字就壓表卡片牆上什麼都不會出現，
而那在畫面上跟做對了一模一樣。

**器材上那一欄字（「要特別提醒的狀況」）也要在警示名單裡**才畫得到客戶身上 ——
器材上有、警示主檔沒有的那幾個字由資料健檢列出來，按一下補進去。

**永久限制（尤其是醫療禁忌）在任何畫面都必須跟著客戶名字顯示，不可被摺疊隱藏。**
第三層在壓表卡片牆上不畫是這一條唯一的例外，理由是那一頁一張卡只回答一個問題
（ADR-0046）：一張卡上十個字等於全都沒有重點。記錄面板與客戶詳情上三層都在。

可用性收集要**同時保留原始文字**和解析後的結構化規則。解析可能出錯，原文是最終依據。
原文攤開在**核對解析對不對的那幾頁**（可用性頁、收件匣）；壓表卡片牆上只放解析出來的
條件丸子，原文收進一摺 —— 表單來的那一份，原文是系統照他的答案產生的，
跟丸子講的是同一件事（`docs/adr/0035-the-wall-shows-rules-not-the-raw-answer.md`）。

**一份收集綁的是一段期間，所以「哪一份算數」要看你在問哪個月。** 壓表與時段反查
一律用 `domain/availability.js` 的 `collectionFor()`（涵蓋那段期間的那一份），
不是 `currentCollection()`（今天還算數的那一份）—— 她八月坐下來壓的是九月的表，
拿今天有效的那一份去算八月，會算出一個假的「可用 0 天」並把人推到排序第一位。
那段期間沒問過就是「還沒問」，見
`docs/adr/0036-availability-is-picked-by-the-month-being-scheduled.md`。

本輪可用性有**兩個來源**：她自己問到之後記進來，或者客戶自己填表單（第 8.9 節）。
表單那條路的答案一出生就是結構化的，解析器不必猜，但原文那一條沒有被放寬 ——
表單裡「還有什麼要跟我說的」那一欄照抄進 `rawText`，永遠比勾選的部分大。
兩種來源在客戶詳情長得一樣、改起來也一樣，差別只有一顆「客戶自己填的」徽章
（`source: 'form'`）。

### 4.4 一天就是一筆來訪，而狀態、次數與那一句話的原子單位是「時段」

**2026-09-09 之後「一天一筆」是真的了**（`docs/adr/0083`）。在那之前它只是
壓表那一頁的合併行為：`firestore.rules` 沒擋、`validateVisit()` 也沒擋，
而日曆那條路根本不查 —— 所以同一位客戶同一天有時候是一筆三段、有時候是
兩筆 2+1，而規則在畫面上看不出來。

判斷只有一支：`domain/visits.js` 的 `sameDayVisitFor()`，壓表與日曆共用。
**收不下（那一天已完成／未到／已取消）時才會有第二筆**，那是唯一的例外。

**「來訪」這個詞不出現在畫面上。** 她看到的是日期與時段。資料層與
`CONTEXT.md` 保留這個術語，因為程式碼裡它仍然是那份文件的名字。

實際紀錄長這樣：

```
8/11 (二)  9:15–10:15  復能
          10:30–11:30  ILIB
          14:00–14:30  心臟科醫生評估
          14:45–15:15  EECP 體驗
```

UI 是「幫某人某天建立一次來訪，裡面加多個時段」，預設間隔 15 分鐘。**不要做成一格一格分開建立預約。**

**但狀態、次數與那一句話都是逐段的**（`slot.status`、`slot.note`，
ADR-0081、0084）：她取消得掉其中一段而不動到那一天剩下的，客人說不行的那一段
也是標成取消而不是從陣列裡刪掉。整筆身上那個 `status` 是從時段推出來、
再存回去的（`visitStatusFrom()`）—— 索引、Rules、試算表、備份四個地方讀它。

**改一筆來訪就是改她點的那一段**（ADR-0085）：日曆上每一條進得了編輯器的路
都帶著 `slotIndex`，帶了就只畫那一段、沒有「＋新增一個時段」、沒有整筆的
狀態卡與危險區。新增那條路不受影響 —— 一次記多段照舊。

**「整天」不是一個改得動的單位了**（ADR-0089，2026-09-12）。ADR-0088 那一顆
「改這一天」連同整天的狀態卡與危險區全部拿掉：**改整天的日期與刪除這一天
一條路都沒有**（誤建的那一筆用取消，暗掉但留著，ADR-0061），取消一整天走
壓表的批次取消（ADR-0082）。長按選單只剩「取消這一段」，而它**不問那一天
有幾段** —— 一天只有一段時取消那一段就是取消那一天。

掛號那一族的待辦仍然綁**一整筆**：她一天去 Examine 一次，不是一段去一次。
所以同一張會出現在那幾段的讀取卡片上（**畫面上不再標「這一天共用」**，
ADR-0089：那件事還在發生，只是不講）。

**四個畫面共用同一張讀取卡片**（ADR-0018、0056）。日曆點進來的帶著段落序號，
所以只畫那一段；另外三頁（客戶詳情、待辦中心、進度追蹤）列的是整筆來訪，
所以**先畫那一天有哪幾段**，而**每一段自己是一列可以點的** —— 點下去用同一支
再開一張只有那一段的（ADR-0080）。卡片副標一律印**那一段**的狀態，
整筆那一個是推導出來的（ADR-0085）。

**沒指定哪一段的那一張只是目錄**（ADR-0089）：只列那幾段讓她點，這一場的
待辦與 SOP 都等她點進某一段才出現。呼叫端進來之前先走 `focusFor()` ——
那一天只有一段時它解成第 0 段，一張只有一列的目錄是講不通的。

### 4.5 方案項目有兩種型態

| 型態 | 例子 | 行為 |
|---|---|---|
| **single** | ILIB(60) 20 次 | 固定療程 |
| **pool（擇一池）** | 復能-三選一(60) 12 次；復能-四選一(30)；復能-SIS(60) | 共用次數，每次來訪選一種**器材** |

擇一池帶著**一組 1～N 台器材**。一台也算數（單買 SIS 就是「這一池裡只有一台」），
零台才擋。名字是算出來的：`復能-N選一(分鐘)`，單台就接那一台的**全名**
（`docs/adr/0078`）。

**這一段算哪一個課程由選到的那台器材決定**（`docs/adr/0075`）：復能四選一裡的
ILIB 要的是治療室，其餘三台要物理治療師 —— 而「要指派什麼」寫在課程上。
**挑器材之前那個問題沒有答案**：兩排都不畫，只留一句「先選上面那一台」
（`assignsFor()`，`docs/adr/0079`）。

**擇一池沒選器材存不下去**，不管推出來的課程是哪一個。閘門只有一個
（`picksEquipment()`）—— 少了那個 id，那一段在月曆與試算表上都印不出是哪一台，
而額度的池成員檢查也沒有東西可以比。

擇一池的選項**一顆都不會被鎖住**。體內金屬那一類只在她真的選了那一台的時候
跳一句明顯的提醒並建議改用哪一台，**不擋**（`docs/adr/0074`）。

### 4.6 系統任務有各自的死線

死線 = 來訪日的前一天，但**可以提早做**。任務一產生就進池子，依「剩幾天到死線」排序，逾期變紅。

不要做成「明天要來的客人」清單——那是錯的模型。

### 4.7 這個 app 不取代 Abovee

**app 看不到同事在 Abovee 上壓的東西。** 所以：

- app 只**提示**她自己排的客人之間的診間／治療師衝突（她一天壓十幾人，自撞很常見），但不阻擋儲存
- 跨同事的衝突仍以 Abovee 為準，app 看不到
- 在標記「已壓表」前，跳出確認：「治3 10:30–11:30，已在 Abovee 確認診間無人使用？」——把她手寫的那兩個驚嘆號變成流程的一部分
- **app 的排班畫面不需要、也不應該跟 Abovee 長得一樣**

### 4.8 試算表改成唯讀報表

app 是唯一真相來源。試算表降級成報表。兩條路都在 `#/settings/report`，排版與次數都在 `domain/sheetReport.js`：

- **手動**：app 產生與現行格式同構的表（**一位客戶一張，沒有總表**），她複製或下載後貼回試算表。
- **自動**：app 寫入成功後把整份報表推給試算表的 Apps Script，由它排版並上鎖。實作在 `sheets/readonly-report.gs`。

日期欄不是一個勾，是四種符號（`○` 待確認、`△` 已確認、`✓` 已完成、`✗` 未到），
TODO / FINISHED 兩塊由 app 填、她不用回來勾，二返寫在那次健檢被勾起來的那一欄底下 ——
理由見 `docs/adr/0024-the-sheet-mirrors-her-old-one.md`。

矩陣底下有**兩種註記**，兩種都寫在那一欄底下但**分開兩份**：二返（`followupNotes`）與
「這一天用了哪一台」（`equipmentNotes`，只有得選的擇一池才有）。塞進同一份的話，
`.gs` 會把它們畫在同一列，而同一個日期欄後面的會蓋掉前面的。

**不做 Apps Script 定時抓 Firestore** —— 那需要把服務帳號金鑰放進第三個地方，見 `docs/adr/0010-sheet-sync-is-an-export-not-a-service.md`；自動那條為什麼是反方向、以及它為什麼不需要任何憑證，見 `docs/adr/0013-sheet-sync-is-a-push-not-a-pull.md`。

**試算表必須設為唯讀**（以下四件由 `sheets/readonly-report.gs` 自動做掉）：

- 移除現有 `onEdit` 的所有寫入邏輯，換成「手動改了就還原並說明」
- 用「保護工作表和範圍」鎖住所有分頁
- 每張分頁頂端加註「⚠️ 本表由系統自動產生，請勿手動編輯。修改請至 app。」
- **不做雙向同步**，一定會打架

### 4.9 TimeTree 可以退場

她用 TimeTree 是因為「工作行事曆和私人 Google 行事曆要分開」。這個 app 加到手機桌面後本身就是那個獨立的工作行事曆，目的已達成。

日曆頁改成可以直接編輯之後，連她原本記在 TimeTree 上的休假與公出也搬得過來 —— 那些是**行事備註**，不是來訪，見 `docs/adr/0015-calendar-is-a-first-class-surface.md`。

若之後仍想在手機原生行事曆看到，再補 ICS 訂閱網址（低優先）。

### 4.10 刻意不做的東西

- **同事輪流壓表的四小時配額**：使用者明確表示不需要在 app 裡追蹤。
- **自動排班**：系統只出建議排序，實際排誰、排哪天由她決定。她一定有系統不知道的判斷。
- **建議時段（第一版不做）**：原本規劃由系統算好時段讓她照抄，但她選擇自己看著資料判斷。等她用一段時間後再決定要不要補。未來若要做，方向是讓她**拍一張 Abovee 畫面的照片**來補足 app 看不到的佔用資訊，而不是靠 app 自己推算。
- **阻擋式的衝突檢查**：見 `docs/adr/0002-app-records-decisions-it-does-not-make-them.md`。一律只提示。醫療禁忌本來是那一條唯一的例外，2026-09-06 收掉了（ADR-0074）。

---

## 5. 資料模型（Firestore）

使用 **Firebase / Firestore**。以下為建議結構。

### 5.1 集合結構

```
config/
  courses/{courseId}
  rooms/{roomId}
  staff/{staffId}
  equipment/{equipmentId}
  ivProducts/{productId}
  plans/{planId}              // items 內嵌為陣列
  taskRules/{ruleId}
  clinicalFlags/{flagId}      // 警示（體內金屬、血管難打…），永久限制的第一層
                              //   集合 id 沒有跟著改名（ADR-0074）
  partners/{partnerId}        // 合作機構（自然美），ADR-0076
  settings/app                // 排序權重、時段間隔、未回覆天數門檻、二返的天數、LINE 回覆模板

customers/{customerId}
  entitlements/{entitlementId}    // 子集合
  availability/{collectionId}     // 子集合，rules 內嵌為陣列

visits/{visitId}                  // 頂層集合，slots 內嵌為陣列
events/{eventId}                  // 行事備註，可跨天，不綁客戶
notes/{noteId}                    // 隨手記
tasks/{taskId}                    // 頂層集合
batches/{batchId}                 // 壓表作業進度，支援跨裝置續作
playbooks/{playbookId}            // 備忘錄／SOP，綁課程不綁人（ADR-0067）
audit/{eventId}                   // append-only 稽核紀錄
```

### 5.2 為什麼 slots 內嵌在 visit 裡

一次來訪只有 2–3 個時段，**一起建立、一起確認、一起讀**，而且一筆文件讀得完。

> **2026-09-09 修正**：這一節本來寫的理由是「內嵌可以做到單次原子寫入」。
> **那個理由今天已經過期了** —— `data/repo.js` 的 `commit()` 用的是
> Firestore `writeBatch`，本來就跨文件原子（它現在同一批就在寫「來訪 +
> 額度計數 + 任務」三種文件）。
>
> 內嵌今天站得住的理由是另外三個：一次來訪的那幾段一起建立、一起讀，
> 而且日曆一次要畫一整個月的來訪 —— 拆成一段一份文件的話那一頁要多讀
> 好幾倍。完整的評估（35 支原始碼 + 15 支測試 + 4 個非 JS 的地方）見
> `docs/adr/0083`。

### 5.3 文件結構

```js
// config/courses/{id}
{
  name, code,
  category,              // 'A' | 'B' | 'C'，決定產生哪些系統任務
  durationMin,
  durationChoices,       // [30, 60]。選填。填了加購那一頁就多一排丸子，
                         //   名字也會帶著它（「超磁場(60)」）。目前只有復能與 ILIB
  shortName, lineName,   // 別稱與 LINE 名（`domain/naming.js`）。兩個都選填 ——
                         //   空的退回全名。月檢視一格只放得下幾個字，而她要認的
                         //   是「那天是哪一台」
  assigns,               // 'therapist' | 'room' | 'none'
                         //   復能三器材 → therapist；含 ILIB 的其餘課程 → room
                         //   **擇一池那一段看的是選到哪一台**（ADR-0075）
                         //   心臟科評估 → none
  allowedRoomTypes,      // ['治療室'] 之類。assigns==='room' 時的預設可選範圍
  allowedRoomIds,        // 例外覆寫，非空時蓋過 allowedRoomTypes。例：EECP 只能 治5、治8
  requiresEquipment,     // bool。true 時來訪要選器材（目前只有復能）
  requiresIvProduct,     // bool。true 時來訪要選營養點滴品項（目前只有營養點滴）
  needsRecord,           // bool。true 時**那一場做完之後**會長出一張「寫紀錄」，
                         //   死線是來訪那一天。目前是二返與營養師諮詢。
                         //   沒有這個欄位就是不用寫（跟 needsTreatmentForm 相反）。
                         //   逐課程不逐類別：二返是 A 類，而同樣 A 類的
                         //   復健科醫師門診不用寫。見 docs/adr/0066
  requiresDoctor,        // bool。**非 A 類**要選醫師時才勾 —— A 類（門診）一律
                         // 選得到，不用勾，見 docs/adr/0058-...
                         // 判斷只有一份：domain/masterData.js 的 picksDoctor()
                         // 不塞進 assigns —— 那是單選的，而二返同時要診間和醫師。
                         // 沒選只提醒不擋，見 docs/adr/0026-...
  frequencyRule,         // 例：'每季一次'，只提示不擋
  followupCourseId,      // 做完之後還要再約一次的那個課程。目前只有健檢 → 二返。
                         // 設了之後：買 N 次這個課程就自動有 N 次後續課程的額度，
                         // 而且這個課程的來訪標成已完成會長出「約⋯⋯」的待辦。
                         // 為什麼記在課程上而不是寫死，見 docs/adr/0022-...
  active, deletedAt
}
// 禁忌只記在器材上，courses 不再有 contraindications 欄位——
// 同一條規則存兩個地方，遲早只改到一邊。

// config/equipment/{id}
{ name, shortName, lineName, courseId, contraindications, active, deletedAt }
// 不記台數，也不納入衝突檢查。app 不判斷資源夠不夠。
//
// courseId：**用這台的那一段算哪一個課程**（ADR-0075）。前三台指復能、
//   ILIB 那一台指 ILIB —— 而「要指派治療師還是診間」是課程說了算。選填，
//   沒填就退回舊的推導（`requiresEquipment` 的那個課程）。
// contraindications：**要特別提醒的狀況**（體內金屬）。2026-09-06 之前這一欄
//   叫「醫療禁忌」而且會硬性擋掉這一台；現在它只讓畫面跳一句提醒（ADR-0074）。
//   這幾個字**也要進得了 clinicalFlags** 才畫得到客戶身上。

// config/rooms/{id}
{ name, type, beds: ['A','B'], active, deletedAt }
//   type: '治療室' | '點滴室' | 'ILIB室'

// config/staff/{id}
{ name, role, active, deletedAt }
//   role: '物理治療師' | '醫師'
//   一份清單放兩種人，而 role 不只是標籤，它是分流：復能的治療師選單只列
//   物理治療師、二返的醫師選單只列醫師。選錯人是實際傷害（CONTEXT.md），
//   所以拿名單一律走 domain/masterData.js 的 staffWithRole()。
//   護理師目前不納入排程（營養點滴不需要指定護理師）。見 docs/adr/0026-...

// config/ivProducts/{id}
{ name, active, deletedAt }

// config/partners/{id}  —— 合作機構（ADR-0076）
{ name, active, deletedAt }
// 客戶身上存的是**字串**不是 id（同永久限制），所以改名不會自動搬既有客戶。

// config/plans/{id}   —— 範本，可就地改、可複製改名、可停用
{
  name, price, membershipMonths, active, deletedAt,
  items: [
    { type:'single', courseId, label, qty, durationMin, frequencyRule },
    { type:'pool',   label:'復能', qty:12, durationMin:60,
      optionEquipmentIds:[...] }   // 擇一池換的是器材，不是課程
  ]
}
// 沒有 version 欄位。改範本不影響已展開的額度，因為額度是展開當下的
// 完整複本。詳見 docs/adr/0003-plan-templates-have-no-version.md

// customers/{id}
{
  name, phone, lineId,
  source,                    // 購買名稱，例：'0522 顧客會-8'
  purchasedAt,
  membershipExpiresAt,       // 留著讀舊資料，畫面上已經不再顯示也不再填（ADR-0019）
  priority,                  // 喜好程度 0–5，排序加權用（0 = 還沒評）
  flags,                     // 永久限制，例：['體內金屬']。系統看得懂，但**不擋**
                             //   任何東西（ADR-0074）。分兩層：進得了 clinicalFlags
                             //   的是「警示」（畫在壓表卡片牆上），其餘不畫
  partners,                  // 合作機構，例：['自然美']。**存字串不存 id**。
                             //   不擋、不影響排班、不產生任何待辦（ADR-0076）
  marks,                     // 備註，例：[{text:'喜歡下午', color:'green'}]
                             //   最多 12 則、每則 40 字，顏色六選一，不影響任何行為
  notes,                     // marks 換行接起來的純文字鏡像，兩者永遠一起寫
                             //   舊客戶只有 notes，讀的時候逐行拆成灰色備註
  importedFrom,              // 從舊試算表匯進來的才有，形狀同 visits
  active, deletedAt
}
// 備註與永久限制的界線見 CONTEXT.md，為什麼分兩個欄位見
// docs/adr/0019-customer-notes-become-coloured-marks.md

// customers/{id}/entitlements/{id}
{
  type,                      // 'single' | 'pool' | 'product'
                             // product = 營養品。**排不進來訪**（只記錄與顯示），
                             // 所以它不可以流進任何「還要排幾次」的計算 ——
                             // 閘門是 domain/entitlements.js 的 schedulable()。
                             // 見 docs/adr/0057-a-product-is-an-entitlement-that-cannot-be-scheduled.md
  label, totalQty, durationMin,
  tier,                      // 健檢的金額等級，例：'8萬'。選填，**只影響顯示名稱**
                             // （CONTEXT.md 的「健檢」）—— 流程與任務都不看它。
                             // 見 docs/adr/0054-a-checkup-tier-lives-on-the-entitlement.md
  ivProductId,               // 這一筆是哪一個營養點滴品項的（『營養點滴 - 雪顏亮彩』）。
                             // 選填。品項各自計次不合併（CONTEXT.md 的
                             // 「營養點滴品項」），所以一筆額度一個品項。
                             // **排班時預設就是它**，其餘的收在「換一款」後面
                             // （`domain/masterData.js` 的 ivChoicesFor()）——
                             // 換得掉，只是那一段會多一句「跟買的不一樣」。
  productId,                 // type === 'product' 才有：哪一款營養品。
                             // 存 id 不是只留在名字裡 —— 主檔上的名字改得了。
  courseId, optionEquipmentIds,   // pool 型態：擇一池換的是器材。**1～N 台**
                             //   （ADR-0075）：單買一台就是「這一池裡只有一台」。
                             //   那一段算哪個課程由選到的那台器材決定
  purchaseId,                // 這一次購買的 id。同一次展開出來的每一筆都一樣 ——
                             //   「買過什麼」那一頁靠它分組，也靠它一次改整組的
                             //   購買日。選填（2026-09-06 之前建的沒有）
  sourcePlanQty,             // 方案本來寫幾次（乘過購買數量）。跟 totalQty 不一樣
                             //   就是加購當下微調過。存數字不存布林 ——
                             //   「15 次（方案本來 20）」講得出改成什麼
  sourcePlanName,            // 展開當下的方案名稱文字快照；null = 單項加購
                             // 不存 sourcePlanId —— 額度不指回範本，範本會被就地改。
                             // 見 docs/adr/0003-plan-templates-have-no-version.md
  purchasedAt,
  expiresAt,                 // **選填而且預設沒有**。她 2026-09-06：「其實現在
                             //   不需要到期日了……基本上不會到期」。加購時的
                             //   進階設定裡可以選半年／一年／自己選
  frequencyRule,
  doneCount, bookedCount,    // 交易維護，可從 visits 重算驗證
  lastReconciledAt,
  followupForEntitlementId,  // 二返那一筆才有：指回它是哪一筆健檢配出來的。
                             // 用課程比對會在一位客戶買兩筆健檢時配錯，
                             // 見 docs/adr/0022-followup-entitlements-are-expanded-in-pairs.md
  importedFrom               // 從舊試算表匯進來的才有，形狀同 visits
}

// customers/{id}/availability/{id}
{
  collectedAt, validFrom, validTo,
  rawText,                   // 原始文字，永遠保留並顯示
  followupNote,              // 例：'禮拜一再確認一次'
  rules: [
    { kind:'exclude_weekday', weekday:1 },
    { kind:'exclude_date',    date:'2026-09-17' },
    { kind:'exclude_range',   from:'2026-09-22', to:'2026-09-24' },
    { kind:'prefer',          weekday:3, partOfDay:'pm' }
  ]
}
// 兩個排除型的 rule 也可以帶選填的 partOfDay（'am' | 'pm'）：「禮拜一下午不行」
// 當成整天不行會蓋掉真的有空的上午，直接忽略「下午」又會推薦她不能用的時段，
// 兩種都是錯的，所以記下來。只擋半天的日子仍然算可用日，由 UI 標出是哪半天。
// prefer 也可以用 date 取代 weekday（「9/17 可以」）。
//
// 解析看不懂的句子**不會**變成 rule，也不會被丟掉 —— 收集畫面把它們原樣列出來，
// 讓她看得出系統沒讀懂哪一句。原文永遠是最終依據。
//
// source: 'form' 的那幾份是客戶自己填表單填的（第 8.9 節），rules 直接從勾選轉，
// 沒有經過解析器。rawText 是「把勾選組回去的人話 + 客戶自由欄照抄」。

// formInvites/{token}          token 就是文件 id，Firestore 自動 id，猜不到
{
  customerId, customerName,
  month,                     // 'YYYY-MM'，要問的那個月
  validFrom, validTo,        // 收下之後那份收集的有效期，預設整月
  expiresAt,                 // Timestamp。Rules 擋過期靠它 ——
                             // Rules 沒辦法把 'YYYY-MM-DD' 跟 request.time 比大小
  sentAt,                    // 'YYYY-MM-DD'，她產生連結那天
}

// formResponses/{token}        id 就是 token —— 一條連結只有一份答案
{
  token, customerId, customerName, month,
  weekdays: [{ weekday: 0-6, partOfDay: 'am'|'pm'|null }],
  dates:    [{ date: 'YYYY-MM-DD', partOfDay: 'am'|'pm'|null }],
  freeText,                  // 客戶自己打的，照抄
  submittedAt,               // Timestamp
  takenAt,                   // null；她在收件匣按「收下」之後才有
}
// 這兩個集合是整份 Rules 裡唯一讓**沒有登入的人**寫得進來的地方。
// 「不重填」是 Rules 保證的：第二次送出在 Firestore 眼中是 update，而客戶沒有
// update 的權限。get 與 list 一定要分開寫，否則任何人都查得出全部客戶的名字。
// 見 docs/adr/0031-the-customer-form-is-its-own-page.md

// visits/{id}
{
  customerId, customerName,   // 冗餘存名字，避免清單頁 N+1 讀取
  date,                       // 'YYYY-MM-DD'
  status,                     // 'pending_confirm' | 'confirmed'
                              // | 'done' | 'no_show' | 'cancelled'
                              // 沒有 draft：app 裡不存在還沒壓表的來訪（第 4.1 節）
  confirmedAt, cancelledAt, cancelReason,
  note,                       // **舊資料才有，不再寫入**（ADR-0084）。
                              // 2026-09-09 之前是「這一天的一句話」，現在
                              // 每一段自己有（上面的 `slot.note`）。她下次
                              // 存那一筆時搬到第一段，`visit.note` 清成 null
  followupNote, followupAt,   // 「問過了、客人還沒回」那一句，例：'禮拜一再問問'
                              // 只活在 pending_confirm 這一段，確認或取消時清掉。
                              // 和 note 是兩回事：note 是壓表當下的事實，
                              // 這一句是「追這件事追到哪了」。轉成已確認之後
                              // 它就過期了，見第 8.1 節的確認動線
  released,                   // 取消後時段是否已釋出供遞補
  importedFrom,               // 從舊試算表匯進來的才有，例：
                              //   { source:'legacy-sheet', sheetName, importedAt }
                              // 有這個欄位的來訪，時段的 startsAt / endsAt /
                              // equipmentId / ivProductId / roomId / therapistId
                              // / doctorId 一律是 null —— 舊表沒有記過那些，見
                              // docs/adr/0011-imported-visits-are-incomplete-on-purpose.md
  slots: [
    { entitlementId,          // **n返 是 null**：它沒有被買、沒有次數、扣不掉
                              // （ADR-0063）。其餘每一種時段都一定要有一筆額度。
      courseId, courseName,
      equipmentId,            // pool 型態時這次選的器材
      ivProductId,            // 營養點滴品項
      startsAt, endsAt,
      roomId, bed, therapistId,
      doctorId,               // 這次是哪位醫師。picksDoctor() 為真的課程才有
                              // （A 類一律，其餘看 requiresDoctor）。
                              // 既有的來訪一律是 null，不要猜
      followupForVisitId,     // 這一段接在哪一次健檢後面。二返與 n返 都用它。
                              // **二返是選填**（舊資料一筆都沒有，ADR-0011），
                              // **n返 是必填**（它全新，而且少了它試算表上
                              // 那一場沒有位置可以印）
      followupNth,            // n返 才有：3、4、5……（3–10）。二返身上**沒有
                              // 這個欄位**，兩者靠它分辨。名字由它組
                              // （3 → 三返），課程借二返那一個。見 ADR-0063
      attended,
      status,               // 'pending_confirm' | 'confirmed' | 'done'
                            // | 'no_show' | 'cancelled'（ADR-0081）
                            // **狀態的原子單位是這裡**，整筆那一個是推導出來
                            // 又存起來的。舊來訪身上沒有這一格，`slotStatus()`
                            // 會退回整筆 + `attended` —— 一個字都不用遷移
      note }                // 這一段記的一句話（ADR-0084，上限 NOTE_MAX）。
                            // **2026-09-09 之前這一句掛在整筆上**（底下的
                            // `note`），她下次存那一筆時由 `withSlotNotes()`
                            // 搬到第一段
  ],
  createdBy, createdAt, updatedAt, deletedAt
}

// tasks/{id}
{
  visitId,                    // 可為 null（獨立待辦）
  customerId, customerName,
  kind,                       // '打電話' | 'Abovee' | 'Examine' | '耀聖' | 自訂
  dueDate, done, doneAt, note,
  autoGenerated, deletedAt
}

// events/{id}  —— 行事備註。不綁客戶、不產生任務、不扣次數。
{
  title,                      // '宜蘭休假'
  category,                   // 'personal' | 'leave' | 'work'
  color,                      // 色票 key，日曆上的色條顏色
  startDate, endDate,         // 'YYYY-MM-DD'。endDate >= startDate，可跨天
  allDay,                     // bool。false 時才看 startTime / endTime
  startTime, endTime,         // 'HH:MM'，allDay 時為 null
  note,
  createdBy, createdAt, updatedAt, deletedAt
}
// 這是唯一可以跨天的資料。來訪的定義是「客戶某一天到院一次」，跨天的來訪不存在。
// 為什麼不跟 visits 合成同一個集合，見
// docs/adr/0015-calendar-is-a-first-class-surface.md

// notes/{id}  —— 隨手記
{
  text,
  customerId, customerName,   // 可為 null（不掛在任何人身上的雜事）
  date,                       // 選填，'2026-09-03'。掛了就出現在日曆「待辦」那一類
                              //   空字串一律正規化成 null —— 日曆是用
                              //   where('date', '>=', …) 撈的，null 撈不到而
                              //   空字串會排在所有日期前面（ADR-0044）
  done, doneAt,
  createdBy, createdAt, updatedAt, deletedAt
}
// 沒有死線，所以不是任務。任務是來訪自動產生的，隨手記是她自己打的。
// 上面那個 date **不是死線**：它是「我想在這一天處理」，過了只有字變色，
// 不進「逾期」那顆數字。見 docs/adr/0044-a-dated-note-goes-on-the-calendar.md。

// batches/{id}  —— 一次壓表作業
{
  targetMonth,                // '2026-09'
                              // 沒有 courseId —— 一批是一個月的一串客戶，
                              // 不是一個課程。見 ADR-0014
  queue: [                    // 建立時算好並凍結順序，避免換裝置後順序跳動
    { customerId, customerName, state, skippedReason }
    //   state: 'pending' | 'done' | 'skipped'
  ],
  cursor,                     // 最後停留在哪一位（換裝置回來時捲到這裡）
                              // 完成度一律由每筆 queue entry 的 state 推導，
                              // 不從 cursor 推 —— 她是跳著處理的，不是照順序推進
  status,                     // 'active' | 'finished' | 'abandoned'
  createdAt, updatedAt, lastDeviceHint
}

// playbooks/{id}  —— 備忘錄／SOP。她自己整理的流程，綁課程不綁人
//                      （ADR-0067、ADR-0069）
{
  title,                      // '營養點滴'。<= 40 字
  courseIds,                  // ['course-iv-drip']。掛哪些課程，選填
  partners,                   // ['自然美']。掛哪些合作機構，選填（ADR-0076）。
                              //   **存字串**：要跟客戶身上那一份比對，
                              //   而那一份存的也是字串
                              //   掛了才會在日曆／確認動線上自己浮出來；
                              //   外檢這種系統裡沒有的項目不用掛，寫在標題就好
  body,                       // 多行純文字，一行一件事。<= 5000 字
                              //   **就這一格**。章節、分類、釘選、時機都是
                              //   2026-09-04 拿掉的（ADR-0069）：她要的是
                              //   「像一般的備忘錄 app 一樣，一個大框框」
  createdBy, createdAt, updatedAt, deletedAt
}
// 順序照 createdAt，新的在最後面 —— 一疊卡不可以在她編輯完之後自己重排。
// 它不勾、不上日曆、不產生任務。要勾的那一種是隨手記與任務，見 CONTEXT.md。

// config/app/clinicalFlags/{id}  —— 警示（ADR-0074）
{
  name,                       // '體內金屬'。<= 12 字，卡片牆上那一排要掃得完
  hint,                       // 一句說明，選填。只出現在客戶的永久限制編輯畫面
  color,                      // 六色之一，跟客戶備註同一份名單。預設 'tea'
  fill,                       // 'solid' | 'outline'。預設 'outline'
                              //   —— 系統分不出哪一個對她比較重要，那是她的判斷
  active, deletedAt
}
// 客戶身上存的是**字串**不是 id（同醫療禁忌），所以改名不會自動搬既有客戶。

// audit/{id}
{
  at, actor, action,          // 'visit.create' | 'visit.cancel' | ...
  targetPath, before, after
}
```

> `queue` 在批次建立時就算好並固定順序。如果每次開啟都重算排序，她換裝置回來會發現順序變了、不知道處理到哪 —— 這正是現在白紙的問題。順序凍結，但每張卡片的即時資訊（剩餘次數、可用天數）仍然現算。

### 5.4 需要的複合索引

**軟刪除是查詢的一部分**（第 6.1 節：永不硬刪除），所以會被查的集合一律以
`deletedAt` 開頭 —— 少了它，刪掉的東西會照樣被撈回畫面上。

- `visits`: `date asc` + `status`；`customerId` + `date desc`
- `events`: `deletedAt` + `endDate asc`；跨天的要靠 `endDate >= 月初` 再過濾，見 `domain/events.js`
- `notes`: `deletedAt` + `done` + `createdAt desc`；`deletedAt` + `customerId` + `createdAt desc`；
  `deletedAt` + `date asc`（日曆上「待辦」那一類，ADR-0044）；`deletedAt` + `createdAt desc`
- `tasks`: `done` + `dueDate asc`；`visitId`；`done` + `doneAt desc`（待辦那幾頁的「已完成」那一格）
- `customers`: `active` + `name` —— **目前沒有任何查詢用得到它**：`data/customers.js`
  的 `list()` 只濾 `deletedAt`，排序刻意在 client 做（Firestore 排的是 UTF-8
  位元組序，中文姓名排出來不是人看的順序），`active` 也是在畫面上濾的。
  留著是為了多帳號那一階段（第 11 節）—— 到那時候客戶清單會大到不能整包拉回來。

實際的那一份在 `firestore.indexes.json`，這裡只講為什麼。

### 5.5 任務產生規則矩陣

| 類別 | 課程 | 壓表登記在哪 | 客人確認後產生的任務 |
|---|---|---|---|
| A | 復健科、心臟科、二返（功能醫學門診） | Abovee | Examine、耀聖 |
| B | 健檢 | **Examine** | 無 |
| C | 復能、ILIB、EECP、營養點滴 | Abovee | 無 |
| 不用掛號 | Inbody、體適能分析、兩種諮詢 | Abovee | 無 |

任務規則綁在**類別**上，不逐課程設定。課程只存 `category`，新增課程時選一個類別即可。

**「壓表登記」不是任務。** 壓表就是在 Abovee 上把時段佔住（`CONTEXT.md`），
而來訪的起點是「已壓表」（第 4.1 節）—— 那筆來訪能存在，前提就是這件事已經做完了。
所以它不產生待辦，只在兩個地方出現：待辦中心那一列「壓表登記」（推導的提醒，
講的是「這個月還有誰沒排」，第 8.1 節），以及取消時要回去哪個系統放掉時段。
理由見 `docs/adr/0041-the-sheet-is-the-registration.md`。

**沒有「打電話」這一種。** 2026-08-23 拿掉，所有類別都不再產生。

### 這張矩陣只講「客人確認之後」那一族。還有第二族。

任務有**兩個時機**（`docs/adr/0066-a-record-is-a-task-that-is-born-after-the-visit.md`）：

| | 掛號那一族 | 紀錄那一族 |
|---|---|---|
| 種類 | Examine、耀聖 | **寫紀錄** |
| 什麼時候長出來 | 客人**確認**之後（ADR-0027） | 那一場**做完**之後 |
| 死線 | 來訪日的**前一天** | 來訪**那一天** |
| 由什麼決定 | 課程的**類別**（上面那張矩陣） | 課程主檔上的 `needsRecord` |
| 取消來訪時 | 要回頭去外部系統收回登記 | 沒有東西要收 —— 那一場沒發生 |
| 未到（`no_show`） | 已經做掉的登記照樣要收回來 | **不長** —— 人沒來，沒有紀錄要寫 |

**紀錄那一族逐課程不逐類別**，判準是「同一個類別裡的課程會不會不一樣」：
二返是 A 類、營養師諮詢是「不用掛號」，而同樣 A 類的復健科醫師門診不用寫。
既有的逐課程欄位還有 `needsTreatmentForm`、`followupCourseId`、`requiresDoctor`、
`requiresEquipment` —— 這是第五個。

兩族在程式裡也各自有一支「該有哪些」與一道閘門，而且
**`tasksForVisit()` 刻意不看狀態、`recordTasksForVisit()` 刻意看**，
理由在 ADR-0066：前者同時被拿來比對「哪些還該留著」，跟著狀態變的話
來訪一結案她還沒做完的 Examine 就會被靜默收掉；後者相反，那一場沒做完
就沒有東西可以寫。

### 任務跟著來訪走

任務不是建立來訪時產生一次就結束，來訪之後怎麼變，任務要跟著變：

| 來訪發生什麼事 | 任務怎麼動 |
|---|---|
| **客人確認**（`pending_confirm → confirmed`） | **這時候才產生登記任務。** 壓完表存下去的那一刻只長出「跟客人確認時間」那一列（第 8.1 節），因為那幾件登記要不要做還沒有答案。見 `docs/adr/0027-registration-tasks-wait-for-the-customer.md` |
| 改日期 | 死線跟著移，已完成的仍然是已完成 |
| 課程被移出這次來訪 | 還沒做的收掉；**已完成的一律不刪**，那件事真的做過了 |
| 取消或刪除 | 還沒做的收掉 |
| 取消或刪除（無條件） | 產生 `取消 {壓表登記的系統}` —— 來訪存在就代表她壓過，那個時段要放回去 |
| 取消，而且**已經勾掉登記任務** | 再各產生 `取消 Examine` / `取消 耀聖`，死線是今天（SPEC 第 4.1 節） |
| **只取消其中幾段**（ADR-0081） | 跟上面兩列一樣，但**只算取消掉的那幾段**：那一段壓在哪就收哪；那一段自己長得出 Examine／耀聖、而且已經勾掉了才收那兩個 |

**「還沒確認就不產生」擋的只有無中生有那一種。** 已經在的任務一個都不會因為狀態走到別的地方而被收掉：`confirmed → done` 不會把她還沒做完的登記洗掉，而**直接標成已完成**（`pending_confirm → done`，她補記一筆已經上完的課）也不會突然長出一串死線早就過了的紅字 —— 那天已經過完了，掛號沒有東西要補。規則在 `domain/taskRules.js` 的 `acceptsNewTasks()`。

「已經登記過」指的是 Examine、耀聖這兩種會在外部系統留下東西的任務已被勾完成（歷史資料裡還有勾掉的 `Abovee` 任務，一樣算）。**打電話做過就是做過了，沒有東西要收回來**，不產生取消任務。取消類任務的 `kind` 是原本的種類加上 `取消 ` 前綴，產生後就不再受來訪現況管轄 —— 它記的是「當初登記過、現在要收回來」這件事。

**取消類任務逐段，不逐天。** 每一張記著自己收的是哪幾段（`slotIndexes`），一段還沒被任何一張蓋到才長新的一張；同一次存檔取消的幾段收成同一張。以前是「同一個系統只長一張，含已經勾掉的」，於是第一張勾掉之後隔天再取消同一天的另一段，一張都不長 —— 那一格在 Abovee 上永遠沒人去放。2026-09-13 之前長出來的那幾張沒有 `slotIndexes`，當成蓋住那一天的每一段。**只取消一段時 Examine／耀聖要不要收，她還沒定案**（暫定：要，因為「Examine 上是一段登記一筆」）。規則在 `domain/taskRules.js` 的 `cancelTasksFor()`，確認框那一句走同一支。見 `.scratch/asks-2026-09-13/issues/02`。

> **不要照抄舊 `.gs` 的分支。** 它用課程名稱做字串包含比對，療程一改名就靜默失效，而且給的任務清單是舊的。

### n返（三返、四返……）走的是課程那一條，不是這一條

加約的 n返 **借二返那個課程**（`followupCourseId` 指到的那一個），所以它的
掛號任務、要不要簽療程單、選不選得到醫師，全部由上面那張矩陣照 A 類推導 ——
一行程式都不用寫。它與二返在資料上完全不相交（`entitlementId` 是 `null`），
所以底下那兩種鏈式待辦一個都不會因為它而多長或少長。見
`docs/adr/0063-an-nth-followup-is-a-visit-without-an-entitlement.md`。

### 健檢那條鏈上的三種不在這張矩陣上

它們是**不由課程類別推導**的自動任務，因為它們問的是另一個問題：不是
「這次來訪要去哪幾個系統登記」，而是「這位客戶還欠幾次二返、報告到了沒」。

```
                                   ┌─▶ 寄報告給醫師   死線 勾掉那天+7
健檢結案 ─▶ 追蹤健檢報告 ──勾掉──┤
             死線 健檢日+21        └─▶ 約二返         死線 勾掉那天+7
```

| | 掛號那兩種 | 追蹤健檢報告 | 寄報告給醫師 | 約二返 |
|---|---|---|---|---|
| 從哪裡推導 | 來訪裡的課程類別 | 這位客戶的健檢額度與二返額度 | 那一筆健檢的追蹤報告勾了沒 | 同左二 |
| 死線 | 來訪日的前一天 | 健檢日 + `settings.reportDueDays`（預設 21 天） | **勾掉追蹤報告那一天** + `settings.followupDueDays`（預設 7 天） | 同左 |
| **長出來**受「還欠幾次二返」管嗎 | —— | 是 | **否**（見下） | 是 |
| **留著**受「還欠幾次二返」管嗎 | —— | **否**（ADR-0068） | 否 | 是 |
| 取消時 | 已登記過的要產生「取消 ⋯⋯」 | 沒有東西要收回，還沒做的直接收掉 | 同左 | 同左 |

**「寄報告給醫師」刻意不受 `owed()` 管**，這是最容易被「修掉」的一格
（`docs/adr/0065-two-things-happen-when-the-report-arrives.md`）：她一去把二返約好，
`owed()` 就掉到 0、那一次健檢整個退出候選 —— 兩張擠在同一圈裡的話，
**還沒寄出去的報告會在那一刻被靜默收掉**。所以它走第二圈，條件只有兩個：
報告那一張勾掉了，而且那一筆健檢還是已完成的。

**「長出來」與「留著」是兩條規則**（ADR-0068）。一張**未完成**的「追蹤健檢報告」
只有一個收掉的理由：**那一筆健檢不再是已完成的**。二返壓好了、或「約二返」被
勾掉了都**不算** —— 那是系統的推論，而她把那一張勾回去是她的決定，兩個衝突時
她贏（ADR-0002）。2026-09-04 之前不是這樣，症狀是她一反悔那一張就被靜默軟刪除。

規則在 `domain/followups.js` 的 `syncFollowupTasks()`，**不在 `syncTasksForVisit()` 裡** ——
那一支的視野只有一筆來訪，看不到「另外那兩次健檢的二返已經約掉了」。
那一支也必須把這三種擋在它的比對之外（`CHAIN_KINDS`），否則健檢一存檔就會把
剛長出來的追蹤報告當成多餘的任務收掉。
約好之後的二返就是一筆普通的來訪，走 A 類的登記任務 —— 而且它做完之後
還會多一張「寫紀錄」（上一小節）。見
`docs/adr/0022-followup-entitlements-are-expanded-in-pairs.md`、
`docs/adr/0042-the-report-comes-before-the-follow-up.md` 與
`docs/adr/0065-two-things-happen-when-the-report-arrives.md`。

---

## 6. 資料正確性與防呆（高優先，使用者明確要求）

使用者的要求原話：**「希望 app 的資料不能消失或誤觸不見，也不能有誤，希望有檢查機制以及防呆。」**

這一節的東西不是加分項，請在對應功能一開始就做進去，不要事後補。

### 6.1 永不硬刪除

- 所有集合都有 `deletedAt`。刪除 = 寫入時間戳，資料留著。
- **Firestore Security Rules 直接禁止 `delete` 操作**，只允許 `update` 標記。
- 清單查詢一律過濾 `deletedAt == null`。
- 設定頁提供「已刪除項目」檢視，可還原。

### 6.2 稽核紀錄（append-only）

每一次寫入都往 `audit` 追加一筆，記錄 `before` / `after`。

- Rules 只允許 create，禁止 update / delete。
- 客戶詳情頁提供「這筆資料的變更歷史」。
- 出事時可以回溯任何一筆資料怎麼變成現在這樣。
- **一則稽核先是一句話，展開才是一張差異表。** 那一句話要講得出
  **誰、哪一天、哪一項**（例：「新增 客戶A・9/14(日)・復能、營養針」
  「客戶A・二返（12萬健檢）・已排未上 1 → 2 次」），翻譯不出來才退回欄位表。
  規則只在 `domain/audit.js` 的 `describeParts()`，`#/settings/audit`、
  客戶詳情的「變更紀錄」與待辦中心的「看今天做了什麼」都讀它。
- 額度與本輪可用性是**子集合**，文件上沒有 `customerName`（只有來訪、任務、
  隨手記有那個冗餘欄位），所以它們的名字靠路徑上的 id 由畫面解析後傳進來。
  **問不到就不講名字，不編一個。** 刻意不回頭替 `audit` 補欄位：既有的補不上，
  補了會變成「新的看得懂、舊的看不懂」，而出事時要回溯的往往正是舊的那些。

### 6.3 復原（undo）

寫入後在畫面上顯示 5–10 秒的「復原」按鈕。有了稽核紀錄，undo 就是把 `before` 寫回去（並再記一筆稽核）。

手機單手操作最容易誤觸，這個功能的價值非常高。

### 6.4 唯讀鎖定區

- 狀態為 `done` 的來訪**不可直接編輯**。要改必須走「更正」流程：填理由 → 產生稽核紀錄 → 才允許修改。
- 已扣掉的次數不可直接改數字，只能透過來訪狀態變化連動。
- 主檔（診間、療程、方案範本）修改需二次確認，且要顯示「這會影響 N 位客戶」。
- **已展開給客戶的 entitlement 與方案範本脫鉤**，改範本不會動到既有客戶。

### 6.5 破壞性操作的動線設計

- 取消來訪、刪除客戶這類操作**不放在主要動線上**（不放在卡片右上角這種容易誤按的位置）。
- 需要滑動確認或長按，不是單擊。
- 確認對話框要顯示具體後果（「將取消 3 個時段，並產生 2 個『回去取消系統登記』的任務」），不要只有「確定嗎？」。

### 6.6 對帳檢查（reconciliation）

提供一個「資料健檢」頁面（`#/settings/health`），入口在設定頁，打開就掃一次。**沒有背景掃描** —— 原本啟動時會跑一次（ADR-0008），2026-08-24 連同待辦中心那顆徽章一起拿掉了，見 `docs/adr/0049-the-todo-centre-does-not-run-the-health-scan.md`：

| 檢查項 | 內容 |
|---|---|
| 次數對帳 | entitlement 的 `doneCount` / `bookedCount` 是否等於從 `visits` 重算的值 |
| 二返額度 | 有健檢額度卻沒有配對的二返額度（沒有額度，二返就記不進來） |
| 孤兒資料 | 指向不存在客戶／療程／診間的 visit 或 task |
| 逾期任務 | 死線已過但未完成 |
| 狀態異常 | 日期已過但仍是 `confirmed`（該標 done 或 no_show 了） |
| 額度超用 | 已排 + 已完成 > 總次數 |
| 資料過期 | 可用性收集已過有效期 |
| 同一個月有兩份 | 同一位客戶同一個月記了兩份不能的時間 —— 壓表只挑得到其中一份（ADR-0053） |
| 衝突殘留 | 同診間／同治療師同時段重複 |
| 備註寫著舊的說法 | 匯入寫的「姓名欄的編號：」，正確的說法是「病歷號」（ADR-0050） |

發現不一致時**顯示差異**，不要自動偷偷改。這一頁的預設是**只讀不寫**（`docs/adr/0007-health-check-reads-only.md`），其餘一律只顯示差異並提供跳到那一筆的連結，要怎麼處理是她的決定（第 4.7 節、ADR-0002）。

**一項要有「一鍵修正」，三個條件都要成立：**

1. **有明確正解** —— 不需要她判斷該變成什麼。
2. **沒有第二種可能的意思** —— 不像「健檢 3 次、二返 2 次」那樣可能是她故意的。
3. **沒有別的地方做得了** —— 這個 app 沒有後台，她只有畫面，跑一次就丟的腳本她跑不了。

目前符合的有三項：**次數對帳**（真相永遠是 `visits`，ADR-0004）、**二返額度缺配對的那一種**（買幾次健檢就有幾次二返，`docs/adr/0023-health-check-can-also-create-the-missing-followup.md`）、**備註寫著舊的說法**（「姓名欄的編號：」改成「病歷號」，`docs/adr/0050-the-health-check-can-rename-an-imported-note.md`）。

**這裡寫的是條件不是數量**：再多一項就補一支 ADR 說明它怎麼符合這三條，不必回來改這一句 —— 上一次加第三項時這裡還寫著「只有兩項」，而那是畫面與規格對不起來。

二返額度那一項裡「次數對不上」的那一種**不給一鍵修正**：健檢 3 次、二返 2 次可能是她故意的（其中一次的報告用電話講完了）。「完全沒有」與「數字不一樣」是兩種問題。

### 6.7 雙層寫入驗證

1. **前端**：送出前驗證（必填、時間合法、禁忌、診間限制、額度足夠）。
2. **Firestore Security Rules**：同樣的關鍵規則再擋一次（欄位型別、必填、禁止刪除）。狀態轉移的合法性是例外，只在前端擋，理由見 `docs/adr/0006-status-transitions-are-not-enforced-in-rules.md`。

前端可以被繞過，Rules 不行。兩層都要有。

### 6.8 備份

- 設定頁提供「匯出全部資料為 JSON」按鈕，手機可下載。
- **含已刪除的資料** —— 備份漏掉軟刪除的東西，就救不回誤刪，而誤刪正是最需要備份的情境。
- 稽核紀錄預設不含，另給一個勾選框：它每次寫入都追加一筆，累積起來可能比其他資料加起來還大。
- 建議每月手動匯出一次存到雲端硬碟。
- Firestore 本身有 PITR，但需要 Blaze 方案，先靠手動匯出即可。

### 6.9 樂觀更新要誠實

寫入未確認前不要讓畫面顯示成功。網路不穩時（她在公司大樓裡用行動網路）要明確顯示「儲存中／儲存失敗，點擊重試」，不能靜默失敗。

### 6.10 舊資料匯入

**只有一條路**：貼合併檔（`#/settings/merge`），寫入端是 `data/legacyImport.js` 的 `importPlan()`。

吃的是舊試算表 × 行事曆比對過的 `import.json`（`.claude/skills/calendar-sheet-merge` 產生），
時間、診間、治療師、器材都補得進來。

**「把試算表文字直接貼進 app」那條路 2026-08-23 拿掉了。** 它讀得到的東西是合併檔的
子集（來訪只有日期），而兩條路共用同一個寫入端 —— 留著等於留一個只會匯進比較少
東西的入口。舊表的解析器（`domain/legacyImport.js` 的 `parseSheet()` / `planForSheet()`）
留著，現在由 skill 那側呼叫。見 `docs/adr/0047-there-is-one-way-in.md`。

從 20+ 張試算表工作表匯入時：

- 先跑 **dry-run**，產出比對報告（會建立幾位客戶、幾筆額度、幾筆來訪，有哪幾筆解析不了）
- 人工確認後才真正寫入
- 匯入的資料標記 `importedFrom` 來源，方便日後追查
- 一位客戶是一個原子單位（客戶＋額度＋來訪同一次寫入），一位失敗不影響其他人
- 同名的客戶整張跳過，所以重複貼同一張不會建出第二份
- **來訪狀態依匯入當下的日期決定**：日期在今天之後的建成「已確認」（她在舊表上也會先把未來的預約寫進去，那一格的勾是「排了」不是「來了」），今天含以前的維持「已完成」。判斷只在 `domain/mergeImport.js` 的 `statusFor()`，產檔那側一律吐 `done` —— 「未來」是在她按下匯入的那一刻才算得準的
- **行事曆上對不到客戶的那些分成三類**：休假、待辦、行事備註。產檔那側照標題出建議（`classifyEvent()`），這一頁每一列都可以改。待辦寫進 `notes`（掛了日期的隨手記，ADR-0044），另外兩種寫進 `events`
- **文字裡提到醫療禁忌的那幾位排在最上面**：舊表沒有「永久限制」這個欄位，那幾句話寫在購買名稱或空白處。匯進來 `flags` 是空的，而擋器材是拿 flags 比對的 —— 只提示，不自動填（ADR-0002）
- **任務逐筆問一次，不是整批不產生**：已經發生的那些一筆都不長（那些掛號在舊系統早就做完了，產生出來只會是一批一出生就逾期的紅字），還沒發生的那些照 `acceptsNewTasks()` 該長就長 —— 她要去另外兩個系統登記、要確認當天出席簽療程單，那些事是真的還沒做

舊表的結構與已知陷阱（購買數量是反推的、`0.75萬健檢` 會被舊正則讀成 75）在
`docs/legacy/README.md`。三個實作上的決定：資料怎麼進來見
`docs/adr/0012-legacy-import-is-a-paste-not-an-integration.md`，
匯進來的來訪為什麼缺欄位見
`docs/adr/0011-imported-visits-are-incomplete-on-purpose.md`，
合併檔那條路的狀態為什麼要看日期見
`docs/adr/0029-imported-visits-take-their-status-from-the-date.md`。

---

## 7. 業務規則

**每一條都只提示，一條都不擋。** 見 `docs/adr/0002-app-records-decisions-it-does-not-make-them.md`。
第 1 條本來是唯一的例外，2026-09-06 收掉了（`docs/adr/0074`）。

1. **器材提醒（提示）**：`customer.flags` ∩ `equipment.contraindications` ≠ ∅ → 她選了那一台的時候跳一句明顯的提醒，並且**建議改用哪一台**（那一池裡沒有東西要提醒的那幾台）。**不擋** —— 「只要儀器不要在金屬的上方或附近」就做得了，而儀器擺在哪裡 app 看不到。
2. **診間限制與順序（提示）**：課程主檔上三個欄位回答三個不同的問題 ——
   `allowedRoomTypes`（能排在哪一類）、`allowedRoomIds`（例外：只有這幾間，
   **硬限制**）、`preferredRoomIds`（**只是順序**，排最前面）。
   已知：**EECP 只能在 治療室5、治療室8**。見 `docs/adr/0079`。
3. **誰／哪裡（提示）**：2026-09-08 收斂成三條（`docs/adr/0079`）——

   | 要什麼 | 哪些課程 |
   |---|---|
   | 物理治療師 | 復能（INDIBA／SIS／高能量雷射）、物理治療師諮詢 |
   | 治療室（空間） | 營養點滴、EECP、ILIB |
   | 醫師 | 門診類（A 類一律選得到，`picksDoctor()`） |
   | 都不用 | 健檢、體適能、身體組成分析、營養諮詢、門診、二返、心臟科評估 |

   **擇一池那一段看的是選到哪一台**（ADR-0075）：四選一選了 ILIB 就要治療室，
   選了其他三台就要治療師。**挑之前那個問題沒有答案** —— 兩排都不畫，
   只留一句「先選上面那一台」（`assignsFor()`）。
4. **來訪內時段間隔**：預設 15 分鐘，可設定。
5. **自我衝突檢查（提示）**：同一診間／同一治療師同時段重複時顯示提醒，但不阻擋儲存。跨同事的衝突以 Abovee 為準，app 看不到。
6. **頻率限制（提示）**：`每季一次`（身體組成分析、體適能檢查分析）→ 排太密時顯示「上次是 X，距今 N 天」，不阻擋。
7. **方案到期**：會籍 1 年。快到期又剩很多次的客戶要浮到排班佇列上方。
8. **健檢完成 → 追蹤報告 → 約二返**。四段：
   - **額度**：建立健檢額度時成對建出一筆同次數的二返額度（買 N 次健檢就有 N 次二返）。
   - **追蹤報告**：健檢那一筆來訪標成已完成 → 長出「追蹤健檢報告」，死線是**健檢日 + `settings.reportDueDays`**（預設 21 天）。**報告通常兩三週才出來**，所以這一站不能跳過 —— 跳過的話「約二返」會在第 8 天變紅，而那兩週她做不了任何事。
   - **約二返**：「追蹤健檢報告」勾掉了（＝報告拿到了）→ 才長出「約二返」，死線是**勾掉那一天 + `settings.followupDueDays`**（預設 7 天）。只在還欠二返時產生；健檢被取消或標成未到時，兩張還沒做的都跟著收掉，已完成的一律不刪。
   - **來訪**：約好的二返就是一筆普通的來訪，走第 4.1 節的狀態機與 A 類的登記任務（Examine、耀聖）。**二返不用簽療程單**（規則 9）。

   **這是整個系統唯一一段鏈式的待辦。** 其餘一律在客人確認的那一刻一次長齊 —— 鏈式會把「前一步沒做」變成「後一步不存在」，而簽療程單那一張如果因為掛號沒勾就不出現，最需要提醒的那一筆就沒有提醒了。

   **系統不會自動幫她約，也不會自動去問報告** —— 它只負責記得「這件事還沒做」（ADR-0002）。實作在 `domain/followups.js`，設計理由見 `docs/adr/0022-followup-entitlements-are-expanded-in-pairs.md` 與 `docs/adr/0042-the-report-comes-before-the-follow-up.md`。
9. **療程單（提示）**：來訪當天要請客戶簽療程單，**除了二返，每一個課程都要簽** —— 二返是回院聽報告，沒有療程可以扣。要不要簽記在課程主檔的 `needsTreatmentForm` 上（沒有欄位就是要簽，認不得的課程也當成要簽）。判斷在 `domain/visits.js` 的 `needsForm()`，畫面不要自己認課程名字。簽單這件事不是任務，它就是待辦中心「簽療程單」那一列的收尾動線本身。
10. **取消即釋出**：客戶取消 → 該時段進入「可釋出時段」清單，供她的其他客戶或同事的客戶遞補。**改期 = 取消 + 重新排**，不是修改日期。
11. **壓表前確認**：標記「已壓表」時跳出「已在 Abovee 確認診間無人使用？」。
12. **雙段勾核**：「客人已確認」與「已登記」是兩個獨立狀態，不可合併成一個「完成」。

---

## 8. 畫面規劃

### 8.0 版型斷點

以**容器寬度**判斷，不是裝置型號（iPad 會用 Split View，寬度會變）。

| 斷點 | 對應 | 版型 |
|---|---|---|
| `< 600px` | 手機、iPad 分割畫面窄側 | 單欄。一次一件事，卡片堆疊。 |
| `600–899px` | iPad 直式 | 單欄但更寬，列表資訊密度提高，可並排放兩個小卡。 |
| `≥ 900px` | iPad 橫式 | 雙欄。左側清單／佇列，右側詳情或日曆。 |

共用規則：最小點擊區 44px、無 hover 依賴、無拖拉。

**「無拖拉」講的是不靠拖拉就做不到的功能。** 手勢可以是快捷方式：壓表的卡片組左右滑換下一位（ADR-0017）、日曆左右滑換月份、底部面板上下拖（`docs/adr/0020-the-calendar-never-changes-pages.md`）—— 這三件事都另外有按鈕做得到。

**底部面板整張都拖得動，不是只有上面那條橫桿。** 三個停的位置：貼著內容的那一格、拉到頂、收起來；從頂上往下甩一次只回到前一格，再一次才收。面板裡會捲的那一塊維持瀏覽器原生的捲動，捲到頭之後同一個手勢才交給面板。**過場只動 `transform`，不動 `height`** —— 動 height 會每一幀重排一次，看起來就是斷一下才跳過去。細節與踩過的坑見 `docs/adr/0021-the-sheet-is-dragged-by-transform.md`。

**點擊區 44px 講的是感應範圍，不是視覺高度。** 泡泡、分段切換、小標籤的視覺高度是 28–34px，用 `::after` 把感應範圍撐回 44px —— 看起來輕，按起來一樣準。

**字級：input 一律 16px 起跳**（iOS 上小於 16px 的 input 會觸發自動放大，畫面會亂跳），其餘內文 15px。**縮小的是包裝，不是資料** —— 外框、標籤、按鈕、說明文字收小一級，姓名、剩餘次數、日期時間這些她真正在掃的東西維持大字。字體見 `docs/adr/0016-web-fonts-with-a-system-fallback.md`。

**一排丸子超過一行就改成左右滑，不換行往下堆** —— 換行會把它底下的內容一路往下推，而那些內容才是她要看的。

**iPad 上放得下兩張卡就放兩張**（`.cardgrid`）。多的是「同時看得到」，不是多出來的功能。

#### 返回鍵：畫面上多一層東西，就多一步退得掉的路

她的主力是 Android 手機，那顆返回鍵是實體的、隨時在手邊。**她按它的意思通常是
「關掉我剛打開的東西」，不是「離開這一頁」。**

所以底部面板、浮出的讀取卡片、二次確認，以及**原地換掉整頁 `innerHTML` 的畫面**
（客戶詳情的「編輯」「加購」、可用性的「記一次」、主檔的編輯表單）——
一律各推一筆瀏覽器紀錄，走 `ui/nav.js` 的 `pushLayer()` / `pushScreen()`。
推的是 `pushState(state, '', location.hash)`，**網址一個字都不會變**，
所以不觸發 `hashchange`、路由不重畫。

三條配套的規矩：

- **「回上一頁」要真的退**（`back(fallback)`），不是把 `location.hash` 再推一筆 ——
  推的話「客戶列表 → 客戶詳情 → 按左上角的『客戶』」會在紀錄裡留下三筆，
  再按返回鍵會回到她剛剛才離開的地方。有沒有東西可以退**自己數**，
  不看 `history.length`（她從 LINE 點連結進來時那個數字跟這個 app 無關）。
- **存檔之後要把那幾層收掉**，不然存完按返回鍵會回到一張已經存過的表單，
  看起來像沒存進去。
- **一層不等於一次重畫。** 同一個畫面重畫（換額度型態、點一天）要靠 key
  認出來，不再疊一層；壓表的卡片組整組只算一層，換人不推
  （`docs/adr/0052-the-deck-is-one-layer.md`）。

`pushState` 有頻率上限（Safari 每 30 秒），所以**高頻的互動一律不推** ——
選一顆丸子、換一個日子、滑到下一位都不是「多一層東西」。
理由與踩過的 race 見 `docs/adr/0048-a-layer-on-screen-is-a-step-back.md`。

### 8.1 首頁 — TODO 中心

**主力裝置：手機**（走動時勾待辦）。iPad 橫式時左待辦、右選中項目的詳情。

**這一頁只放分類與數字，完整內容一律點進去看。** 她開這一頁是為了知道「現在有幾件事、哪一件最急」，不是為了讀完所有細節。

頂端三顆大數字做分流：**逾期／今天／明天**。底下是分類清單，**一列只有標題與數字**。

**說明文字不印在總覽上**（2026-09-04）。底下那張表裡的說明是規格，不是畫面文案 —— 它們印在**點進去那一頁的第一行**（`GROUPS[].lead`）。她掃這一頁是在問「接下來做什麼」，而十三句說明回答的是「這十三種各自是什麼意思」，那是第一次用的人的問題。「Examine：預約作業 → 查核 → 已報到」在她按下去之後才有用，見 `docs/adr/0072-a-row-on-the-todo-centre-is-only-a-title.md`。唯一的例外是已經取消的種類，那一列標題旁邊一顆寫著「舊資料」的小丸子。

**真的要做的那幾列才有數字。**「問這輪的時間」「壓表登記」是**提醒**（推導的、
沒有死線、沒有完成的定義），而她身上永遠有人還沒壓 —— 給提醒一個跟真任務一樣的
大數字，等於每天告訴她「你有九件事沒做」而那九件永遠不會歸零，看久了的結果是
整頁的數字都貶值。所以壓表那一列不給數字，整列也淡一級（2026-08-24）。

| 段 | 分類 | 是什麼 |
|---|---|---|
| ① 問到時間 | **問這輪的時間** | **那一列與點進去那一頁問的是兩個不同的問題，這是刻意的。**<br>**那一列**問「**現在**還沒問到誰」：身上還有次數、而且沒有有效本輪可用性的客戶。規則在 `domain/scheduling.js` 的 `customersToAsk()`（一個字都沒有變），見 `docs/adr/0028-asking-for-times-is-a-todo-row-not-a-queue.md`；已經發出連結的人為什麼還算在那個數字裡，見 `docs/adr/0033-a-sent-link-is-a-third-state-of-asking.md`。<br>**點進去那一頁**（2026-09-02 起）問「**某一個月**問到了誰」：頂端一組月份切換器（照客戶詳情「不能的時間」那一段的排版，共用 `components/monthnav.js`），預設下個月，上下不設限。兩個關鍵差別：**不拿次數當濾網**（她的原話：「不用看他身上還有沒有次數，就單純看他那個月有沒有被排過時間」——剩幾次仍然顯示，但只當提醒加購的線索），以及**用 `collectionFor()` 不是 `currentCollection()`**（這一頁綁月份，ADR-0036）。規則在 `customersToAskForMonth()`。<br>底下兩格加一個摺疊區：**「還沒發連結」**（那個月既沒連結也沒時間）、**「已經發出」**（那個月發過連結——**不會因為客戶填了或她收下就消失**，狀態寫成 `等待回覆` / `已填寫時段` / `已確認排定` 三顆徽章，判斷只在 `domain/availabilityForm.js` 的 `inviteProgress()`），以及摺疊起來的**「這個月已經問到了」**（她自己在 LINE 問完直接記的那幾位——整個藏掉最乾淨，但「東西不見了而畫面上什麼都沒說」是這個 app 反覆踩過的錯）。分區在 `splitByMonth()`。**產生的連結帶的是選中的那個月**，按鈕上也印出來。換分段不重新讀資料就地重畫，**換月份要重讀**（三份資料都是照月份挑的）；產生連結之後自動跳到「已經發出」並把該客戶的訊息展開——她的下一個動作是複製，不是找按鈕 |
| ① 問到時間 | **客戶填好的時間** | 客戶自己填了表單、她還沒收下的那幾份。**不會自動生效** —— 她按「收下」才變成本輪可用性，並附一則回覆客戶的訊息。見第 8.9 節與 `docs/adr/0032-what-the-customer-fills-in-waits-in-an-inbox.md` |
| ② 壓表 | **壓表登記** | 這個月還沒排的客戶 —— 她流程的第三步。**這一列是提醒不是任務**：壓表就是在 Abovee 上把時段佔住，做完了才有那筆來訪（ADR-0041），所以它沒有死線也不計分。點進去分兩區：**健檢直接去 Examine**、**其餘去 Abovee**，同一位客戶兩區都還沒排就兩區都出現。規則在 `domain/scheduling.js` 的 `customersToBook()` |
| ③ 等客人回覆 | **跟客人確認時間** | 壓好了、還沒問過本人的客戶。壓表記完會自動長出來，見下面的確認動線 |
| ④ 來訪前一天 | Examine、耀聖 | 任務，依 `domain/taskRules.js` 的種類分開。**沒有 Abovee 與打電話** —— 壓表本身就是 Abovee 登記，見 `docs/adr/0041`。已經拿掉的那兩種若還有未完成的舊資料，照樣列在這一段的最後面 |
| ⑤ 來訪當天 | **簽療程單** | 日子到了、還沒結案的來訪。客人來了、療程單簽了就打勾 —— **次數是這時候才扣的**（第 4.2 節）。和「跟客人確認時間」一樣是推導的，不是任務。**二返不用簽單**，但那一筆照樣要在這裡結案（「不用簽單」跟「不用收尾」是兩件事），卡片上會寫明 |
| ⑥ 來訪之後 | **寫紀錄** | 客人走了、還沒去曜聖補那一份文字紀錄。目前是二返與營養師諮詢（課程主檔上一個勾）。**死線是來訪那一天**，不是它的前一天 —— 這件事是當天做的（ADR-0066） |
| ⑥ 來訪之後 | **追蹤健檢報告** | 健檢做完了、報告還沒拿到。死線是健檢日 + 21 天（報告通常兩三週出來） |
| ⑥ 來訪之後 | **寄報告給醫師** | 報告拿到了，要寄一份給看報告的那位醫師。跟「約二返」同一站、死線一樣，但**不受「還欠幾次二返」管**（ADR-0065） |
| ⑥ 來訪之後 | **約二返** | 報告拿到了（追蹤報告勾掉了）、還沒回去跟客人約聽報告的時間。死線是**勾掉那天**往後算（第 7 節規則 8、ADR-0042） |
| ⑦ 要收回來的 | 改時間／取消 | 需要回頭取消舊系統登記的項目 |

**「寄報告給醫師」與「約二返」什麼時候長出來**：勾掉「追蹤健檢報告」的**那一刻**，
兩張跟那幾筆勾選寫在同一個 commit 裡（`data/tasks.js` 的 `setDone()` →
`data/visits.js` 的 `followupOpsAfterTaskChange()`）。2026-08-25 以前它只在
「這位客戶下一次有來訪被存檔」時才跑，所以勾完什麼都不會發生。

**⑥ 那一段裡的順序是「寫紀錄 → 追蹤報告 → 寄報告 → 約二返」**，不是照死線排 ——
寫紀錄是客人走了之後立刻做的，追蹤報告是三週後的事（ADR-0043）。

**右上角有一顆「備忘錄」**，跟客戶頁的「看這個月的進度」同一個位置與畫法。
它通到 `#/playbook`（第 8.10 節）。**導覽列不動** —— 五格變六格會讓每一格
從 20% 掉到 16.6%，而她九成的時間在前四格上。


**總覽這一頁照流程分七段**，段落的順序就是她真的在做的順序：①問到時間 ②壓表 ③等客人回覆 ④來訪前一天 ⑤來訪當天 ⑥來訪之後 ⑦要收回來的。編號講的是流程的第幾步，不是畫面上的第幾段 —— 中間某一段空了（很常見），其餘的編號不重新編。段落左邊那條線的顏色**就是那一筆來訪當下在日曆上的顏色**（琥珀／靛藍／墨綠／硃紅），一個新色相都不開。段的定義與段裡的順序在 `domain/todoFlow.js`，畫面不要自己再排一次；理由見 `docs/adr/0043-the-todo-centre-follows-the-flow.md`。

已經拿掉的種類（打電話、Abovee）不會再產生，但 Firestore 裡既有的未完成任務照樣列出來 —— 那是她真的還沒做的事。那一列標題旁邊一顆「舊資料」的小丸子，點進去第一句話講完整版，並多一顆「全部勾起來」。

**點進任務那幾頁之後，長相跟隨手記那一頁一樣**（2026-08-25）：

- 頂端一組**未完成／已完成**切換。勾掉的**不會消失** —— 她會勾錯，看得到才點得回來（同 `.scratch/todo-declutter/issues/08` 對隨手記的判斷）。「已完成」那一格照**完成那一天**分段（「今天」「8/24(日)」），一列一個垃圾桶，底下一顆「清掉這 N 筆」（軟刪除，還原得回來）。
- 勾完成之前先跳一個**置中的確認**，把要標掉的那幾筆列出來，並講明「它們會移到『已完成』，不會消失」。
- 每一列多一個「**N 項**」（那一筆來訪壓了幾個時段）—— 她的原話是「多一個幾項讓我知道今天要壓多少」。任務身上沒有時段數，那一頁多讀一次來訪（`getMany()`），跟「問這輪的時間」那一列同一個作法：頁面先畫出來，數字等資料回來再補。
- 一列底下多一行「**來訪日・課程**」（例：`9/14(日)・復能、營養針`），跟「N 項」同一趟讀回來、同一個時機補上去。**日期是來訪那一天，不是死線** —— 她認的是「哪一天那一場」，而死線是它的前一天，兩個差一天最容易看錯人。判斷只在 `domain/taskRules.js` 的 `taskLine()`，客戶詳情與試算表的 TODO 區讀的是同一支。**不要拿死線 + 1 反推來訪日**：取消類的任務不是那樣算的。
- 右邊那顆按鈕叫「**詳情**」不叫「來訪」，而且點下去是**讀取卡片**不是編輯器：那一天壓了哪幾項攤開來看，要改按鉛筆（`ui/components/card.js` 的規矩，日曆與客戶詳情早就這樣了）。

另外有兩種看法可以切換：**總覽**（照流程分段）與**依客戶**（一位客戶一列，看得出「這個人身上還有幾件事」）。兩種都是同一批資料，只是分組方式不同。**依客戶那一頁不分段** —— 那一頁問的是「這個人身上還有幾件事」，流程順序在那裡沒有意義。

#### 確認動線

這是壓表之後最容易掉東西的一段，所以做成一條線走完：

1. 壓表記完 → 該客戶出現在「跟客人確認時間」。
2. 她用 LINE 問客人，回來按那一列的打勾。
3. **跳出確認畫面**，把這個人所有的時段攤開：日期時間、做什麼、器材、治療師或診間、她自己寫的備註。
4. 客人說可以 → 確認，來訪從 `pending_confirm` 轉 `confirmed`，出現在日曆上，**並且這時候才長出該做的系統登記任務**（第 5.5 節）。
5. 客人說某幾筆不行 → **逐筆退回**，那幾筆取消（該產生的取消任務照樣產生），其餘照樣成立。
6. **問了但還沒回** → 在那一列直接寫一句（例：「禮拜一再問問」），存進 `visit.followupNote`。

**不做成整批一次確認。** 客人常常是「這兩天可以、那天不行」，整批只能全對或全錯，等於逼她重壓。

**第 6 條不是第三種結果，是「還沒有結果」。** 寫了那一句之後那一列**還在清單上** —— 事情還沒完，移走就等於忘記。變的是兩件事：那一列看得出「問過了，在等」，而且「已等 N 天」改成從**問的那天**重新算（不然她禮拜五才問過，禮拜六紅字照樣往上加，然後她就不看它了）。判斷全部在 `domain/confirmations.js`，畫面不要自己再算一次。

那一句**不是任務**（任務要有死線，而「再問問」沒有死線），也**不是備註**（備註跟著人一輩子，而這一句下禮拜一就過期）—— 三者的界線見 `CONTEXT.md`。

#### 隨手記

客人的零碎小要求（指定治療師、下次帶健保卡）沒有死線，也不是來訪產生的，所以它不是任務，是另一種東西 —— 見 `CONTEXT.md`。首頁底下給一塊，可以直接打一行字、直接勾掉，也可以掛在某位客戶身上。掛了之後在該客戶的詳情頁也看得到。

**可以掛一個日期，掛了就上日曆。**「今天」一次點擊，或是「挑日期」開系統原生的日曆。選填 —— 預設沒有日期，跟以前一模一樣。掛了的那幾筆會出現在日曆「待辦」那一類（第 8.6 節），勾掉了在日曆上畫成刪除線。**那個日期不是死線**（隨手記沒有死線），過了只有字變色，不進「逾期」那顆數字。見 `docs/adr/0044-a-dated-note-goes-on-the-calendar.md`。

`#/todo/notes` 頂端一組**未完成／已完成**切換。**未完成照「什麼時候記的」排、已完成照「什麼時候勾的」排**（`doneAt` 由新到舊，跟任務那幾頁同一條規矩）—— 兩組問的不是同一個問題：前者是「我剛記了什麼」，後者是「我剛做完什麼」。兩組都照建立時間的話，一件很久以前記、今天才處理掉的事會沉在今天記今天勾的那幾筆下面。

**右下角另外有一顆鉛筆泡泡，點下去就記得了。** 底下那一塊解決的是「看有哪些、勾掉」，泡泡解決的是「記進去」——「三秒內記完」這個標準要從她掏出手機開始算，不是從捲到底開始算。點下去鍵盤自己上來，送出之後**不關掉也不重畫**（她一次常常記三件事，而重畫會把鍵盤收起來再叫一次）。「掛給誰」選填，點開才讀客戶名單。

泡泡裡另外有一顆「**給營養品**」的捷徑（日曆的待辦編輯器也有）。兩層：先選客戶（**只列身上有還沒給完的營養品的那幾位**），再選哪一包；選完之後文字、掛給誰、`entitlementId` 三件事一起填好，她一個字都不用打。選了之後「掛給誰」那一排收起來 —— 客戶已經由那一包決定了。**已經約過的那幾包照樣列**，只是標出來（「9/3 已經約了」），選了它是改那一筆提醒的日期，不是長出第二筆。見 `docs/adr/0059`。

**每一列都吃長按**（首頁那張卡、`#/todo/notes`、客戶詳情、日曆那三個檢視 —— 五個入口同一份）。點一下維持原樣（勾掉／取消勾），長按跳出快捷選單：勾掉、改成今天、挑一天、掛給誰、拿掉日期、刪掉（只有勾掉的那幾筆才有，跟那顆垃圾桶同一條規矩）。營養品的提醒是另一組，而且它那一顆「勾掉」會先問「給了哪些」。見 `docs/adr/0060`。

#### 看今天做了什麼

這一頁最底下一塊，**展開才載入**（跟客戶詳情的「變更紀錄」同一種：她一天開這一頁十幾次，而看回顧是收工前一次的事）。

她要回答的是「**有沒有漏掉登記**」，所以它列的是**做過的**，不是還沒做的（那是這一頁上面那些段落的工作）。內容是**稽核紀錄的白話版，不是第二份紀錄** —— 每一次寫入本來就留著一則（第 6.2 節），這裡只多一層歸類。

**兩種分組可以切換，預設照人**（2026-09-01）。兩種是同一批稽核的兩種排法，切換**不重新讀資料**：

- **照人** —— 客戶姓名當大標題，底下條列今天對他做了什麼，每一列左邊是時間、中間是流程分段的名字。回答「這個人今天處理完了沒」。一個人身上的事**照她做的順序由早到晚**（壓表 → 確認 → 掛號），那是一條線。掛不到人的（設定、行事備註、休假）收成最後一組，抬頭寫「沒有掛客戶」—— 刻意不叫「其他」，那三個字已經是流程分段的最後一段。
- **照流程** —— 問到時間 → 壓表 → 跟客人確認 → 登記掛號 → 簽療程單 → **寫紀錄** → 取消與改期 → 日曆與待辦 → 客戶與額度 → 設定 → 其他。順序跟這一頁上面同一條。回答「哪一類整個漏了」。維持**新的在前**，那一格問的是「我剛剛做了什麼」。
  「寫紀錄」自成一段是 2026-09-04 加的：在那之前它落進「登記掛號」，而那個段落名在講一件它收不到的事。分段的判斷**由上到下比、第一個對上的算數**，所以要把一種從別段分出來時，改的是前面那一段的條件，不是把新的一段插到前面去。

空的段不畫，**最後一段永遠收得下剩下的**。頂端一排摘要數字（壓了 6 筆・簽了 3 張單・記了 4 件待辦）兩種看法共用，只印有值的、而且數的是**留下來的**（畫面上寫 6 筆，底下就要數得出六列）。連著一樣的句子收成一則寫「×8」，但**全天不去重**（早上勾三張、下午又勾兩張是兩件事）。

**這一頁只留她做的事，不是流水帳**（2026-09-04）。那一頁上本來混著兩種東西而且畫得一樣大：她按下去的（壓了誰的表、勾掉了哪幾張掛號），與系統跟著寫的（來訪一存檔額度的已排未上就 0→1、對帳跑過留一則、備忘錄改一個字留一則）。第二種每一則都是真的，它只是把第一種淹掉。判準是「**這一則是她按下去的，還是那一下的餘波？**」——

- ①到⑧ 全部印。
- ⑨ 客戶與額度只印五種：新增／刪掉客戶、新增／刪掉額度、給了哪幾款營養品（那是要進試算表的紀錄）、改永久限制或特殊狀況（那會改變往後每一次壓表）。次數的變化與「對帳過了」不印 —— 造成它的那筆來訪就在上面幾列。
- ⑩ 設定、⑪ 其他（備忘錄、壓表批次、拿回來一張待辦）不印。

**一則都不可以安靜地消失**，這條規矩**兩種分組都成立**：濾掉的不列出來，但底下一行寫著「另外 12 則沒列出來 —— 改欄位、對帳這種」，點「攤開」就地展開、比一般的列淡一級。`total` 永遠等於進去的筆數。判斷全部在 `domain/dayReview.js` 的段落表（`STAGES` 的 `keep`），**稽核紀錄一個字都不改** —— 那一頁的角色是「出事時往回找」，它應該是流水帳。見 `docs/adr/0071-today-shows-what-she-did-not-what-was-written.md`。

每一列講的是**誰、哪一天、哪一項**（第 6.2 節），句子由 `domain/audit.js` 翻譯，這一塊只決定要不要印名字 —— 照人那一格不印，抬頭已經寫了。**勾掉一張任務也要講得出那一場**（「勾掉 客戶A・9/14(日)・復能・Examine」）：任務身上沒有來訪日與課程名、也不該有，但它有 `visitId`，所以這一頁展開時多讀一趟來訪，把 `visitOf` 傳進 `describeParts()`。**問不到就不講**，那一列退回「勾掉 客戶A・Examine」。`#/settings/audit` 與客戶詳情的變更紀錄刻意不傳它。

**每一列不能展開** —— 要看某一筆改了哪個欄位去 `#/settings/audit`，那才是那一頁的工作。往前最多翻七天。最底下一行講試算表推到哪了（她點名的那句「忘記把某個紀錄同步到試算表」），**只在「今天」那一格出現**。見 `docs/adr/0062`。

### 8.2 壓表模式（核心畫面）

**兩種裝置都是主力。** iPad 看得更多，但手機必須完整可用 —— 她有一半時間拿的是手機。

> **一批是一個月的一串客戶，不是一個課程。** 她面對的是公司系統上的一張時段表，看到空的就搶，而且習慣把一個客人的所有課程壓完再換下一個人。這推翻了本節原本「先選課程」的設計，理由見 `docs/adr/0014-scheduling-is-per-customer-not-per-course.md`。

這一頁分兩層：**客戶卡片牆**（一次看到多位客戶，自己決定先處理誰）→ **記錄**（挑日子、把壓好的東西記下來）。

#### 客戶卡片牆

一張卡片就是一位客戶，而**這一層只回答一個問題：下一個處理誰。** 所以一張卡上只有三樣東西（ADR-0046）：

| 欄位 | 來源 |
|---|---|
| **姓名** | 客戶 |
| **醫療禁忌**：會真的擋掉器材的那幾個永久限制（實心紅），加上一句算出來的「只能 INDIBA」／「不能用 超磁場」／「3 台都不能用」 | `domain/contraindications.js` 的 `contraindicationTerms()` 與 `equipmentLimitLabel()` |
| **壓了沒**：還沒壓／已壓好／壓了 N 天／跳過 | batch |

其餘七塊 —— 不能的時間、各課程剩餘次數、這個月與上個月來幾次、距上次幾天、備註、喜好程度、最強的那一個排序理由 —— **一塊都沒有消失，全部在記錄面板上**。那幾塊是她**點進去之後**要一直對照的東西；在牆上只會把二十幾位客戶拉成三次捲動，而她在那一層做的只是「點下一個人」。

**不擋器材的永久限制不上牆。** 「固定禮拜五不行」也是永久限制，但一張卡上十個紅字等於全都不紅 —— 這一支讓醫療禁忌變得**更**明顯，不是相反（第 4.3 節仍然成立：記錄面板上全部都有，一個都沒摺疊）。

搜尋框、排序（`QUEUE_SORTS`）與三顆篩選丸不變。

頂端是**月份**（「9月壓表」，她講的是「九月的表」）、進度（「已壓 5 / 23 位」）與一條進度條，底下是**搜尋框**（打名字，二十幾位裡跳到某一位）。

排序仍然由 `domain/scheduling.js` 那一組計分算出來，但它現在只是**預設順序**，不是佇列。理由一律是人話標籤（`可用 6 天・最少`、`距上次 21 天`），她隨時可以跳著點。**排法可以整個換一種**（預設／問到時間的先／喜好／剩最多次／最久沒來），每一種都只是同一份名單的重排，不是第二套計分，而且同值時仍然照預設順序 —— 見 `docs/adr/0037-the-wall-can-be-re-sorted-but-never-re-scored.md`。

#### 記錄

進到一位客戶之後：

- 上面是那位客戶的**全部**背景：不能的時間（含「他原本是怎麼說的」那一摺）、各課程剩餘次數、這個月與上個月來幾次、距上次幾天、備註、喜好程度，以及「排在這裡的理由」。**捲動時一直看得到**。
  **這個月沒問過就寫「還沒問 8 月的時間」，不要拿別的月份那一份硬算**（ADR-0036）—— 講月份是必要的：少了它，「還沒問」與「問過但那是別的月份」在畫面上長得一樣。
  「壓復能的時候只要看到復能的剩餘次數就好」這句話仍然成立，用剩餘次數泡泡解決：一個課程一個數字，一列掃得完，不會變成十二種課程的大表。
- 中間是**小日曆**。點不下去的日子分三種顏色，因為她只會在「為什麼這天不能點」上停下來：**他說不行是紅色劃掉**、你自己休假是灰青加斜線（同日曆上的休假）、已經過去的只是變淡。已經記過的那天標起來，點一天選中它。已經過去的優先於他說不行 —— 月中打開時前面半個月本來就不能點，那幾天再標紅，紅色就從「這天他不行」變成「這半頁都是紅的」。

  **只擋半天的日子是第四種，而且它點得下去。** 客戶在表單上點的「只有上午不行」一路存成規則（`partOfDay`），`domain/availability.js` 的 `dayStatus()` 回得出 `blockedPart` —— 那天真的排得進去，只是要挑另外半天（`availableDates()` 也是這樣算的）。畫法跟客戶自己填的那一頁同一招：被擋的那半邊塗紅、寫上「上午」或「下午」並劃掉那兩個字，而**數字擠進還可以排的那半邊** —— 數字站的位置本身就講得出他還有空的是哪半天。點下去之後那一天的面板上會再講一次，落在那半天的時間丸子標成紅虛線。**標起來但不擋**（這個 app 沒有任何硬性阻擋，ADR-0074；原文永遠比解析結果大，第 4.3 節）。
- 點中一天之後，底下展開：課程、時段、器材、治療師或診間，**全部用點的**，備註才要打字。
- 已經記好的列在下面，可以逐筆刪掉。
- 一位客戶可以記很多筆，記完按「這位壓完了」，她就會出現在待辦的「跟客人確認時間」。

**這一頁不出建議時段**（`docs/adr/0002-app-records-decisions-it-does-not-make-them.md`）。它的工作是把判斷所需的資訊攤開、把結果記下來。

**選一個東西不重畫整頁。** 她記一位客戶要點五六下，一批二十幾位 —— 整頁重畫的代價是卡片閃一下（淡入動畫重播）、捲動位置回到最上面，一個晚上幾百次。只改按下去的樣子、只換真的變了的那一塊，規矩見 `docs/adr/0038-picking-an-option-does-not-repaint-the-page.md`。

#### 記錄是疊上來的一張卡，不是另一個畫面

點一位客戶不換頁，而是把記錄面板**疊在卡片牆上面、置中浮出來**，一位客戶一張，**左右滑就是上一位／下一位**。旁邊那幾張只畫姓名與那顆醫療禁忌丸，**跟卡片牆上看到的一樣**（ADR-0046）——
剩餘次數不畫，一張卡只有一半露在外面，四顆泡泡在那個寬度只會是四塊色。滑過去才展開成完整面板。抬頭上有上一位／下一位兩顆鈕，不靠手勢也換得了人。

三個斷點共用同一份：寬螢幕只是卡片更寬、旁邊多露一點，**不是另一套版型**。理由見 `docs/adr/0017-recording-happens-in-a-card-deck.md`。

**手機的返回鍵關掉卡片組，不是跳走整頁**（第 8.0 節的通則：畫面上多一層就多一步退得掉的路）。**整個卡片組只算一層** —— 左右滑換人、點丸子、換日子一筆紀錄都不推，不然按返回鍵會倒著走過她剛剛處理完的二十幾位，而且會撞上 Safari 的 `pushState` 頻率上限。見 `docs/adr/0052-the-deck-is-one-layer.md`。

**iPad 上多的是「同時看得到」，不是多出來的功能。** 手機做得到的事一件都不能少。兩種版型共用同一個 `batch`，任何時候換裝置都接得上。

卡片牆上的篩選只有三個：全部、還沒壓、沒問過時間。原本的「快到期」拿掉了 —— 它講的是會籍，而實務上沒有會籍這件事（ADR-0019）。

### 8.3 來訪編輯器

**兩個入口、一份實作。** 從客戶詳情進來是一整頁（`#/visits/:id`）；從日曆進來是掛在底部面板裡的同一支（`mountNew` / `mountEdit`），差別只有外框與存完去哪裡。表單、驗證、二次確認、狀態按鈕都是同一份 —— 一段來訪的驗證只能有一個實作。見 `docs/adr/0020-the-calendar-never-changes-pages.md`。

一個客戶、一天、多個連續時段，垂直堆疊：

```
客戶丁・9/3（三）        ⚠ 體內金屬
超磁場、高能量雷射已停用 — 體內金屬禁忌

10:30–11:30  物理賦能            剩 11/12
  器材  [INDIBA] ~~超磁場~~ ~~高能量雷射~~
  治療師 [騰崴]    診間 [治3]
   ── 15 分鐘 ──
11:45–12:45  ILIB                剩 17/20
  診間 [治2]
  ⚠ 已被客戶己佔用 — 治7、治9 有空

客人確認後產生：Abovee      死線 9/2
```

**選項一律用丸子，不是下拉選單。** 上面那張圖畫的就是丸子，而實作到 2026-08-24
之前用的是 `<select>` —— 那不是規格改了，是實作從一開始就沒照著做。
丸子比下拉好的三個實際理由：看得到有哪些選項、**被禁忌擋掉的器材不用點開就看得見**
（第 4.3 節要求醫療禁忌永遠可見不可摺疊，藏在下拉裡不算可見）、少一次點擊。
畫法在 `ui/components/form.js` 的 `chips()`。

**時間是一行**（`10:30 – 11:30`）：結束時間本來就是依課程時長算出來的，
拆成兩個上下疊的欄位等於用兩行講一件事。

日期底下有一欄**「這一次記一句」**（第 5.3 節的 `note`）。它是**這一天**的，不是某一個時段的 —— 她記的通常是「這次來訪」的事。與客戶身上的**備註**是兩回事：備註跟著人一直在，這一句跟著這一筆來訪。長度上限在 `domain/visits.js` 的 `NOTE_MAX`，壓表那一頁寫的是同一個欄位、用同一個上限。

### 8.4 時段反查（使用者主動提出的額外功能）

**主力裝置：手機**（臨時空出一格時通常人不在桌前）。

輸入「日期 + 時間 + 課程」（結束時間依課程時長推算），系統由上到下列出候選客戶：

- 該時段沒有被客戶的本輪可用性擋掉
- 該課程還有剩餘次數
- **依壓表同一套優先序公式排序**（第 9 節），不另做一套 —— 同一位客戶在兩個畫面排名不同，她會不知道哪個算數
- 顯示推薦理由，以及「這一格他為什麼合適」（他說這天方便／那天他本來就要來／還沒問這輪的時間）
- 每一位附一則可以直接貼到 LINE 的邀約訊息

**沒被列進來的人與理由也要顯示**，收在「沒列進來的」底下。排除只有四種依據，全部不是推測：沒有剩餘次數、醫療禁忌把擇一池的器材全鎖死、那個時間他已經有另一筆來訪、他自己說那天不行。**「還沒問這輪的時間」不會被排除**，只標成「不知道他那天行不行」。見 `docs/adr/0009-backfill-excludes-but-explains.md`。

用途：Abovee 上臨時空出一格、或有人取消釋出時段時，快速找到誰可以補。

### 8.5 客戶總覽 / 客戶詳情

**總覽主力：iPad**（欄位多）；**詳情主力：手機**（臨時被問「我還剩幾次」）。iPad 橫式為左總覽、右詳情。

- **總覽**：可搜尋清單，每列顯示姓名／備註／各池進度／限制標籤，名字底下一行是**買了什麼**（`0723 顧客會 新8萬方案x2+EECPx40+SIS(60)x5`，算出來的，見 `docs/adr/0090`）。上次來訪與下次預約 2026-09-13 從卡片上拿掉了（她：「不用寫上次，下次麼時候來」），「最近來過／最久沒來」照樣排得動，只是不印。

  右下角的懸浮鈕點開兩條路：**新增一位**與**快速建立一群**（`#/customers/bulk`）。後者是給「同一場顧客會來了八位」用的：購買通路、購買日、方案、數量**填一次**，底下只打名字（貼一串進去會自己拆開），需要的那幾位才點「微調」改數量、不套方案、或加購單項。微調裡的加購是**跟客戶詳情同一張表**（`ui/components/buy.js`），所以健檢選得到金額等級、營養點滴選得到品項、營養品也在那一排丸子上；進階設定那七個欄位仍然不在這裡。**沒微調過的那幾列什麼都不多寫** —— 「跟大家一樣」是預設，而預設不需要標籤。一位一個 commit，中間失敗就停下來講清楚已經建了幾位、誰失敗，已經建好的不退回去（第 6.1 節）。

  上面那一排丸子是**排序**不是篩選 —— 她心裡的問題是「先看誰」，不是「把誰藏起來」：最近來過／最久沒來／新加入的／喜好程度／姓名。後面隔一條線之後是**課程丸**（由實際還有剩餘次數的額度算出來），點了才會收窄名單並改照那個課程還剩幾次排，再點一次取消。原本的「剩餘快用完」「會籍快到期」兩個篩選拿掉了（ADR-0019）。已停用的客戶走頁首那顆小標籤切換。

- **詳情**：**順序照她問問題的順序排**，不是一疊白卡（`docs/adr/0018-detail-pages-are-one-page-not-a-stack-of-cards.md`）：

  1. 姓名、喜好程度、永久限制，名字底下是跟總覽同一行的**買了什麼**（電話與 LINE 2026-09-13 從抬頭拿掉了，欄位留著，搜尋還靠它們）（新增與編輯表單裡，**會擋掉器材的那幾個永久限制是可以點的丸子**，選項由器材主檔上的醫療禁忌推出來；其餘的走自由輸入。那種字打錯一個就完全不會擋，而不會擋的醫療禁忌比沒有更危險）
  2. **備註** —— 彩色小丸，點「編輯」從底部滑出編輯器
  3. **這個月** —— **選中的那個月**（左右箭頭換月），一天一組、一段一列，
     跟「看這個月的進度」共用同一份畫法。以前是「本月與之後」的橫捲日期卡，
     而標題只寫「這個月」—— 兩邊從一開始就對不起來（2026-08-24 定案：
     標題說哪個月就只有哪個月）。點一筆先浮出讀取卡片，鉛筆才進編輯器（ADR-0020）
  4. **不能的時間** —— 本輪可用性。畫法跟壓表卡片牆共用
     （`ui/components/ban.js`）：條件丸子在上、原文收在「他原本是怎麼說的」
     那一摺裡（ADR-0035）。「記一次」是一個日曆，不是解析器的表單
     （ADR-0051）—— 原文從輸入變成產出，但它還在，而且保證跟規則一致。

     **一份就是一個月，而且抬頭要印出那個月**（ADR-0053）：紅框框寫的是
     「9月不能的時間」。「記一次」點下去先出一張面板問是哪個月 ——
     「改已經填過的」列已經有的月份，「新增」只列**還沒有的**那幾個，
     所以同一個月不會有第二份。既有的那一份不給換月份（換了會讓壓那個月的表
     突然找不到它）。改完存檔前用二次確認把差異列出來
     （拿掉哪一天、加上哪一天）。

     「以前問過的」是**一行一份**：月份 + 一句摘要，點開才是那一份的紅框框。
  5. **額度** —— 三段式次數明細，現算，**橫著捲**（一位客戶十二種課程直著疊
     會佔掉半頁）。順序是「還有剩的在前、剩得少的更前」，因為橫著捲只有最前面
     兩三張會被看到。計數對不起來時抬頭掛一顆徽章 —— 那句話不能被捲走。
     健檢那一張卡片底下多一句「健檢做完 N 次，二返還欠 M 次」；
     還沒有二返額度的（2026-08 以前建的客戶）在那裡補得起來
  5b. **營養品** —— 額度底下自己一小段，一筆都沒有就整段不畫（ADR-0057）。
     那一列寫「給了沒」與「哪天給」（`還沒給・9/3 給` / `都給了・9/1`）。
     **點一列開的是快捷選單，不是「調整」那一張表**：她點它十次有九次要問的是
     「這一包給了沒、什麼時候給」，不是「改幾個月」。三顆 ——
     **約時間**（選一天，日曆的那一天會多一件待辦；那一件就是提醒本人，
     ADR-0044，所以沒有第二份資料要同步）、**已經給了**（選一天預設今天，
     逐款確認，交付寫進額度的 `deliveries[]` 並同步勾掉關聯的提醒，
     兩件事在同一個 commit 裡，ADR-0059）、**編輯這一包**。
     長按開的是同一張。都給完了就沒有「已經給了」那一顆
  6. **來訪紀錄** —— **預設收起來**，展開才是最近幾筆，超過收進「看全部」的面板。
     這一段跟上面的「這個月」看的是同一批來訪，只是換個角度 ——
     她要看歷史的時候是在對帳，不是每次打開這一頁都要看。
     **不從這一頁新增來訪**（路由留著，入口拿掉）
  7. **隨手記**
  8. **任務** —— 一列自己就講得完：`種類・來訪日・課程`（例：`Examine・9/1(一)・二返`），
     右邊一顆死線丸子，再右邊「詳情 ›」浮出唯讀卡片。**日期是來訪那一天，不是死線**
     （判斷只在 `domain/taskRules.js` 的 `taskLine()`，待辦中心與試算表的 TODO 區
     讀的是同一支）。來訪找不到（獨立待辦、來訪被刪了）就只印種類 ——
     右邊那顆丸子已經在講死線，再印一次只會看起來像來訪日。

  最底下一排小標籤：**LINE 訊息**（問這輪的時間／問壓好的時間可不可以／來訪前提醒，只列現在用得到的）、**變更紀錄**、**停用與刪除**。這三個一年點兩次，點了從底部滑出來 —— 給它們一整張卡等於天天提醒她那件事存在。

  會籍到期日不再出現在這一頁，也不再出現在新增／編輯表單裡（ADR-0019）。

- **進度追蹤**（`#/customers/progress`）：入口是客戶頁首那一排的「看這個月的進度」，和「看已停用的」並排 —— 底下五個分頁已經滿了，不加第六個。

  一位客戶一張卡，卡裡一天一組，一組裡一段一列：時間、課程、**那一段自己的狀態**（`○` 待確認、`△` 已確認、`✓` 已完成、`✗` 未到）。同一天的兩段狀態可以不一樣 —— 客人做了一半就走是會發生的（ADR-0025）。每一天的右邊小小地記著這個狀態是什麼時候變的（`visit.statusAt`）。

  **這一頁是唯讀的。** 點一筆浮出讀取模式的卡片，**連鉛筆都沒有** —— 要改東西她會自己去日曆或客戶詳情，而那是一個明確的決定，不是在對帳的時候手滑。取消掉的時段不畫，跟日曆一樣（時段已經還回去了）。

  這個月沒排的人收在底下一排名字裡，不佔畫面 —— 但也不能不列，「誰這個月還沒排」正是她要掌握的一半。

  狀態的判斷走 `domain/visits.js` 的 `slotStatus()`，顏色與符號走同一支的 `STATUS_VIEW`，資料整理在 `domain/progress.js`。這一頁一個規則都不自己寫 —— 使用者的要求是「這一頁要和 todo 日曆對的上」。

### 8.6 日曆

**主力裝置：兩者**。手機用日／週檢視；iPad 橫式才開放月檢視的完整密度。

日曆是**可以直接編輯的第一線畫面**，不是唯讀的檢視。上面有**四類，來自三個集合**，見 `docs/adr/0015-calendar-is-a-first-class-surface.md` 與 `docs/adr/0045-the-calendar-has-four-kinds.md`：

| 類 | 資料在哪 | 可以跨天 |
|---|---|---|
| **客戶來訪** | `visits`。綁客戶，產生任務，扣次數，走第 4.1 節的狀態機 | 不行 |
| **待辦** | `notes` 裡**有日期的**那幾筆 —— 就是隨手記本人，不是另一份資料（`docs/adr/0044`），所以「同步回隨手記」沒有東西要做 | 不行 |
| **行事備註** | `events`（`category: 'personal'`）。公出、演講。不綁客戶、不產生任務、不扣次數 | 可以 |
| **休假** | `events`（`category: 'leave'`）。那幾天她根本不在 | 可以 |

「行事備註」2026-08-23 從「個人行程」改名而來，**資料上的 `category` id 沒有跟著改**。

頂端一排可勾選的篩選，一類一顆。

**這一頁要一眼分辨的是七種東西**：來訪的四種狀態（待確認琥珀、已確認靛藍、已完成墨綠、未到硃紅）加上行事備註（紫）、休假（灰青**加斜線紋**）與待辦（**中性墨色加方框勾勾記號**，勾掉了畫刪除線）。休假與待辦刻意都不搶色相 —— 色相輪上塞六個彼此分得開的顏色，最後兩個一定會擠在一起。休假講的本來就不是「這裡有一件事」是「這幾天我不在」；待辦則是「一件可以勾掉的事」，而顏色沒有「完成」這個狀態，記號有。色值與理由見 `docs/adr/0039` 與 `0045`；**來訪那四個顏色是全站共用的**，客戶詳情、進度追蹤、試算表讀的是同一份。**行事備註與休假可以自己挑顏色**（六色，跟客戶備註同一組名單），但休假身上的斜線不跟著顏色走，見 `docs/adr/0040`；**待辦不能挑顏色** —— 待辦之間沒有只有她知道的差別。右下角懸浮鈕點開分成「新增來訪」「新增待辦」與「新增行事備註」三條路。從日曆新增來訪走的是同一支編輯器與同一套驗證。

**待辦那一類點一筆時，「勾掉」那一顆就長在讀取卡片上** —— 這是唯一一個不必先按鉛筆的動作（ADR-0045）。改文字與改日期還是要按鉛筆。編輯器底下的「從日曆拿掉」只是清掉日期，那一筆會留在隨手記裡，不是刪除。

**按下去之後那一張卡片自己收掉，底下那一天的抽屜當場跟著變**（那一列變成劃掉的樣子，不用重新整理）。**唯一的例外是資料庫沒有真的勾掉的那一次** —— 營養品的提醒只給了一部分時，那一筆刻意留成沒勾掉、只換掉文字，這時候卡片留在原地照實說（第 6.9 節）。判準是寫入那一層回報的結果，不是按鈕上寫了什麼。見 `docs/adr/0073`。

**待辦那一類新增時掛得了客戶。** 欄位跟隨手記其他四個入口一模一樣（`ui/components/note.js`）—— 日曆上那一類**就是隨手記本人**（ADR-0044），長得不一樣只會讓她以為是兩種東西。新增時另外多一顆「**給營養品**」的捷徑：選客戶、選哪一包，文字與 `entitlementId` 自動填好（ADR-0059）。**不要讓她自己打那一行字** —— 手打的那一筆沒有 `entitlementId`，勾掉時就不會問「給了哪些」，那筆要進試算表的交付紀錄會靜靜地沒了。

**已取消的來訪畫得出來，但暗掉。** 三個檢視一致：整列降透明度加刪除線，右邊掛「已取消」的徽章。它**不算進**頂端那一行與週檢視格子上的「N 筆」（那一行回答的是「那段時間有幾件事要做」），也**不參加撞期判斷**（那個時段已經還回去了，標成撞期是假警報）。看得見是必要的 —— 「那一格本來有人、後來取消了」正是她會想再排一個人進去的訊號。見 `docs/adr/0061`。

**每一列都吃長按。** 點一下維持原樣（浮出讀取卡片），長按跳出**快捷選單**：來訪是「客戶說可以／客人來了，去簽療程單／改這一筆／取消這一筆」，待辦是「勾掉／改日期／掛給誰／…」，行事備註是「改這一筆／刪掉」。**「已完成」與「未到」不在選單上** —— 那兩個是逐段的結果（ADR-0025），整筆一起標會把「客人做了兩段就走」記錯，而次數就是跟著它扣的，所以那一顆通到待辦中心的「簽療程單」。取消照樣走二次確認。長按是捷徑不是唯一的路，見 `docs/adr/0060`。

**任務（`tasks`）不上日曆。** 它們的死線是來訪日的前一天，而那筆來訪就畫在那裡了 —— 同一天畫兩條是同一件事佔兩格。

- **月**：格子裡是**橫跨日期的色條**，不是圓點 —— 跨天的行事備註要看得出從哪天到哪天。放不下的用「+N」表示。前後補滿鄰月的日子：留白會讓她以為那幾天沒事。

  **點一天，月曆整片留在原地，那一天的內容從底部滑出來**（像一般日曆 app 那樣），面板裡可以直接點進某一筆，也可以直接替那一天新增來訪或行事備註。往下滑掉就回到剛剛那個月。整頁切到日檢視要靠頂端的日／週／月，不是靠點某一天 —— 理由見 `docs/adr/0018-detail-pages-are-one-page-not-a-stack-of-cards.md`。

  **可以點的是一整格，不是那顆日期數字；色條不吃點擊。** 同一格裡有兩種點擊結果本身就是問題 —— 手指落在名字上跳去別的畫面，落在旁邊的空白什麼都不會發生，而那兩處相距不到十個像素。那一格從此只做一件事：打開那一天。

  月份切換、今天、日週月、篩選一律收成兩列。三列控制項會把格子推出螢幕，而她開這一頁就是為了看格子。

  **標題只寫月份與第幾週**（2026-09-13）：月 `9月`、週 `9月 W2`、日 `9/18(五)`，不是今年才加年份（`2027年1月`）。以前寫「2026 年 9 月」與「9/14(一) – 9/20(日)」，手機上被截成「…」而看不出是哪一月。跨月的那一週**四天以上落在哪個月就算哪個月**（也就是週四那一天的月份），規則在 `domain/calendar.js` 的 `weekOfMonth()`。**點標題浮出一張面板跳日期**，單位跟著檢視走：月檢視選年月、週檢視點小月曆的一整列、日檢視點一天（`.scratch/asks-2026-09-13/issues/10`）。
- **週**：七天各自的摘要，手機是七段直的清單，iPad 橫式變七欄。一週是她真正在規劃的單位。每一天的抬頭寫 `9/7`。七欄時一列改成直排（時間一行在上、名字一行、課程一行，超過省略）—— 左邊那一欄時間是給手機一整排寬度設計的，放進九十幾像素的欄裡名字會一字一行。**不做橫向滑**：週檢視本身左右滑是換一週（ADR-0020）。
- **日**：一整天照時間排開，左邊時間、中間內容、右邊狀態（已確認／等回覆）。同診間床位或同治療師撞在一起就標出來 —— 只提示，不阻擋（第 4.7 節）。整天的行事備註釘在最上面。

**這一頁只有她自己排的來訪。** 同事在 Abovee 上壓的看不到（第 4.7 節），所以空的格子不等於那個時段真的空著，畫面上要講明，否則她會拿它當可用時段表用。

#### 這一頁從頭到尾不換頁

理由與細節見 `docs/adr/0020-the-calendar-never-changes-pages.md`。四件事：

- **左右滑換上一個月／下一個月**（週檢視換週、日檢視換天）。頂端的箭頭走同一條路，不會一個閃「載入中」一個不閃。
- **點一筆先浮出讀取模式的資訊卡片**，右上角的鉛筆才進編輯器。她點一筆的十次有九次只是要確認「那天幾點、誰、做什麼」，直接落進表單等於每次都冒著改到東西的風險。
- **卡片疊在那一天的面板上面**，不是取代它 —— 抽屜講的是那一天，卡片講的是那一筆，看完關掉還在同一天。
- **新增與編輯就長在面板裡**，走的是第 8.3 節那支編輯器本人。選完客戶之後編輯器換進同一張面板，面板本身沒有消失過。

底部面板**整張都拖得動**（不是只有上面那條橫桿）：往下拖收回去、往上拖到頂、從頂往下拖回原本高度。名單自己捲，捲到頭才把手勢交回給面板。編輯器掛進去時直接就是滿版。見第 8.0 節與 `docs/adr/0021-the-sheet-is-dragged-by-transform.md`。

### 8.7 資料健檢

**主力裝置：iPad**（差異比對是表格）。手機版可看摘要與逐項修正。

第 6.6 節的對帳結果，一頁呈現。符合那三個條件的檢查項給「一鍵修正」（目前是次數對帳、缺配對的二返額度、備註寫著舊的說法），其餘只顯示差異並跳到那一筆（`docs/adr/0007-health-check-reads-only.md`）。沒有 finding 的檢查項收起來 —— 一頁十項全部攤開，她要找的那一項就被稀釋了。

**沒有背景掃描，也沒有徽章**：要看就自己來這一頁（第 6.6 節、`docs/adr/0049-the-todo-centre-does-not-run-the-health-scan.md`）。

### 8.8 設定

**主力裝置：iPad**（表單多、要打字）。手機版仍需完整可編輯。

診間、治療師、器材、營養點滴品項、療程項目、方案範本、任務規則、排序權重、**LINE 回覆模板**、資料健檢、稽核紀錄、試算表報表、匯出備份、已刪除項目還原。

**LINE 回覆模板**（`#/settings/templates`）：全站六則要貼給客戶的話 —— 問這一輪的時間（兩則：有連結／沒連結）、收到他填的時間回一句、問壓好的時間可不可以、來訪前提醒、臨時空出一格。她的原話是「我需要一個地方可以一次修改所有回復的模板」。

一則一個框，底下一排點得到的變數丸子（`{name}`、`{month}`…，點一下插在**游標處**）。逐則有「回復預設」，整份有「全部回復」（走二次確認）。**只存她改過的那幾則**，所以之後改預設值時她沒動過的會跟著更新。

**字在設定裡，判斷留在程式裡**：「一次來訪只講第一段的開始時間」「前一天就說明天」「沒有課程名就整段消失」那幾個條件不搬進模板 —— 模板只有 `{}` 一種語法，寫不出條件。預設值與驗證在 `domain/messageTemplates.js`，算變數在 `domain/messages.js`。

**外觀**：跟著系統／淺色／深色。記在這一台裝置的 `localStorage`，不進 Firestore —— 它是「這一台現在想要什麼」，不是資料，兩台本來就該各自設定。深色那一整組值在 `css/tokens.css`，只有一份，靠 `<html data-theme>` 挑，見 `docs/adr/0055-she-can-switch-to-dark-herself.md`。

**使用者明確要求診間與治療師清單要能自行新增修改**，不可寫死在程式碼裡。這一頁全面套用二次確認。

### 8.9 客戶自己填時間的表單

**這一頁的使用者不是她，是她的客戶，而客戶全是長輩。** 網址是
`/form.html?t=<token>`，一位客戶一個月一條，她從待辦中心或客戶詳情複製一則
含連結的 LINE 訊息貼給客戶。完整的設計與取捨在
`.scratch/customer-availability-form/spec.md`。

**它是獨立入口，不是 app 的一條路由**（`docs/adr/0031-...`）：客戶不該為了填幾題
下載一整個排班系統，app 的登入閘門也不該為了它開一個 if。

#### 一頁一件事，不要往下捲

兩頁，見 `docs/adr/0034-the-customer-page-does-one-thing-per-screen.md`：

| | 這一頁只有 | 按鈕 |
|---|---|---|
| 1 | 一個月的日曆 | 填好了 |
| 2 | 「您告訴我的是」＋「還有什麼要跟我說的嗎」 | 確定送出／回去改 |

按鈕**不寫「下一頁」**——長輩看到會先想「還有幾頁」。往下捲之所以比換頁糟，
是因為捲到一半的畫面同時露出兩題的一半，看起來永遠像沒做完。

**只問「哪幾天不方便」。** 沒有「固定哪個星期不行」那一題（它和日曆問同一件事，
兩條路通到同一個地方本身就是雜亂），也沒有「這個月都可以」那顆按鈕
（什麼都不點直接按「填好了」就是那個意思，抬頭一句話講清楚就夠）。

**不問「哪幾天方便」**（不產生 `prefer`）：客戶勾「二四方便」的意思是「這兩天最好」，
不是「其他天不行」，而兩邊都很容易讀成後者。同一份資料兩個人讀成不同意思，
比少問一題糟。她自己記錄時仍然可以手動加 `prefer`。

#### 點一個日期，底下浮出三選一

點日期 → 底下浮出「整天不行／只有上午不行／只有下午不行」→ 選完收起來。
已經標過的那一天再點一次，那張卡最底下是「這天其實可以」。

從底下浮出來而不是畫面正中央：拇指構得到，而且不會蓋住日曆上半部 ——
他要看得到自己剛剛點的是哪一格。

**不做三態循環**（點第二下變上午、第三下變下午）：誤觸看不出來，而且點錯了
要再點三下才回得到原點。**不做拖拉**：拖拉要關掉那一區的原生捲動，長輩想捲頁時
畫面不動會以為當掉了 ——「無拖拉」在第 8.0 節本來就允許手勢當快捷，這裡是連快捷都不做。

顏色**不能只靠顏色**：紅底一律配上「✕」或「上午／下午」兩個字。
這一頁的顏色寫在 `public/css/form.css`，**不套 `.status-*` 那一組** ——
那一組是來訪狀態的顏色（`domain/visits.js` 的 `STATUS_VIEW`），套過來的話改一個
來訪顏色會同時改到客戶看的東西。

#### 收合：規則、原文、複述共用同一份

`domain/availabilityForm.js` 的 `groupPicks()`，三層由大到小：

```
整個月都點掉      → 9/1 到 9/30 不行
整個禮拜五都點掉  → 每個禮拜五不行
連續兩天以上      → 9/1(二) ~ 9/3(四) 整天不行
```

**整個星期的排在最前面，其餘照日期由早到晚**，連續的和單獨一天混在同一串裡：

```
整個禮拜五不行
9/1(二) 整天不行
9/7(一) ~ 9/8(二) 整天不行
9/10(四) 整天不行
```

把範圍全部提到前面會讓人讀成「9/7~9/8、9/1、9/10」，而客戶自己講的順序、
她排班時看的順序，都是日曆的順序。

**規則、原文、給人看的複述三邊共用它**，所以客戶按下送出時看到的字，
就是她收到的字。星期那一層的條件刻意嚴格：那個月裡的每一個禮拜五都要被點到、
半天別一致，少一個就不收 —— 少的那一天他是真的可以，多擋一天在排班上是
看不出來的錯。

#### 送出之後

**暫時不重填。** 同一條連結再打開只會看到「收到了」與他填的內容。客戶填錯的出路是
**她重發一條新連結**（舊的作廢）。這個決定同時讓 Rules 變得極簡單：文件 id 就是 token，
第二次送出是 `update` 而客戶沒有 update 權限 —— 不重填是 Rules 保證的，不是前端擋的。

答案落在 `formResponses`，**不會自動生效**：待辦中心「客戶填好的時間」那一列，
她按「收下」才寫成本輪可用性，並附一則把系統讀到的東西複述回去的回覆訊息
（`docs/adr/0032-...`）。

#### 表單上不會出現的東西

剩餘次數、永久限制、來訪、任何健康資訊。**連結會被轉傳**，所以它只寫得進去、
讀不到任何既有資料 —— 邀請上只有名字與月份。

`<title>` 與 OG 標籤要講得出這一頁是什麼（LINE 貼上連結會抓它們畫預覽卡）。
沒有 OG 標籤時 LINE 會自己從內文撈一段，第一版的預覽卡上寫著「載入中…」，
看起來像壞掉的網頁 —— 而客戶要不要點下去就是看那張卡。

也不讓客戶自己選時段：這個 app 從頭到尾不是預約系統（第 4.7 節），
表單收集的是**限制**，不是預約。

### 8.10 備忘錄 / SOP

她自己整理的流程與心得。**綁課程不綁人、不上日曆、勾不掉** ——
它不是待辦，是隨時翻得到的參考。決定與理由見
`docs/adr/0067-a-playbook-is-read-not-ticked.md`。

**入口在待辦那一頁的右上角**（`#/playbook`），跟客戶頁的「看這個月的進度」
同一個位置與畫法。導覽列不動。

**它是一疊左右滑的卡牌**（ADR-0069）。一頁到底，沒有清單頁與閱讀頁之分：

```
備忘錄 / SOP                          🔍     ← 放大鏡按下去才長出搜尋列
● 營養點滴   外檢   復健科流程                ← 標題丸子，點一顆滑到那一張
╔═══════════════════╗ ╔═══════
║▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔║ ║▔▔▔        ← 上緣一道茶色，像便條紙的黏邊
║ 營養點滴        ✎ ║ ║ 外檢          ← 鉛筆就地改，不換頁
║ ───────────────  ║ ║ ────
║ 飯後打針          ║ ║ 要帶⋯
╚═══════════════════╝ ╚═══════
          · ● · ·                              ← 第幾張／共幾張
                              ( ＋ )           ← 右下角泡泡加一份
```

**畫面上沒有一句解說文字**（只有一份都沒有的時候留一句）。左右滑走原生
`scroll-snap`，停在第幾張用 `IntersectionObserver` 認 —— scroll 事件在 iOS 上
捲動時會被節流，圓點跟不上手指的話那一排看起來就是壞的。

內文比一般頁面大一級、行距 1.85、`pre-wrap`（她自己打的換行與縮排原樣留著）。
一張卡放不下就在卡片裡面捲，不把整疊撐高。

**這一頁沒有勾選框、沒有進度、沒有百分比。** 一放勾選框它就變成第二個
待辦中心，而她指名「不需要變成打勾任務」。

`#/playbook/:id` **不是另一頁**，是「這一疊，開在那一張」。

**掛了課程的那一份會自己浮出來**（`ui/components/playbookHint.js`，兩個入口
共用）：日曆上點開那一筆來訪、待辦的「跟客人確認時間」。浮的是**整份的前四行**
加一句「還有 N 行」與一個「看整份」——
那就是「飯後打針該何時提醒」的答案：**不另外開一種要勾的任務**。
洗版由行數擋不由時機擋，因為她看得到自己寫的第幾行會出現在卡片上。

沒掛課程的那一份永遠不會自己浮出來（外檢那種系統裡沒有的項目），
但它照樣查得到。

---

## 9. 排序演算法

待排佇列的分數，**權重做成設定頁可調**：

```
score = w1 × (1 − 可用天數 / 當月天數)        // 限制越多越優先
      + w2 × (喜好程度 / 最大喜好值)
      + w3 × (剩餘次數 / 距方案到期天數)      // 快到期又沒上完
      + w4 × (距上次上課天數 / 30)
```

初始建議 `w1=1.0, w2=0.8, w3=0.6, w4=0.3`。一開始沒人知道對的值，讓使用者用兩週後自己微調。

**每一項的貢獻都要能在卡片上顯示成人話標籤。**

分數排出來的是**預設順序**。卡片牆上可以整個換一種排法（喜好、剩最多次、最久沒來、
問到時間的先），但那幾種都只是拿 row 上已經有的欄位重排，**不是第二套計分** ——
同一位客戶在壓表與時段反查永遠是同一個分數。見
`docs/adr/0037-the-wall-can-be-re-sorted-but-never-re-scored.md`。

**每一項先夾在 0–1 之間再乘權重。** 上面的式子沒有夾，但不夾的話「距上次 180 天」那一項會貢獻 6 倍，其他三項等於不存在，權重也就失去意義了。實作在 `domain/scheduling.js`。

幾個邊界的處理：

- **還沒問這輪時間的人**：第一項貢獻 0（不知道他的限制，不能假裝他限制很多），但卡片上標「還沒問 N 月的時間」讓她看得見。「還沒問」是**照壓的那個月**判斷的，不是照今天 —— 別的月份那一份與這個月沒有交集時會算出「可用 0 天」，而 0 天在這個公式裡是滿分（ADR-0036）。
- **從來沒上過課的人**：第四項給滿分。買了方案卻一次都沒上，會籍照樣在跑。
- **方案已經過期還有剩餘次數**：第三項給滿分。

---

## 10. 技術棧

| 層 | 選擇 | 說明 |
|---|---|---|
| 前端 | **原生 HTML / CSS / JS**，不用 React | 維護者只熟 HTML/CSS/JS，明確表示看不懂 React。不要引入 build pipeline。需要一點結構可用 Alpine.js。 |
| 形態 | **PWA，觸控優先，響應式** | Android 手機與 iPad 各約一半。以容器寬度切三段版型（<600 / 600–899 / ≥900），不是偵測裝置型號 —— iPad Split View 會讓寬度改變。要有離線快取，訊號差時仍打得開（讀取用快取，寫入排隊重試）。 |
| 後端 | **Firebase**（Firestore + Auth + Security Rules + Hosting） | 使用者指定。Firestore 免費額度對此規模綽綽有餘。 |
| 排程／背景工作 | 盡量放在 client（app 開啟時執行對帳） | Cloud Functions 需要 Blaze 方案（此規模費用趨近於零，但要綁信用卡）。**先不用 Functions**，等真的需要定時觸發再說。 |
| 部署 | **Firebase Hosting + GitHub Actions** | `firebase init hosting:github` 建好 workflow，push 就自動部署。維護者多用手機開發，不能依賴本機跑 `firebase deploy`。私有 repo 每月 2000 分鐘免費 Actions 額度，此專案用不到零頭。 |
| 試算表同步 | Apps Script 定時讀 Firestore → 寫回既有格式 | **單向輸出，試算表設為唯讀** |
| 行事曆 | app 內建；ICS 訂閱為選配 | TimeTree 無穩定寫入管道，且 app 已滿足「工作／私人分離」需求 |

### iPad / iOS Safari 的坑

- **PWA 安裝**：Android Chrome 有安裝提示，iOS 必須手動「加入主畫面」。要在設定頁放一段圖文教學。
- **推播通知**：iOS 只有「已加入主畫面」的 PWA 才支援，且需 iOS 16.4+。**不要把任何流程設計成依賴推播** —— 提醒必須在 app 內看得到。
- **本機儲存可能被清掉**：iOS 對閒置網站的儲存有回收機制。這正是為什麼 Firestore 是唯一真相來源，本機只是快取 —— 清掉了重新載入即可，不會掉資料。
- **input 字級 < 16px 會觸發自動放大**，畫面會亂跳。全站 input 至少 16px。
- **`100vh` 在 iOS Safari 不準**（工具列會伸縮），用 `100dvh` 或 flex 佈局。
- **沒有 hover**：iPad 有時接鍵盤滑鼠，但不能假設有。所有資訊不可藏在 hover 後面。

### 程式碼組織（為了好維護）

使用者明確要求「做得完整並且好維護」。請維持清楚分層：

```
/data     Firestore 存取層 —— 只有這裡能碰 firestore SDK
/domain   業務規則（禁忌、衝突、額度、排序、任務產生）—— 純函式，不碰 IO
/ui       畫面 —— 只呼叫 /data 和 /domain，不直接寫 query
```

**規則絕不寫在 UI 事件處理器裡。** `/domain` 是純函式，方便單獨測試——這是這個專案唯一真的需要測試的地方（算錯次數、漏擋禁忌都是實際傷害）。

### 資安

資料含客戶健康資訊（例：癌症化療中）。

#### 先建立正確的心智模型

**Repo 私有 ≠ 網站私有。** 不管 repo 設不設 private，部署出去的網站都是一個公開網址，任何人拿到網址都打得開，所有 HTML / CSS / JS 都可被下載檢視。

所以**安全防線完全不在 repo 隱私，而在 Firebase Auth + Firestore Security Rules**。判準只有一條：

> 沒登入的人，查不到任何一筆資料。

前端程式碼被看光不會造成資料外洩；Rules 寫錯才會。

#### 可以 commit 的

- **Firebase 前端 config**（`apiKey`、`authDomain`、`projectId` 那串）。這**不是密鑰**，Firebase 設計上就是公開的，會出現在任何人的瀏覽器裡。不用 gitignore，也不用塞環境變數。
- Firestore Rules、索引定義、Hosting 設定
- 種子資料（診間、療程、方案範本這類非個資的主檔）

#### 絕對不能 commit 的

- **Service account 金鑰**（Admin SDK 的 JSON）—— 這才是萬能鑰匙，繞過所有 Rules
- 任何含**真實客戶資料**的匯出檔、備份 JSON、截圖
- **真實客戶姓名**，包含寫在註解、文件、測試裡當例子的。例子一律寫「客戶A」；規則跟名字的字數或結構有關時用假名（王小明）
- GitHub Actions 用的部署 token → 放 repository secrets

前兩項與第三項都由 `tests/no-secrets.test.js` 盯著：它掃全部被追蹤的檔案，看內容不看檔名（`.gitignore` 換個檔名就漏了）。姓名那一條掃的是「中文字黏著病歷編號」這個形狀，另外在有 `.local/aliases.json` 的機器上會拿真的名單再對一次 —— **測試本身一個真名都不能寫**。

`.gitignore` 至少要有：

```
*-firebase-adminsdk-*.json
serviceAccountKey.json
/backups/
/exports/
.env*
```

> 注意：一旦真實客戶資料被 commit 過，就算之後刪掉，**它仍然留在 git 歷史裡**。寧可一開始就擋掉。

#### 其他

- Repo 保持 private（保護的是你的程式碼，不是客戶資料，但仍然值得）
- Firestore Security Rules 必須寫，**絕不使用測試模式的全開規則**（那個預設 30 天後失效，但期間內是全世界可讀寫）
- Firebase Auth 限定白名單帳號；未來開放同事時用自訂 claim 或白名單集合控管
- Rules 要有測試。Firebase Emulator Suite 可以在本機驗證「未登入讀不到」「A 使用者改不了 B 的資料」

---

## 11. 開發節奏

使用者的態度：**「不急。可以做到 70–80% 再開始用。希望仔細做完整、好維護。有些事情需要和她繼續使用後回饋，然後迭代。」**

所以：

- **不要為了趕上線而砍掉驗證、稽核、錯誤處理。** 那些正是第 6 節要求的東西。
- **每個切片做完整再往下**：一個功能包含 domain 規則 + Firestore rules + UI + 錯誤狀態 + 稽核 + 對帳，才算做完。
- **大約做到第 5 步時交給她試用**，開始收回饋。剩下的功能依實際使用調整——現在 spec 裡的某些設計一定會被真實使用推翻。
- **響應式從第一天就做，不要事後補。** 兩種載具各佔一半，先做手機再「加寬」通常會得到一個在 iPad 上很空的版面。每個元件一開始就想好兩種寬度長怎樣。

### 建議順序

| # | 內容 |
|---|---|
| 1 | 專案骨架、Auth、Firestore Rules 基本框架、分層結構 |
| 2 | 主檔設定（診間、治療師、器材、療程、方案範本、點滴品項） |
| 3 | 客戶 + 額度池（含方案展開）+ 進度顯示 |
| 4 | 來訪 + 狀態機 + 時段內嵌 + 衝突與禁忌檢查 |
| 5 | 任務自動產生 + 首頁 TODO 中心 ← **這裡交付試用** |
| 6 | 可用性收集（原文 + 規則解析） |
| 7 | 壓表模式（資訊攤開、優先序、逐位推進、快速記錄） |
| 8 | 資料健檢／對帳、稽核檢視、備份匯出 |
| 9 | 時段反查、LINE 訊息產生器 |
| 10 | 日曆、試算表單向同步 |
| 11 | 舊資料匯入（dry-run + 報告） |
| 12 | 介面重新設計 |
| 13 | 客戶自己填時間的表單（第 8.9 節） |
| 14 | 多帳號（開放同事使用） |

---

## 12. 種子資料

以下為目前已知的資料，**全部都要能在設定頁修改**，這只是初始值。

### 方案：筋骨強身（$288,000，會籍 1 年，限本人）

| 分類 | 項目 | 次數 | 型態 |
|---|---|---|---|
| 醫療專業諮詢 | 復健科醫師門診 30min | 6 | single |
| | 物理治療師諮詢 20min | 4 | single |
| | 營養師諮詢 20min | 4 | single |
| 健康檢測 | 身體組成分析（每季一次） | 4 | single |
| | 體適能檢查分析（每季一次） | 4 | single |
| 物理賦能課程 | 復能三選一(60)（高能量雷射／超磁場／INDIBA） | 12 | **pool** |
| | ILIB(60) | 20 | single |

### 方案：8萬方案（試算表模板，購買數量 = 1 時的基準）

Inbody 4、復健門診 2、物理諮詢 4、營養諮詢 4、體適能分析 4、復能三選一(60) 20、ILIB(60) 12。

購買數量 N → 各項次數 × N。

> 兩個方案的復能／ILIB 次數是相反的（12/20 vs 20/12），這是正常的，不是筆誤。

> **健檢的課程要把 `followupCourseId` 指到二返**（種子資料已經設好）。沒設的話健檢
> 不會配二返額度，也不會長出「約二返」的待辦 —— 而那在畫面上看不出來。

### 空間（2026-09-08 重畫，見 `docs/adr/0079`）

**三種類型，一間 4 號都沒有。** 簡寫裡的數字就是門上那個號碼。

| 類型 | 房間 | 簡寫 |
|---|---|---|
| 治療室 | 治2、治3、治5、治8 | 就是全名（本來就夠短） |
| 點滴室 | 點滴2、3、5、6、7、8、9、10 | `.2` `.3` `.5` `.6` `.7` `.8` `.9` `.10` |
| VIP室 | VIP2、3、5、6、7 | `vip2` `vip3` `vip5` `vip6` `vip7` |

**簡寫是另一格不是改名**：月曆、日／週那一列、讀取卡片印簡寫，
主檔清單、設定頁、確認框、試算表、稽核印全名。

**沒有床位這一層了** —— 一間就是一個資源。舊來訪身上那個 A／B 由資料健檢的
「來訪上還記著床位」清掉。

> 2026-08-19 使用者確認過治7、治9、治10、ILIB4「也都還在」。
> 2026-09-08 她重畫清單時把那四間拿掉了（ILIB4 有個 4）—— **既有來訪還指著
> 它們**，所以刪掉之後那幾筆印不出診間名字，她知道並且選了。

限制：**EECP 只能在 治療室5、治療室8**（`allowedRoomIds`）。
**優先置頂**（`preferredRoomIds`，只是順序不是限制）：EECP 治5、治8；
ILIB `.10`、治2、治3；營養點滴靠 `allowedRoomTypes` 就已經只剩點滴室。

### 器材

**兩格名字回答兩個問題**（`docs/adr/0078`）：

| 全名（她叫它什麼） | 別稱（月曆那一格的縮寫） |
|---|---|
| SIS | （空，全名就夠短） |
| INDIBA | IN |
| 高能量雷射 | （空） |
| ILIB | IL |

額度的名字讀全名（`復能-INDIBA(60)`），月曆讀別稱（`IN(60)`）。

> 那一台 2026-09-08 從「超磁場」改名成 `SIS` —— 她自己講的、寫的、記的都是
> SIS。既有資料庫靠資料健檢的「器材的名字跟建議的不一樣」跟上。

**SIS 與高能量雷射對體內金屬要提醒；INDIBA 與 ILIB 不用。**
提醒**不擋任何東西**（ADR-0074）—— 她選了那一台的時候跳一句話，並且建議改用 INDIBA。

每一台記著「用這台的那一段算哪一個課程」（ADR-0075）：前三台指復能（要治療師）、
ILIB 那一台指 ILIB（要診間）。復能四選一那一筆額度四台都用得到。

### 警示（`config/app/clinicalFlags`）

體內金屬（紅・實心）、血管難打（茶・實心）、第一針（藍・空心）。
**什麼都不擋**，只是要在她壓表那一刻被看到。每一個字自己挑顏色與填法。

### 合作機構（`config/app/partners`）

自然美。客戶身上打得上的一個標記，**不產生任何待辦**（ADR-0076）。

### 治療師與醫師

兩種人共用 `config/staff`，靠 `role` 分開。**不可以互相取代** —— 復能三器材要的是物理治療師（`CONTEXT.md`）。

物理治療師：騰崴（行事曆上寫騰威）、芝寧、LuLu、欣穎（也寫新穎）、耕宇、姿璇、怡婷、珮喩、王婷。

醫師：夏、許、李。姓氏就是她講的全部，名字她沒說。**門診（A 類）一律選得到醫師**，其餘課程看主檔上的 `requiresDoctor`（見 [ADR-0058](./docs/adr/0058-outpatient-always-picks-a-doctor.md)）。壓表那一頁與來訪編輯器兩邊都有那一排。

> 2026-08-20 之前這裡寫的是「醫師不放進 `config/staff`」。她那天決定要記進 app，見 [ADR-0026](./docs/adr/0026-doctors-are-assignable-staff.md)。她的舊試算表本來就手寫著 `7/13 二返(夏)`，括號裡那個字在醫師進 app 之前 app 記不住。

### 營養點滴品項

護心抗老、護肝排毒、腸道修復、速利清、猛健樂、NAC 愛咳痰、雪顏亮彩。

她在行事曆與試算表上寫的是簡寫：雪顏、護肝、腸道。

---

## 13. 尚未確認的事項

開發時如遇到需要決策，請以「先做可設定、預設值合理」的方式處理，並記錄下來。

以下都是**資料缺口**，不是設計缺口——設定頁填得進去，填錯改一下就好，不影響資料模型：

- [x] ~~各點滴室床位數~~（2026-09-08 **床位那一層取消了**，一間就是一個資源，
      見第 12 節與 `docs/adr/0079`）
- [x] 完整治療師名單（2026-08-19 確認，見第 12 節）
- [x] 各課程可用的診間類型與例外（2026-09-08 三條指派規則與三組優先順序都
      確認了，見第 7 節規則 2、3）
- [ ] 「打電話」的實際時機定義（前一天提醒電話？）
- [x] 二返距離健檢的標準間隔（2026-08-19 使用者選 **7 天**，做成設定頁的
      `followupDueDays` 可調，見第 7 節規則 8）
- [ ] 健檢的壓表是在 Examine 還是別的系統
- [ ] 「已壓表超過 N 天沒回覆」的 N 值
- [ ] 舊資料匯入的欄位對應細節

已確認：**營養點滴不需要指定護理師**。所以 `config/staff` 的角色只有物理治療師與醫師兩種，沒有護理師。

---

## 14. 給 Claude Code 的提醒

- **沒有桌機。** 只有 Android 手機和 iPad，各約一半。公司電腦連不了外網，任何「在桌機上比較好操作」的設計都是錯的。
- **不做兩套 app，也不做只有某台裝置能用的功能。** 同一組元件、三段斷點；iPad 是看得更多，不是功能更多。
- **壓表當下她在看自己的裝置、打另一台電腦。** 資訊要一眼可讀、輸入要極少。判斷由她做，app 只負責攤開資訊與記錄結果。
- **壓表進度存雲端**（`batches`），換裝置要能接著做。佇列順序建立時凍結，不要每次重算。
- 任何「應該記得」的事情都要變成系統狀態，不要變成給使用者的提示文字。
- **這個 app 沒有任何業務規則會擋下儲存**（ADR-0074）。剩下的 errors 全部是「這筆資料寫下去會壞掉」（沒選額度、時間格式不對、指到一筆不存在的東西），不是「這件事不該做」。
- **永不硬刪除、永遠留稽核、關鍵操作可復原。** 這是使用者明確要求的第一優先。
- 保留原始文字（可用性原文、備註）永遠優先於解析結果。
- 業務規則寫在 `/domain` 的純函式裡，不要散落在 UI 事件處理器。
- UI 文案用繁體中文，沿用第 2 節的名詞表。
- 不要引入 React／Vue／TypeScript build pipeline。
