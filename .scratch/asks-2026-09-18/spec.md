# 2026-09-18 她丟來的五條

一支 PR（開向 `develop`），五支 issue、一支一個 commit。五條互不依賴、全部是畫面層：
**沒有碰任何業務規則、沒有推翻任何 ADR**，所以不補 ADR、不改 SPEC。

她 2026-09-18 回的：「策略都同意，三個決定也都同意，照你的建議五條同一支 PR，都一次做完不用問，
並且開完pr且合併部屬」。

## 第一段談定的

| # | 她說的 | 分類 | 談定的做法 |
|---|---|---|---|
| 01 | toast 蓋住底部導覽列，誤觸「復原」 | UI bug | 手機上貼在導覽列**上方**、**靠左**，右邊讓出懸浮鈕那一欄；iPad（側邊導覽）不動 |
| 02 | 日曆「＋」帶到舊日期 | bug | 「剛點過的那一天」只有在畫面現在顯示的範圍內才算數，否則用畫面正在看的日期。三顆（來訪、行事備註、待辦）共用一支。**月檢視裡還標著的那一格照樣算數**（她同意）|
| 03 | 點滴品項的時長要多點一次 | bug | `readDraft()` 在額度換了的時候，品項用那一筆買的那一款（同 `blankSlot()`、壓表） |
| 04 | 新增客戶那一排按鈕懸空 | UI bug | 同批次取消 9/13 的解法：那一條出現時捲動區不留底下的留白 |
| 05 | 照片預覽能不能轉 | 新功能 | `openPhoto()` 加一顆「轉 90°」，CSS 轉、**不記住**（她同意） |

## 第一段量到的（模擬器，414×896）

- 01：導覽列上緣 838；「復原」在 y 823–865、x 203–263，正好蓋住「壓表」「日曆」兩格的上半。
- 02：日檢視在 8/29（今天），之前在月檢視點開過 8/5 又關掉 → 選人那張標題「8/5(三) 要幫誰排？」、日期 2026-08-05。
- 03：從復能切到點滴那一筆 → 結束 11:00、七顆品項沒有一顆選著；直接存會被擋
  （「第 1 個時段：營養點滴 每次都要記錄施打的品項」），點一次品項才變 12:00。
  **所以這條路存不出錯的時長**。會存錯的是兄弟情境：兩筆不同品項的點滴額度 A → B，品項留在 A。
- 04：捲到一半那一條底 794、導覽列頂 838（縫 44px）；捲到底縫 58px；iPad 底 738／畫面 768（縫 30px）。

## 做完之後要補的

- `sw.js` VERSION：v135 → **v136**（一支 PR 跳一次）
- `CLAUDE.md` 連動表補一列：**貼在畫面底部的東西**（toast、sticky 那一條）跟 `.app__main` 的留白、
  導覽列、懸浮鈕的關係 —— 批次取消、新增客戶、toast 已經是第三次同一個形狀
