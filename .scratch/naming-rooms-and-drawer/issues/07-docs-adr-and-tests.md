# 文件、ADR、測試收尾

Status: open（最後一支）

## 要補的 ADR

| 編號 | 題目 | 為什麼要一支 |
|---|---|---|
| 0078 | 復能與 ILIB 的名字只有六種 | 推翻 `naming.js` 的 `復能(SIS)` 接法與 `poolName()` 的標點。ADR-0077 的立論不動，動的是字面 |
| 0079 | 指派由器材決定，而空間清單沒有 4 號 | 記下「治療室／點滴室／VIP 室」與停用 ILIB 室的理由，以及 `preferredRoomIds` 為什麼是排序不是限制 |
| 0080 | 讀取卡片只畫她點的那一段 | ADR-0018「四個畫面共用一支」不推翻 —— 多一個參數，沒帶就照舊 |

**不要改掉 ADR-0075 與 0077**（`docs/agents/domain.md` 的慣例：推翻既有決定
要補一支新的，不是改舊的）。

## 要改的既有文件

| 檔案 | 改什麼 |
|---|---|
| `SPEC.md` 第 12 節 | 診間清單整段重寫（治7/治9/治10/ILIB4 的那條 2026-08-19 註記要留著並註明已停用） |
| `SPEC.md` 第 7 節規則 2、3 | EECP 的限制與推薦分開講；健檢等五個課程不需要空間 |
| `SPEC.md` 第 4.5 節 | 擇一池沒選器材存不下去 |
| `SPEC.md` 第 13 節 | 「各點滴室床位數」那一列拿掉（床位取消了）；「各課程可用的診間類型」補上答案 |
| `CONTEXT.md` | 「床位」那一條標成已停用；「診間」那一條補上簡寫；六種名字進詞彙表 |
| `CLAUDE.md` 連動表 | 「一段來訪在畫面上叫什麼」「一筆額度叫什麼」兩列改字；新增「診間的名字或簡寫」「一筆來訪的讀取卡片」兩列 |
| `docs/課程與待辦對照表.md` | `assigns` 那一欄跟著 `03` 改 |
| `docs/操作手冊.md` | 加購那一段的六種名字；壓表要先選器材 |
| `docs/常見問題.md` | 新增「我點一段為什麼看到三段」（已修）、「為什麼健檢不用選診間了」 |
| `docs/邊界測試清單.md` | 同一天三段、四選一切換器材、停用診間的既有來訪 |
| `README.md` | 開發狀態表打勾 |

## 全套測試

```
npm test                 # 單元
npm run test:rules       # Firestore Rules
npm run test:e2e         # Playwright（約 18 分鐘，CI=1 快一倍）
```

**跑 E2E 的時候不要改 `public/`**（記在 `local-e2e-run-gotchas`）。

## 交付

- 從 `develop` 開 feature 分支，**PR 的基底是 `develop` 不是 `main`**
- 每一支 issue 一個 commit，跑完 `npm test` 才進下一支
