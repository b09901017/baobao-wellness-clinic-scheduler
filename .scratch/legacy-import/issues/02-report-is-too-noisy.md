# 比對報告被「沒有次數也沒有勾選」洗版，真正要看的埋在裡面

Status: done
回報者：拿到真的 `.xlsx` 跑 dry-run，2026-08-18
動工前先讀：`SPEC.md` 第 6.10 節、`docs/legacy/samples/dry-run-report.txt`

## 症狀

只買了健檢的客戶（K、L、M、O、R、S、U 共 7 位）在報告上長這樣：

```
── 客戶K｜工作表：客戶K
   額度 1、來訪 1、時段 1
     · 3.6萬健檢 1 次
   ⚠ 表頭｜7月17日｜這些日期沒有寫年份…
   － 第 2 列 Inbody：沒有次數也沒有勾選
   － 第 3 列 復健門診：沒有次數也沒有勾選
   （再八行一模一樣的）
```

**九行講的是同一件事：這位客戶沒買 8 萬方案裡的那些項目。**
那不是問題，是常態 —— 空白的模板列本來就會全部落到這裡。

同一份報告裡真正要她看的是「`5萬健檢(心臟)` 對不到課程，整列沒有匯入」，
那一行被夾在中間。

## 影響

報告是她按下「開始匯入」之前唯一的依據（`SPEC.md` 第 6.10 節）。
一份 316 行、其中 100 行是雜訊的報告，她會直接捲到最下面按下去。

## 想要的樣子

`reportText()` 把 `skippedRows` 收成一行：

```
   － 第 2、3、4、5、6、7、8、10、11 列沒有次數也沒有勾選（空白的模板列）
```

`problems` 一行都不要少 —— 那些是真的要看的。
