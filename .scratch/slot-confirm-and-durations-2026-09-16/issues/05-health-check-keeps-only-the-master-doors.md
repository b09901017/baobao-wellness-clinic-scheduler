# 資料健檢的「去看看」只留通往主檔設定的那幾顆

Status: todo
來源：`../spec.md`
動工前先讀：`domain/health.js` 的檔頭（2026-09-16 那一段要改寫）、
`.scratch/prelaunch-fixes-2026-09-16/issues/10`、`docs/常見問題.md:194`

## 她要的（原話）

> 資料健檢中同一天有兩筆來訪以及逾期任務 資料過期都還有去看看的按鈕？可以不用有
> 應該說去看看這個按鈕都不要了

問過之後定案：**設定主檔那 9 顆留著**（那幾列唯一的出口 ——「器材主檔少了一台」
把她帶去建那一台），其餘 13 顆拿掉。

## 現在有幾顆、在哪

2026-09-16 那一輪只拿掉了指向 `#/visits/:id` 的那幾顆，`#/calendar` 的留著了 ——
**而她點名的「同一天有兩筆來訪」正是其中一顆**。

| 去哪 | 幾顆 | 行號 | 這一輪 |
|---|---|---|---|
| `#/customers/:id` | 9 | 330、370、390、579、707、755、781、877、922 | **拿掉** |
| `#/` | 1 | 672（逾期任務）| **拿掉** |
| `#/calendar` | 3 | 1158（舊床位）、1336（狀態跟時段對不起來）、1460（同一天兩筆）| **拿掉** |
| `#/settings/*` | 9 | 944、980、1029、1093、1110、1127、1213、1264、1301、1368、1414 | **留著** |

（設定那一欄實際是 11 個 `link:`，分屬 9 種檢查。以 `link:` 為準逐一核對。）

## 要做什麼

1. 上表前三類的 `link` 一律 `null`，而且 **`detail` 要自己把話講完整** ——
   拿掉出口之後那一列是她唯一的資訊來源（照 2026-09-16 那幾列的作法）。
2. `domain/health.js` 的檔頭那一段改寫：現在寫著「其餘三種留著」，要改成
   「只剩指向主檔設定的那幾顆」，並寫上理由（那是那幾列唯一的出口）。
3. `ui/views/health.js:152` 的 `finding.link ? … : ''` **留著** —— 還有 9 顆在用。
4. `docs/操作手冊.md` 的資料健檢那一節與 `docs/常見問題.md:194-201` 那一條跟著改
   （常見問題現在寫「其餘幾列的『去看看』還在（資料過期指那位客戶、狀態跟時段
   對不起來指日曆）」—— 那兩句在這一輪之後都不成立了）。
5. `tests/health.test.js:415-421` 那一條從「來訪相關的那幾列」擴大成
   「只有 `#/settings/` 開頭的 link」。

## 判準

- 跑一次 `runHealthCheck()`，把每一個 finding 的 `link` 收集起來：
  **要嘛 `null`，要嘛以 `#/settings/` 開頭**，第三種都不合格
- 拿掉出口的每一列，`detail` 自己讀得懂要去哪裡做什麼
- 這一行會不會讓某一列變成「講了問題卻沒有任何出口」？
