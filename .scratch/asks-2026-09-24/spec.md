# 2026-09-24 她丟來的六點：時段才是原子單位的第二輪

**共同的根：** ADR-0081（2026-09-08）把狀態搬到時段上，ADR-0097（09-16）讓「確認」跟上了，
但**「結案（簽療程單）」「寫紀錄」「健檢那條鏈」「待辦的詳情」四族還在讀整天**。
她報的五點裡有四點是這個形狀；第六點要的「全域排查」又找到九件同形狀的（R1–R9）。

## 她 2026-09-24 回的

> 流程：三個都同意你的建議第一批一支 PR、先上 staging 點過，再做第二批，都不用逐支看過可以一次做完，
> 但隨時有問題還是可以問
>
> 1 能不能預設還沒回，然後可以的選勾，不能的選叉之類的，你可以幫我設計
> 2 也是同上預設先不結做了打勾未到打叉之類的，可以用可愛的icon之類的不用說明
> 3 未到可以改成退回簽療程單之類的，或可以改成其實有到這樣
> 4 好 只有已完成按得下去可以，不要讓整個流程亂掉，也可以在幫我排查會不會有其他地方會不小心讓流程亂掉
> 5 好可以，只在這種時候補上時間
> 6 好照你的建議
> 7 全都修，但是一樣要注意會不會牽連或是連累產生衍生問題

## 兩批

**第一批（規則，一支 PR，先上 staging 點過）**：01–07。動到狀態機、結案、寫紀錄、健檢鏈、確認。
04／05／06 **一定要一起出**：只結一半的那一天整筆停在「已確認」，寫紀錄與追蹤健檢報告若還在讀整筆，
先結掉的那一段就永遠長不出後面的待辦。

**第二批（畫面，第一批上 staging 之後）**：08–13，外加 14（規則，第一批交付時她決定要擋的邊界）。

**第一批已出貨**：PR #130 合進 develop（`d00b644`），staging 部署成功，sw.js v138。沒進 main。

**第二批**：PR #131（開向 develop，sw.js v139），08–14 全部 done，外加排查時她同意的兩件（補登過去那一天的確認框、客戶詳情「看全部」的詳情）。新 ADR 0113。手動驗收清單在本機模擬器上走過（這個 session 沒有瀏覽器工具、登不進 staging），合進 develop 之後要在 staging 再點一次。

| # | 什麼 | 來源 | 批 | Blocked by |
|---|---|---|---|---|
| 01 | 改狀態之前先把每一段的狀態補齊 | 二b | 1 | |
| 02 | 未到只能「退回簽療程單」；長按每一顆都問那一段 | 二a、R1、Q3 | 1 | 01 |
| 03 | 「已完成不能直接改」看那一段 | R6 | 1 | 02 |
| 04 | 簽療程單一段一段來（✓／✗、預設先不結） | 一、R2、R7、四b（簽單那一頁）、Q2 | 1 | 01 |
| 05 | 寫紀錄一段一段長 | 五（規則） | 1 | 04 |
| 06 | 健檢鏈看健檢那一段 | 三b、R3、R4、Q4 的排查 | 1 | 04 |
| 07 | 確認抽屜 ✓／✗、預設還沒回 | 一、Q1 | 1 | 01 |
| 08 | 「這一張待辦講的是哪幾段」＋ 詳情只開那幾段 ＋ N 項 | 四a、五（顯示）、R8 | 2 | 05、06 |
| 09 | 待辦列三行、說明收進 `?` | 四b、四c、Q5、Q6 | 2 | 08 |
| 10 | 二返排在最後面 | 三a | 2 | |
| 11 | 「哪一次健檢」標狀態，只有已完成按得下去 | 三c、Q4 | 2 | 06 |
| 12 | 客戶詳情的 LINE 訊息只講還算數的段 | R5 | 2 | |
| 13 | 客戶詳情「來訪紀錄」那一列逐段 | R9 | 2 | |
| 14 | 日子過了才變成已確認的段，不長掛號待辦 | 第一批留下的邊界，她「要擋掉」 | 2 | |

## 新 ADR（舊的一個字不改）

- **0110 待辦中心的兩張抽屜都可以先決定幾段**（04、07）—— 推翻 ADR-0097 的兩句：
  「一次確認整天是待辦中心那條路」與「`visitsToClose()` 不改，收尾整天走一次」
- **0111 未到只能退回簽療程單**（02）—— 狀態機第一版（`239f5a7`，2026-08-18）的 `no_show → confirmed / cancelled`
  沒有任何 ADR 或她的原話；這一支第一次替它寫下理由
- **0112 寫紀錄與健檢鏈看那一段**（05、06）—— 延伸 ADR-0066、0042、0081

## 做完之後要補的

- `sw.js` VERSION：第一批 v137 → **v138**，第二批 → v139
- `SPEC.md` 第 4.1 節（狀態機）、長按選單那一段（1435 行附近）、待辦中心的確認與收尾
- `CLAUDE.md` 連動表：「一段來訪的狀態」「任何一列的長按選單」「任務有兩個時機」「健檢與二返的關係」、
  新增「結案」一列
- `docs/課程與待辦對照表.md`（寫紀錄逐段）

## 第二批的審查（`/matt-code-review`，2026-09-24）

照改的：拍 Abovee 那一排的「已約」也走 `examChoiceNote()`（三個入口都寫「已約 9/30」）、一次都沒排過時也給 `?`；
舊寫紀錄的歸屬＝要寫紀錄而且做完的段（`recordSlots()`，取消與未到的不認）；二返的存檔驗證**只擋新接上或換過的連結**
（存著的那一份本來就指著它的是舊資料，ADR-0011）；共用元件 `tasklist.js` 的屬性改成自己的前綴（`data-tasklist-task`）；
`messages.js` 走 `liveSlots()`；CLAUDE.md 補 `only`、`ownsTask()`、`followupsLast()`、`asPending()`；SPEC 客戶詳情「來訪紀錄」；
ADR-0113 拿掉補登那一句（那不是這一支的決定）；兩則過期的循環 import 註解。

留著、寫了理由的：`tasklist.js` 的 `?` 在按鈕外面（button 裡放不了 button）；分類頁那一顆 `?` 在 `<label>` 裡（跟 `form.js` 同一種放法）；
每一段都一樣時印一個字（13）；三個入口各自兩行的「task → taskSlots → focusFor」沒有再包一層。
