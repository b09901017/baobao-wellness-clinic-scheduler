# 全域清掉說明文字，以及「app 看不到同事在 Abovee 壓的東西」

Status: todo
來源：使用者，2026-09-08（需求三 2c、2f）＋ 2026-09-09 確認留哪三句

## 她要的

> 我發現很多的提醒都會提到「app 看不到同事在 abovee 壓的東西」類似的這個東西，
> 不用不需要，這個提醒完全是多餘的，全域請掉

> 然後畫面拜託 不要說明文字 太雜亂了 會視覺疲勞

## 要清的

**Abovee 那一句（UI 上共四處，domain 與 docs 的註解不動）：**

| 檔案:行 | 那一句 |
|---|---|
| `visitEditor.js:314` | 「這些都只是提醒，不會擋著不讓你存 —— app 看不到同事在 Abovee 上壓的東西」（連同整張卡，見 issue 10） |
| `visitEditor.js:810` | 確認框裡的「app 看不到同事壓的東西，診間有沒有被佔用要以那邊為準」 |
| `calendar.js:221` | 「這裡只有你自己排的。同事在 Abovee 壓的看不到 ——…」 |
| `calendar.js:518` | 「但同事在 Abovee 壓的看不到，空的不代表真的空著。」 |

**說明文字：**

- `visitEditor.js` 的「跟著這一筆來訪，不是掛在客戶身上 —— 那是備註…」
  （issue 05 已經把那一格換掉，順手）
- `visitEditor.js` 的「預設接在上一段結束的 15 分鐘後」
- `visitEditor.js` 的「上面紅色的問題要先處理才存得下去」
- `schedule.js:241` 的 `page__lead`「一個人壓完再換下一個」（issue 01）
- `schedule.js:246` 的 `card__note`「點一個月就進去。裡面會照…」（issue 01）
- `schedule.js:260` 的 `card__note`「輸入日期、時間與課程…」（issue 01）

## 留著的三句（她 2026-09-09 明確說留）

1. `f.undecidedHint()`「先選上面那一台 —— … 要治療師還是治療室」
   —— 不留的話那裡是一片空白，她不知道在等什麼
2. 「主檔裡還沒有醫師，到設定 → 治療師與醫師新增」
   —— 空清單不講原因會被當成壞掉
3. 確認框裡的後果句（`domain/consequences.js`）

**判準寫下來，之後才不用再問一次：**
「不講會出錯」的留，「講了也不會改變她下一步做什麼」的拿掉。

## 注意

`SPEC.md` 第 4.7 節、`docs/常見問題.md`、`docs/操作手冊.md`、ADR-0002 裡的
同一句話**不要動** —— 那是寫給看程式與看文件的人的，不是畫面上的提醒。

## 測試

- 原始碼掃描（新增一支或併進 `tests/no-secrets.test.js` 旁邊）：
  `public/js/ui/` 底下沒有「看不到同事」這幾個字
- 那三句留著的，各有一條斷言釘著它還在
