# 文件跟上

Status: done
來源：CLAUDE.md 的「容易漏掉的連動」

## 要改的

### `SPEC.md`

- **第 4.4 節**：標題與內文改成「建立的動線是那一天，狀態、次數與那一句話的
  原子單位是時段」。加上「一人一天一筆」現在是程式擋得住的（issue 04）
- **第 5.2 節**：那句「單次原子寫入」的理由**要修正** ——
  `repo.commit()` 用的是 `writeBatch`，本來就跨文件原子。內嵌的理由改成
  「一起建立、一起確認、一起讀，而且一筆文件讀得完」
- **第 5.3 節**：`slots[]` 補上 `status`（ADR-0081 加的，一直沒補進來）與
  `note`；`visit.note` 標成「舊資料才有，不再寫入」

### `CONTEXT.md`

- 「來訪」：改成「客戶某一天到院一次。**畫面上不出現這個詞**，她看到的是日期與時段」
- 「時段」：補上「自己的一句話」
- 新詞「記一句」：跟「備註」（客戶身上的彩色小丸）與「隨手記」分清楚

### `docs/adr/`（三支新的）

| | 標題 | 講什麼 |
|---|---|---|
| 0083 | 一人一天一筆，來訪是那一天不是一次建立 | issue 04。**不推翻 ADR-0081**，是把它做完 |
| 0084 | 記一句記在時段上 | issue 05。含舊資料 A 方案與 ▤ 的三種狀態 |
| 0085 | 改一筆來訪就是改那一段 | issue 07。ADR-0080 那條線延伸到編輯器 |

**不要改掉 ADR-0025、0080、0081** —— 它們沒有被推翻，是被延伸。

### `CLAUDE.md` 的「容易漏掉的連動」

- 「一段來訪的狀態、或它算不算數」那一列：補「畫面上一律講那一段的狀態」
- 新增一列：「一段來訪身上那一句話」→ `slot.note`、`slotNote.js` 共用元件、
  舊資料只搬一次
- 「一筆來訪改得動的地方」那一列：補「而且只改她點的那一段」

### `README.md`

開發狀態表打勾。

### `docs/操作手冊.md` / `docs/常見問題.md`

- 手冊：新增與修改的動線變了，那兩段要重寫
- 常見問題：新增一條「我在日曆上排了第二次，為什麼併到同一天了？」

### `docs/邊界測試清單.md`

- 「同一天排到已完成的那一筆上」
- 「舊資料的那一句話搬過去之後再存一次」
