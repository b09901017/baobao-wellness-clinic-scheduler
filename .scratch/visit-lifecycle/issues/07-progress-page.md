# 進度追蹤頁：一頁看完每位客戶排了哪些時段、各自到哪一步

Status: done
回報者：使用者，2026-08-20
動工前先讀：`SPEC.md` 第 8.0 節（斷點）、第 8.5 節（客戶總覽）、
`docs/adr/0004-counts-are-derived-in-detail-cached-in-lists.md`、
`docs/adr/0018-detail-pages-are-one-page-not-a-stack-of-cards.md`、
`docs/adr/0020-the-calendar-never-changes-pages.md`

## 她要什麼

> 我還希望多做一個進度追蹤頁面，也就是呈現每個客戶，我目前這個月預約了那些時段，
> 而這些時段分別的狀態是什麼（待確認，已確認，已完成），我希望能有一個頁面讓我
> 一目了然的掌握我所有客戶的情況，並且這個頁面要和 todo 日曆對的上…
> **進度追蹤那頁是唯讀的不能修改**

2026-08-20 補的四件事：

- **要能往前翻**月份
- 每一列旁邊要**小小地記上「這個狀態是什麼時候更新的」**
- 狀態用**顏色或小 icon**呈現
- 入口是**客戶頁的一顆切換**

她舉的例子（名字改寫過）：

> 客戶A 8/3(一) 13:30–14:30 的復能，狀態是待確認（紀錄時間，小小的）；
> 14:45–15:45 的靜脈，狀態是已確認（標記時間…）

## 現在沒有，兩個最接近的都不是

| | 為什麼不是 |
|---|---|
| 客戶總覽 `#/customers` | 一位客戶一列的**合計**，看不到每一段各自是什麼狀態 |
| 客戶詳情的「這個月」 | 一次只看得到一位 |

## 狀態要掛到時段上（2026-08-20 使用者選 B）

**她選了 (B)：時段各自有狀態。** 理由是她給的這一句：

> 雖然不常發生，但是還是有可能發生**客人只做一半就走**的情況

她同時修正了一個誤解：

> 補充一點待確認不是等治療師，是**我已經先壓表，接著要和客戶確認我事先壓的時間是否可以**

**這兩句話合起來，指出的其實是一個比「全部逐段」更精確的答案。** 把五個狀態拆開看：

| 狀態 | 講的是什麼 | 逐段會不一樣嗎 |
|---|---|---|
| `pending_confirm` | 壓好了，還沒問客人 | **不會**。同一天的時段是一起壓、一起問的 |
| `confirmed` | 客人說這個時間可以 | **不會**。客人說不行的那幾段在確認動線裡就被移出來訪了 |
| `cancelled` | 這一天不來了 | **不會**。整天的事 |
| `done` | 真的做了，療程單簽了，扣次數 | **會** —— 她講的「只做一半就走」 |
| `no_show` | 沒來 / 沒做 | **會**，同上 |

也就是說**會逐段不一樣的是「結果」，不是「預約進行到哪」**。
前三個是一次預約作為一個單位的生命週期，後兩個是那天實際發生了什麼。

### 建議：預約的狀態留在來訪上，結果記在時段上

`slot.attended` **這個欄位本來就在**（`SPEC.md` 第 5.3 節），
只是現在它是跟著整筆狀態一起寫的（`ui/views/visitEditor.js` 第 590 行
`attended: to === 'done'`），沒有自己的意思。讓它變成真的：

- 收尾時她逐段勾「這段做了沒」，`slot.attended` 各自記
- 全部沒做 → 整筆 `no_show`；有做 → 整筆 `done`，但**次數依 `attended` 逐段算**
- `counts()` 從「看來訪狀態、數它有幾個時段」改成「看每一段自己的結果」

**這比「五個狀態全部搬到時段上」小得多**，而且它接得住她真正講的那個情境。
把 `pending_confirm` 也搬下去，換來的是每一段各自要走一次確認動線 ——
而客人是一次講完一整天的。

### 這一項要小心的地方

- **ADR-0004**：`counts()` / `summarize()` / `reconcile()` 只能一起改。
  `entitlement` 上的 `doneCount` / `bookedCount` 是快取，改法要跟著。
- **資料相容**：既有的來訪 `slot.attended` 可能是 `undefined`。
  讀的時候要能吃 —— `done` 的來訪沒寫 `attended` 一律當成有做（那是舊的寫法），
  **不要當成沒做**，否則所有人的次數會一夜之間全部退回去。
- **匯入的來訪**（ADR-0011）也是 `done`，同上。
- 這一段動的是次數，是這個專案最不能錯的東西。**要有測試，而且要單獨一支 commit。**

**這一支（進度追蹤頁）只是顯示端，不要在這裡改次數的算法。**

> **2026-08-20 更新：逐段結果已經在 `issues/04` 做掉了。**
> `slot.attended` 現在是真的（`domain/entitlements.js` 的 `slotOutcome()`），
> 收尾畫面逐段勾，次數只扣做了的那幾段，補了
> [ADR-0025](../../../docs/adr/0025-what-happened-is-recorded-per-slot.md)。
> 這一支直接讀它就好，不用再自己算。
> 「這個狀態什麼時候更新的」也有欄位了：`visit.statusAt`。

## 「這個狀態什麼時候更新的」要有地方存

現在存得下來的只有兩個：`confirmedAt`、`cancelledAt`。
**沒有 `doneAt`**，也沒有 `no_show` 的時間。待確認那個時間可以用 `createdAt` 代替
（來訪一建立就是待確認），但那不精確 —— 改期重排會蓋掉。

兩條路：

1. **各狀態各一個時間戳**（`confirmedAt` / `doneAt` / `noShowAt` / `cancelledAt`）。
   簡單、讀起來便宜，但每加一個狀態就多一個欄位。
2. **從稽核紀錄推**。`audit` 每一次寫入都留了 `at` 與 `before`/`after`，
   所以「什麼時候變成已確認的」本來就查得到，而且**不可能不同步**。
   代價是進度追蹤頁要多讀一整包稽核，那一頁一次要畫二十幾位客戶。

**建議第 1 條**，`doneAt` 在 `issues/04` 就會補上（那一支要它）。
稽核留給「變更紀錄」那個面板 —— 它本來就是為了回答「這筆怎麼變成現在這樣」而存在的。

## 其他四個實作重點

1. **狀態的分類、顏色與符號不可以自己寫一份。** 和日曆（`issues/03`）、
   試算表（`issues/05`）用同一組 `status → { label, cls, mark }`
   —— 待確認 `○`、已確認 `△`、已完成 `✓`。使用者原話：「這很重要」。
2. **次數現算，不讀計數欄位**（ADR-0004：真正不能錯的地方一律現算）。
   照月份範圍一次撈完再自己分組，**不要一位客戶打一次查詢** ——
   `visits` 有 `date` 的複合索引（`SPEC.md` 第 5.4 節）。
   往前翻月份時要記得換月就是換一次查詢，加個簡單的快取。
3. **唯讀要防得住誤觸。** 最乾淨的是這一頁根本沒有可以改東西的控制項。
   如果要留「點進去看那一筆」的連結，那要是**讀取模式的資訊卡片**
   （ADR-0020：她點一筆的十次有九次只是要確認那天幾點、誰、做什麼），不是編輯器。
4. **版型三段都要能用**（`SPEC.md` 第 8.0 節）。手機單欄堆疊，
   iPad 橫式可以做成客戶 × 日期的矩陣。
   **不要做成只有 iPad 能看的表格** —— 第 1 節：不能有任何功能只有某台裝置做得到。

## 入口（2026-08-20 已決定）

**客戶頁的一顆切換。** 底下五個分頁（待辦／客戶／壓表／日曆／設定）已經滿了，
不再加第六個。

客戶總覽頁首現在已經有一顆小標籤在切「已停用的客戶」，這一顆放在同一排，
兩顆一起變成「看什麼」的切換。要注意的是那一頁上面**還有一排排序丸與課程丸**
（`SPEC.md` 第 8.5 節），控制項不要疊到第三列 —— 那一頁是拿來掃名單的。


## Comments

**2026-08-20 — 做完了。** `npm test` 從 651 變成 679 全過。

`#/customers/progress`，入口是客戶頁首那一排的「看這個月的進度」，
和「看已停用的」並排 —— 沒有加第六個分頁，也沒有疊到第三列控制項。

### 一位客戶一張卡，一天一組，一段一列

```
王小明                                    ○2  ✓1
  8/3(一)                            8/1 更新
  ┃ 13:30–14:30  復能           ○ 待確認
  ┃ 14:45–15:45  靜脈           ○ 待確認
  8/12(三)                          8/12 更新
  ┃ 10:00–12:00  0.75萬健檢     ✓ 已完成
```

她舉的那個例子（同一天兩段狀態不一樣）畫得出來 —— `issues/04` 把
`slot.attended` 變成真的之後，這一頁只要讀就好。

### 六個實作上的決定

1. **狀態的判斷多一支 `slotStatus()`，但 `attended` 的讀法還是只有一份。**
   `slotOutcome()` 是計數用的，待確認與已確認都收斂成 `booked`；
   這一頁必須分得出那兩種。所以 `slotStatus()` 是拿 `slotOutcome()` 的答案
   再退回整筆狀態做出來的 —— 改 `attended` 的意思時只要改 `slotOutcome()`。
   顏色、符號、說明全部走 `STATUS_VIEW`，這一頁一個都沒自己定義。
2. **唯讀是真的唯讀。** 點一筆浮出讀取模式的卡片，`canEdit: false` ——
   **連鉛筆都沒有**。要改她會自己去日曆或客戶詳情，而那是一個明確的決定，
   不是在對帳的時候手滑。卡片內容直接用日曆那一支（`visitReadHtml()` 改成 export），
   兩邊各畫一份遲早會變成一邊看得到治療師、另一邊看不到（同 ADR-0018 的前例）。
3. **快取每次進頁清掉。** 這一頁最重要的保證是「和待辦、日曆對得上」，
   而她要改東西一定得離開這一頁 —— 回來時看到的必須是改完的樣子。
   用一份跨頁存活的快取換一次查詢，換到的是一頁會騙人的畫面。
   同一次進來翻來翻去照樣不重查。
4. **連按三下箭頭不會畫錯月份。** 三次查詢回來的順序不保證跟按的順序一樣，
   所以每一輪帶一個號碼，過期的那一輪直接不畫。實際用 Playwright 連按驗證過。
5. **取消的時段不畫**，跟日曆一樣（時段已經還回去了）。頁尾寫明了。
6. **這個月沒排的人收成一排名字**，不佔畫面 —— 但也不能不列，
   「誰這個月還沒排」正是她要掌握的一半。

### 沒有做成 iPad 專用的矩陣

原本寫著「iPad 橫式可以做成客戶 × 日期的矩陣」。實際上二十幾位客戶乘三十天
是一張沒有人看得懂的表，而且那會變成「只有 iPad 看得到的東西」（SPEC 第 1 節禁止）。
改成 `.cardgrid`：同一份版型，寬螢幕一排放三張。**多的是同時看得到，不是另一套版型。**

### 驗證

- `tests/progress.test.js`（新，21 條）：月份範圍、取消／刪除／壞日期不畫、
  排序、逐段狀態、沒時間的排最後、`statusAt` 沒有就說沒有、合計對得起來、
  以及「畫得出來的每一種狀態都有顏色」。
- `tests/visits.test.js` 補 `slotStatus()` 的七條。
- Playwright 實跑真的 view（只把 `/data` 換成替身）：
  360／380／420／1080 四種寬度、淺色與深色、空月份、五個字的名字配四種狀態徽章、
  讀取模式卡片（確認沒有鉛筆）、連按箭頭的競態。360px 沒有橫向溢位。
