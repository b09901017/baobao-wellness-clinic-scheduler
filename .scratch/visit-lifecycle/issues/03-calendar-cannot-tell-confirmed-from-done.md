# 日曆上「已確認」和「已完成」同一個顏色，月檢視根本不分

Status: done
回報者：使用者，2026-08-20
動工前先讀：`SPEC.md` 第 8.6 節、`docs/adr/0015-calendar-is-a-first-class-surface.md`、
`docs/adr/0020-the-calendar-never-changes-pages.md`

## 症狀

她要三種顏色，一種對應流程上的一步：

| | 什麼時候 | 來訪狀態 |
|---|---|---|
| 顏色 A | 壓完表記進來，還沒問客人 | `pending_confirm` |
| 顏色 B | 客人說可以了 | `confirmed` |
| 顏色 C | 客人當天真的來了、簽了就診單 | `done` |

現在只有兩種，而且兩種是在兩個地方各寫一次的：

- **日檢視**（`ui/views/calendar.js` 的 `timerow`）：
  `r.status === 'pending_confirm' ? 'badge--soon' : 'badge--ok'`
- **週檢視**：同一個三元式又寫了一次，
  `background: ${... ? 'var(--soon)' : 'var(--accent)'}`
- **月檢視**：`visitAsBar()` 一律回 `kind: 'kind-visit'` ——
  **完全沒有狀態這回事**，取消以外的每一筆都同一條色條

所以「已確認」「已完成」「未到」在日曆上長得一模一樣。

## 想要的樣子

一個地方決定「這個狀態長什麼樣」，三種檢視都讀它。建議放
`domain/visits.js`（狀態機本來就在那裡）或另開一支 `domain/visitStatus.js`，
export 一組 `{ status → { label, cls, mark } }`，CSS 那邊一個狀態一個 class。

`mark` 是試算表用的符號（2026-08-20 使用者定的）：

| 狀態 | 日曆 | 試算表符號 |
|---|---|---|
| `pending_confirm` | 顏色 A | `○` |
| `confirmed` | 顏色 B | `△` |
| `done` | 顏色 C | `✓` |

三個畫面共用這一份：日曆（這一支）、進度追蹤頁（`issues/07`）、
試算表報表（`issues/05`）。

`no_show` 與 `cancelled` 也要有各自的樣子 —— 現在 `cancelled` 根本不畫
（`isActive()` 濾掉了，這是對的），`no_show` 混在綠色裡是錯的。

## 為什麼不能各寫各的

這一支和 `issues/07`（進度追蹤頁）講的是同一份狀態。使用者原話：

> 前面說的流程，寫程式的時候都要記得同步，這很重要

日曆說「已確認」、進度追蹤頁說「已完成」，她不會知道哪個算數 ——
這正是 `CLAUDE.md` 那張表上「同一位客戶在兩個畫面排名不同」的同一種毛病。
所以顏色與標籤只能有一份，做完在 `CLAUDE.md` 的「容易漏掉的連動」補一列。

## 一起處理

- 月檢視的色條放不下文字，光靠顏色分三種在小格子上不一定看得出來。
  可以考慮讓 `summaryByDate()` 除了現有的 `pending` 之外也回 `confirmed` / `done`，
  月格子上用一個小記號帶出「這天還有 N 筆沒問」。
- 頂端那排篩選（`ui/views/calendar.js` 的 `KINDS`）現在只分「來訪／個人行程／休假」。
  要不要多三顆「只看待確認／已確認／已完成」，做之前先問她 ——
  那一排已經有五顆，`SPEC.md` 第 8.6 節說控制項最多兩列。


## Comments

**2026-08-20 — 做完了。** `npm test` 從 586 變成 595 全過。

`domain/visits.js` 的 `STATUS_VIEW` 現在是唯一的一份，一個狀態四個欄位
（`label` / `short` / `mark` / `cls`），四種畫面全部讀它：

| 在哪裡 | 之前 | 現在 |
|---|---|---|
| 月檢視的色條 | 一律 `kind-visit`，完全不分狀態 | `statusClass(visit.status)` |
| 週檢視的小圓點 | 就地寫的三元式 | 整列掛狀態 class，圓點讀 `--kind-fg` |
| 日檢視的色棒與徽章 | 又一個三元式 | 整列掛狀態 class，兩個都從它繼承 |
| 客戶詳情的日期卡與徽章 | 自己一份 `statusClass()` | 刪掉，改用 `domain/visits.js` 的 |
| 來訪編輯器的徽章 | 又一份一模一樣的 `statusClass()` | 刪掉，同上 |
| 試算表的勾選格 | （issue 05 已經接上） | 同一份 `mark` |

**顏色**：待確認琥珀 → 已確認霧藍 → 已完成墨綠（暖 → 冷 → 綠），未到紅、取消灰。
色票做成 `tokens.css` 的 `--visit-*` 別名指到既有的語意色，
所以深色模式不必再抄一份 —— 抄一份就會有一份忘了改。淺色與深色都實際截圖看過。

**多做的三件事：**

1. **頂端加了一排圖例。** 月檢視的色條上放不下狀態兩個字，顏色是唯一的線索，
   沒有圖例她只能用猜的。它是說明不是控制項，不佔第 8.6 節那兩列控制項的額度；
   關掉「來訪」篩選時整條收起來。
2. **「來訪」那顆篩選改成中性灰。** 它本來是綠的，而綠色現在的意思是「已完成」——
   一個開關和一種狀態用同一個顏色，正下方就是圖例，會打架。
3. **加了一條 JS ↔ CSS 的守門測試。** JS 說 `status-confirmed`、CSS 只寫了
   `status-confirm`，畫面上就是「沒有顏色」，而那不會讓任何測試變紅。
   現在少一個 class、少一個 `--visit-*` 色票都會紅。（故意改壞驗證過會紅。）

**沒有做的一件事**：頂端那排篩選**沒有**多加「只看待確認／已確認／已完成」三顆。
這一支原本就寫著「做之前先問她」，而且那一排已經有五顆，
`SPEC.md` 第 8.6 節說控制項最多兩列。等她用一陣子再決定。

`CLAUDE.md` 的「容易漏掉的連動」補了一列。
