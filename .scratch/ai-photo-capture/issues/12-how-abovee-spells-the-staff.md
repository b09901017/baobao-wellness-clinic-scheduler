# 治療師主檔「Abovee 上的寫法」與服務資源的認法

Status: done
Blocked by: 11
來源：`../spec.md`
動工前先讀：`domain/masterData.js:244` 的 `validators.staff()`、`ui/views/masterList.js` 的治療師編輯器、
`domain/seed.js` 的 `staff` 與 `rooms`、`domain/visits.js` 的 `nameOf()`／`roomSlots()`、`docs/adr/0026`、`0079`、
`.local/references/images/壓表壓完的照片-2.png`（右半邊那幾欄，**有真名，只看不抄**）

## 她要的（原話）

> h Abovee 列表右邊被切掉的是哪幾欄，可以參考壓表壓完的照片-2

> 2. 可以記住就讓治療師主檔要多一格「Abovee 上的寫法」

## 為什麼會這樣

Abovee 右半邊跟 app 主檔講的是同一批東西，寫法不一樣（例子用假名）：

| Abovee 那一格 | 寫的是 | app 主檔 |
|---|---|---|
| 服務資源（復能那一列） | 治療師**全名** `陳小芳` | 治療師叫她叫的名字 `小芳`；也有 `LuLu` 這種全名推不出來的 |
| 服務資源（二返那一列） | 醫師全名 `夏OO` | 醫師只有姓 `夏`（SPEC 第 12 節） |
| 服務資源（ILIB 那一列） | 機器＋房間 `I1點10`、`I2治2` | 診間 `點滴10`（簡寫 `.10`）、`治2` |
| 服務資源（EECP） | `EECP1` | 沒有對應，房間看「診間」那一欄 |
| 診間 | `治療室5`、`點滴室10` | `治5`、`點滴10` |
| 課程 | `SIS 60`、`ILIB 60`、`EECP60`、`二返60` | 器材 SIS、課程 ILIB／EECP／二返，時長另外算 |

`validators.staff()` 現在只有 `name` 與 `role` 兩格。

## 要做什麼

### 主檔

- 治療師與醫師多一格 `aboveeNames: string[]`，編輯器上一格「Abovee 上的寫法」（頓號分隔，說明進 `?`）
- 驗證：每一個字去掉空白後不空白；**兩位不可以有同一個寫法**（不然認得出來卻是錯的人）
- 既有資料沒有這一格 = 空陣列，一筆都不用搬
- 稽核那一句（`describeParts()`）：「幫 小芳 記住 Abovee 上的寫法「陳小芳」」

### 認法（`domain/abovee.js`，純函式）

- `staffFrom(text, staff)`：`aboveeNames` 完全相同 → 那一位；否則治療師名字是全名的**結尾**、醫師的姓是全名的**開頭**，
  而且只有一位符合 → 那一位；零位或好幾位 → `null`
- `roomFrom(診間, 服務資源, rooms)`：`治療室N` → `治N`、`點滴室N` → `點滴N`、`VIP室N` → `VIPN`，再拿主檔的全名比；
  `I{n}點{N}`／`I{n}治{N}` 的房間部分走同一條
- `courseFrom(課程, master)`：`SIS 60` → 器材 SIS、60 分；`ILIB 60`／`EECP60`／`二返60` → 課程、分鐘數；
  走 `rowShape()` 認得的就用它，其餘比課程與器材的全名、別稱
- **認不出來一律 `null`**，不猜
- `aliasWrites(picks)`：她在 13 替一個認不出來的寫法選了人之後，回傳要寫進主檔的那幾筆（13 在存檔時一起寫）

## 判準

- `tests/abovee.test.js`：
  - `陳小芳` → `小芳`；主檔另有一位 `小芳芳` 時 → `null`（兩位都是結尾）
  - `aboveeNames` 裡有 `陳小芳` 的那一位 → 直接對上，不管結尾規則
  - `夏OO`，role 是醫師的 `夏` → 對上；role 是治療師的 `夏` 不算
  - `I1點10` → 點滴10；`治療室5` → 治5；`VIP室3` → VIP3
  - `SIS 60` → SIS、60；`二返60` → 二返課程；`XYZ 30` → `null`
- 兩位治療師填同一個 Abovee 寫法 → 存不下去、講得出是跟誰撞
- **這一支有沒有任何一條路，讓一個 Abovee 上的名字被認成兩位裡面的某一位、而她沒選？**

## 實作時跟上面不一樣的地方

- 判準的「`陳小芳` → `小芳`；主檔另有一位 `小芳芳` 時 → `null`（兩位都是結尾）」這個例子本身不成立：
  `陳小芳` 的結尾不是 `小芳芳`。測試改用另一位叫 `芳` 的治療師（`小芳` 與 `芳` 都是結尾 → `null`），判的是同一件事
- 名字與寫法的比法只有一份：`masterData.js` 的 `normalizeAlias()`，11 的 `identify.js` 也改成用它
- `courseFrom()` 一律帶著同名的那一台器材（`ILIB 60` → eq-ilib）：四選一那一筆要知道選的是哪一台，單買 ILIB 的用不到
