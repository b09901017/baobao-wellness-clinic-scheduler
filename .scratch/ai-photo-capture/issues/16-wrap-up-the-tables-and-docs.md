# 收尾：連動表與文件

Status: done
Blocked by: 01～15
來源：`../spec.md`、`.claude/skills/kickoff/SKILL.md`「這一輪結束前」
動工前先讀：`CLAUDE.md` 的「文件分工」與「容易漏掉的連動」、`docs/操作手冊.md`、`docs/常見問題.md`、`docs/邊界測試清單.md`、`README.md` 開發狀態表

## 她要的（原話）

> 第一 我希望UIUX 是精心設計過的，要簡潔直觀並且排版舒服有呼吸感，動畫流暢精緻

（這一支沒有新功能。它存在的理由在 CLAUDE.md：「新長出來的連動關係要回來補進這張表 ——
不補的話它的價值會隨每一輪重構衰減」。）

## 為什麼會這樣

這一輪長出好幾條新的「改了這個一定要一起檢查」，而它們散在 15 支 issue 裡。
每一支做完時照理已經補了自己那一列；這一支是**最後對一次**，而且把跨好幾支的那幾條寫完整。

## 要做什麼

### `CLAUDE.md`「容易漏掉的連動」至少補這幾列（各一行，細節指 ADR）

| 改了這個 | 一起檢查 |
|---|---|
| AI 抄字格式（`functions/transcripts/`） | `domain/transcripts.js` 那一份（測試盯著）、04 的考卷要重跑、**不可以長出 id 欄位** |
| Function 的防護或單價表 | 用量頁的「估計」說明、ADR 的價格日期；價格一律照漲價後的算 |
| 新增一個會建立時段的入口 | 已經有一列 —— 補一句「組時段走 `domain/slotDraft.js`」 |
| 照片存不存 | 照片與隱私 ADR；訂購單、Abovee、文宣**不存** |
| 認人（`domain/identify.js`） | Abovee 與療程單兩個入口共用；**對不上時不自動挑人** |
| 治療師的 `aboveeNames` | 兩位不可以同一個寫法；Abovee 匯入存檔時會寫它 |
| 療程單同一張的判斷、比對區間 | `domain/treatmentSheets.js` 一支；區間**只到最後一次簽名** |
| `storage.rules` | Rules 測試、白名單跟 `firestore.rules` 同一份 |
| `aiUsage` | 瀏覽器寫不進去（Rules）；不進備份 |

### 文件

- `docs/操作手冊.md`：三條拍照的路一步一步（客戶 → 長按 ＋；設定 → 方案範本 → 相機；壓表 → 右下角相機；設定 → 療程單）。
  **一條規則都不定義**，規則一律連回 ADR
- `docs/常見問題.md`：「我拍了為什麼說 AI 暫停中」「為什麼那一列不能勾」「為什麼右半邊的診間是空的」「為什麼 9 月之後的都沒比」
- `docs/邊界測試清單.md`：1 億像素的照片、iPad PWA 相機權限、離線按快門、左右兩張列數不一樣、上傳到一半斷線、同名兩位客戶
- `README.md` 開發狀態表打勾
- `docs/STAGING.md` 對一次：02 寫的那幾步她真的照著做完了嗎

## 判準

- 15 支 issue 的 `Status:` 都是 `done`
- 這一輪補的每一支 ADR，在 `CLAUDE.md` 那張表上都至少被一列指到
- 舊 ADR 的 diff 是空的
- 操作手冊裡沒有任何一句「因為……所以……」的規則說明
- 手動驗收清單（交付時條列給她的那一份）**每一條都真的走過**：選擇器要是那一頁上真的有的

## 實作時跟上面不一樣的地方

- **連動表補的列**：AI 的抄字格式、Function 的防護／上限／單價、`aiUsage`、一種單子的照片要不要存、認人（`identify.js`）、
  治療師的 `aboveeNames`；14、15 做的時候補了療程單與 `storage.rules` 兩列。「新增一個會建立時段的入口」那一列 10 已經補過。
  0099～0105 七支 ADR 每一支都至少被一列指到
- **比對只到最後一次簽名、只列不修寫進了 ADR-0105**（第 9、10 條）：操作手冊與常見問題要連回 ADR，而那兩條原本只寫在 15 的 issue 裡
- **常見問題多一條「存療程單時說照片傳不上去」**：正式環境第一次用、bucket 或 Storage Rules 還沒設好時也是這一句
- **邊界測試清單的 M 那一節要她自己手寫假單子拍**：送出去的照片會叫真的 AI，不可以拿真客戶的單子
- **STAGING.md 對過一次（2026-09-17，唯讀查）**：staging 的 bucket 在 US-EAST1、App Check 沒有強制、`ai-extract` 恰好兩個角色、
  Function 是 04 之後那一版（120 秒逾時）、Storage Rules 推上去了。**正式環境還一樣都沒做**，寫在上線前檢查表
