# ADR、SPEC、CONTEXT 與 CLAUDE.md 一起跟上

Status: done
PR: 1（地基）
動工前先讀：`docs/agents/domain.md`（ADR 慣例）

## 一、新 ADR：0081

`docs/adr/0081-the-slot-carries-the-lifecycle.md`

- **推翻 ADR-0025 的後半**（「前三個逐段不會不一樣」那一段），
  **不推翻它的前半** —— `slot.attended` 留著，逐段記結果那件事是對的
- 要寫進去的四件事：
  1. 為什麼「確認」與「取消」的界線不一樣（客人一次答一整天；取消是事後一段一段）
  2. `visit.status` 為什麼**照樣存**（索引、Rules、試算表、備份四個地方讀它）
  3. 舊資料零遷移的退路長什麼樣
  4. 代價：同一件事有兩個層級，可能對不起來 → 資料健檢那一列

## 二、SPEC.md

- 第 4.1 節那台狀態機：狀態改成畫在時段上，來訪是容器
- 第 4.2 節：「做了幾段就扣幾次」那一段補上「取消掉的那一段把次數還回去」
- 第 4.4 節：**把「原子單位是來訪」改寫清楚** —— 建立的動線仍然是「一天一筆、
  裡面加多段」，但**狀態與次數的原子單位是時段**。這兩句話當初混在一起了

## 三、CONTEXT.md

- **來訪**：補一句「它是那一天的容器，狀態逐段記」
- **時段**：補「自己帶狀態」
- 新詞不加 —— 這一輪沒有長出新的東西

## 四、CLAUDE.md 的「容易漏掉的連動」

新增一列：

```
| 一段來訪的狀態 | 只寫在 `domain/visits.js` 的 `slotStatusOf()`（讀）與
  `visitStatusFrom()`（推整筆）。`visit.status` 是**推導出來又存起來的**，
  改了時段一定要重推 —— 資料健檢有一列盯著。次數走 `slotOutcome()`，
  取消的那一段回 `null`（ADR-0081） |
```

順手改掉現在那幾列裡講「整筆」的字（讀取卡片那一列、長按選單那一列）。

## 五、README.md 開發狀態表打勾
