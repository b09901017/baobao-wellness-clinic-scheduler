# 文件、ADR、SPEC、README 收尾

Status: todo
來源：這一輪的連動（`CLAUDE.md` 的連動表）
Blocked by: 01, 02, 03, 04, 05
動工前先讀：`CLAUDE.md`、`docs/agents/domain.md`、`docs/adr/0062`

## 為什麼是最後一件

前面五件每一件都改了「畫面上講什麼」，而 `CLAUDE.md` 的連動表點名了
四個地方會因此對不上。一次收完比每一件各改一點好 ——
分五次改同一份 SPEC 段落，中間那幾次都是暫時錯的。

## 要改什麼

### `SPEC.md`

| 節 | 補什麼 |
|---|---|
| 8.1 「看今天做了什麼」 | 兩種看法（照人／照流程），預設照人；照人那一格「沒有掛客戶」收最後；一則都不會被丟掉這條規矩對兩種分組都成立 |
| 8.1 任務那幾頁 | 一列多一行「來訪日・課程」，等來訪讀回來才出現（同「N 項」的作法） |
| 8.5 客戶詳情的任務 | 一列講得出 `種類・來訪日・課程`；日期是**來訪那一天**不是死線 |
| 6.2 稽核紀錄 | 一則稽核那一句話要講得出「誰・哪一天・哪一項」；子集合（額度、本輪可用性）身上沒有名字，靠路徑上的 id 解析，**解析不到就不講，不編一個** |

### `docs/adr/`

**不開新的 ADR。** 這一輪沒有推翻任何既有決定：

- ADR-0062（今天是稽核的白話版）→ **補一段 Consequences**：
  分段仍然是流程順序，但它變成兩種看法的其中一種；照人那一種不新增任何資料，
  分組是同一批稽核的另一種排法。
- ADR-0043（待辦中心照流程）→ **不動**。它講的是待辦中心本身，不是回顧那一塊。
- ADR-0004、0013、0024、0056、0057、0059、0060 → **不動**，這一輪一條都沒碰到。

如果實作過程中發現非得推翻某一支，**停下來先補 ADR**，
不要改掉舊的那一支（`CLAUDE.md` 的規矩）。

### `CLAUDE.md` 的連動表

兩列要補字：

1. 「隨手記的欄位或那一列的樣子」→ 補一句：
   **包住它們的那一層（`.notemeta`）也是共用的一部分** ——
   四個入口只共用元件、不共用外框，就會變成這一輪修的那個樣子。
2. 新增一列：**「稽核或回顧上那一句話」→ 只寫在 `domain/audit.js` 的
   `describeParts()`。`views/audit.js` 與 `domain/dayReview.js` 都讀它，
   兩份寫法遲早有一份會漏掉名字或印出疊字。**

### `CONTEXT.md`

**不動。** 這一輪一個新詞都沒有：用的是既有的「來訪」「額度」「本輪可用性」
「隨手記」「待辦」「任務」「對帳」。

（順帶檢查一次：新句子裡不可以出現 `CONTEXT.md` 標 _Avoid_ 的字 ——
「預約」「行程」「備註」指日曆上那一類、「代辦事項」。）

### `README.md`

開發狀態表打勾（`CLAUDE.md`：做完一個開發步驟就打勾）。

### `public/sw.js`

`VERSION` 加一。**`SHELL` 不用改** —— 這一輪一支新檔案都沒有。
（`tests/shell-cache.test.js` 只盯清單，不盯版號，所以這一步靠人記得。）

## 不做

- 不寫新的 ADR。
- 不動 `CONTEXT.md`。
- 不改 `firestore.rules` 與 `firestore.indexes.json`
  —— 這一輪一個欄位、一個集合、一個要排序的查詢都沒有新增。

## 驗證

- `npm test` 全綠，`tests/no-secrets.test.js` 尤其
  （這一輪的 issue 與註解裡的例子一律用「客戶A」或假名「王小明」，
  **不可以有真實客戶姓名**）
- 這一輪引用她原話時出現的三個名字是**真的**，所以 `.scratch/` 裡一律換成
  「客戶A／客戶B／客戶C」。收尾前再掃一次：`git grep -nP "[\p{Han}]{2,3}(?=的來訪|的隨手記)"`
  掃出來的每一個都要是「客戶A」這種假名
