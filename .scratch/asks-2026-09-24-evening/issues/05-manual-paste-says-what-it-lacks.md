# 手動貼上那一格講它少了什麼

Status: done
Blocked by: 04
來源：`../spec.md` Q7、第四 2
PR：2（`claude/asks-0924e-sheet`）
動工前先讀：`ui/views/report.js`、`domain/sheetReport.js` 的 `customerReport()` 檔頭

## 她要的

> 7. 沒再用了，但是那邊可以有一個tooltip提醒

## 為什麼會這樣

`customerReport()` 的檔頭寫「兩條路必須長一樣」，但它從來沒有來訪紀錄；格式 5 的「記一句」、04 的「買過什麼」也都只在自動推送那條。
她不用這條了，補齊是白工 —— 但畫面上沒講，哪天拿出來用會以為資料不見了。

## 談定的做法

- 手動貼上那一塊的標題旁邊一顆 `?`：「這裡貼出來的沒有來訪紀錄、記一句、買過什麼 —— 那幾樣只有自動推送才有。」
- `customerReport()` 的檔頭改成講實話（不再說兩條路一模一樣），02 的修正照樣兩條路都吃到

## 判準

- 設定 → 試算表報表：手動那一塊有一顆 `?`，點開講得出少了哪三樣？
- 這一句**不改變寫入結果**（它是 help，不是紅線那一種）？

## 做完時留下的

- `b52b93c`：`report.js` 那一顆 `?` 多一句；`tests/report-manual-tip.test.js`。檔頭那段在 `84c44ba` 裡。
