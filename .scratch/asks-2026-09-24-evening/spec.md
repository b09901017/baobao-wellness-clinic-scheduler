# 2026-09-24 晚：拿掉多餘的 `?`、試算表四件、拍 Abovee 跟上這幾輪

基底：`develop`（`9904de8`，#129–#131 都合了）。PR 開進 `develop`。

## 她 2026-09-24 晚上丟來的（逐字）

> 第一 : 有一些tooltip是完全不必要的，不要卡在標簽之間很奇怪，而且有些根本其實不用那些說明
> 例如 : 不用說明的有 : 就直接拿掉tooltip，要檢查那些地方用到了像是(包含但不限於依客戶那邊呈現以及逾期/今天/客戶詳情那邊呈現的等等的以下abc也不要tooltip)
> a追蹤健檢報告那邊的待辦，不需要提醒"健檢做完了，去問報告出來沒" 這是多餘的話
> b簽療程單那邊寫的"有幾段到現在還是.....沒來就打叉"看不懂且沒必要，是多餘的
> c改時間/取消"2026-09-08的來訪取消了....放掉"不需要，是多餘的
>
> 第二 : 試算表的小問題 :
> a 我發現試算表路的來訪紀錄是不是就算取消了也會顯示? 如果取消了可以標註或是就不寫，也可以在這些來訪紀錄中標記狀態嗎? 像是已完成以確認取消未到等等
> b我覺得來訪紀錄那邊的排版好像會跑掉，因為第一段會接在日期後面，然後下面的不會，能不能就是第一行是日期，然後換行後在寫每一段，這樣感覺就可以對齊了
> c我發現有時候會沒有打勾二返的地方，下面註記二返()，我在猜是不是二返取消了但是這個沒消到?以及是不是其他的像是sis in il等等的也會有這個問題? 取消了但沒更新到?我猜的，請幫我排查
> d能不能有一行是顯示，app中買過什麼那邊的資訊，我也想在試算表中看到
> e可以幫我檢查一下經過pr129-131 有沒有甚麼是那邊動到的會牽連試算表這邊的，需要更新，如果沒有就說沒有不用硬找
>
> 第三 : 關於AI以及合併skill的檢查更新
> 經過pr129-131這幾輪的大改動，不知道會不會影響之前做的AI的部份以及合併skill，不知道要不要新增甚麼或是微調什麼來符合這個新版本，可以幫我全面檢查，並且也順便幫我健檢之前做的關於AI的部分，這部分似乎還沒有健檢到(還有其他地方沒健檢到嗎?)，也不知道會不會受這次更新的影響
>
> 第四 : 如果在各種全域排查或是修正的過程中有發現什麼我沒發現的關聯問題，或是相同類似的問題，也可以提出來，問我要不要修正，然後記得修正優化的時候要注意會不會牽連或是連累產生衍生問題，這個要多注意 !

## 第一段查到的（重現過的用 app 自己的函式跑過）

- **tooltip**：a、c 是同一個東西 —— 系統自己寫在任務上的那一句（`autoGenerated` 的 `note`），
  9/24 下午（`asks-2026-09-24/issues/09`）從常駐字收進 `?`，沒有拿掉。b 是簽療程單那張卡的 `?`。
- **試算表 c（重現）**：二返註記挑「哪一欄是健檢」用的是 `usedOn()`／`examOn()`（那一天有沒有用那筆額度），
  不看那一段的狀態 —— PR #130 的 06（ADR-0112）只改了「二返佔不佔」那一半。健檢那一段取消了、同一天 SIS 做了，
  健檢那一格空的、底下照印 `二返()`。器材那一列（`equipmentCells()`）也不濾取消的段：SIS 取消、INDIBA 做了印「SIS、IND」。
  「記一句」那一列（`slotNoteCells()`）本來就有濾。
- **試算表 a、b**：`log` 列取消的段、沒有狀態；`.gs` 的 `renderNotes()` 把第一段接在日期後面。
- **試算表 e**：#129–#131 牽連到試算表的**只有 c 那一條**。
- **合併 skill**：不用改。它 import 的模組 #129–#131 沒動；app 那側匯入走 `importedTasksFor()`，已經跟上 ADR-0107／0112／0113。
- **拍 Abovee**：存檔確認框沒跟上 #129 的 22（「等客人說可以之後會再多一張 X」）與補登過去那一天；
  app 上取消掉的時段在 Abovee 上還在時被當成「新的」、未來的預設打勾（`existingAt()` 只找活著的段）。
- **沒健檢到的**（這一輪不做，列給她）：正式站 deploy 從 9/17（#127）紅在 Storage Rules（CI 正式服務帳號缺
  Service Usage Consumer）；staging 療程單存照片 403（9/18）沒有紀錄說解決了 → 療程單存放＋比對沒端對端走過；
  9/17 審查留下的七件她還沒決定；邊界測試清單 M 一條都沒走、C4／C5 被 9/24 改過要重走、A／C3／D／E／F／H／C10–C12 沒走過。

## 她 2026-09-24 晚上回的（逐字）

> 1清單 B 全部拿掉可以，但是C先不用，目前感覺沒有影響版面
> 2. 不寫
> 3. 好，照你的建議 : 跟舊表和 app 的「約二返」同一個時機
> 4. 可以
> 5. 好，照你的建議，但其實拍 Abovee上面也有標已取消等等的標記(可以參考.local/refences/images/壓表壓完的照片.png)，其實也可以當作某一方面的交叉驗證?
> 6. 好
> 7. 沒再用了，但是那邊可以有一個tooltip提醒
> 8. 正式站先等全部健檢完後在說沒關係
> 9. 可以，照你建議的就可以
> 10. 都同意
> 11. 都不用逐支看，你可以一次做完

問題對照：1＝清單 B 拿不拿、C 要不要過；2＝來訪紀錄取消的段；3＝`二返()` 印在哪幾欄；4＝「買過什麼」一天一行放營養品上面；
5＝Abovee 碰到 app 已取消的段；6＝「整天退回待確認」改說法；7＝手動貼上那條路；8＝正式站權限；9＝分批；10＝策略；11＝逐支看。

Abovee 上的預約狀態（她給的截圖，只取欄位寫法，**截圖在 `.local/`、有真名，一個字都不抄**）：`課程完成`、`確認前往`、`已取消`。

## 三支 PR（平行，都從 `develop` 開）

| PR | 分支 | issue | `sw.js` |
|---|---|---|---|
| 1 | `claude/asks-0924e-tips` | 01（＋這個資料夾、README 那一列） | v140 |
| 2 | `claude/asks-0924e-sheet` | 02、03、04、05 | v141 |
| 3 | `claude/asks-0924e-abovee` | 06、07、08 | v142 |

**這個 `.scratch/` 資料夾只放在 PR 1 的分支上**（另外兩支一個檔都不帶，不然合併時 add/add 衝突，
見記憶 parallel-prs-scratch-and-sw-version）。三支的 `Status:` 都在 PR 1 的分支上改。
`sw.js` 三支各用不同號；合併時那一行衝突留數字大的。

PR 2 的 02–05 **一定要一起出**：`SYNC_FORMAT` 5 → 6 只升一次，她只要回試算表重貼一次 `.gs`。

| # | 什麼 | 來源 | PR | Blocked by |
|---|---|---|---|---|
| 01 | 系統寫的待辦說明不畫、清單 B 的 `?` 拿掉 | 第一、Q1 | 1 | |
| 02 | 試算表：二返註記只在健檢 ✓ 那一欄、器材那一列不印取消的段 | 二c、二e、Q3 | 2 | |
| 03 | 試算表：來訪紀錄不寫取消的段、每一段標狀態、日期自己一行 | 二a、二b、Q2 | 2 | |
| 04 | 試算表：「買過什麼」一天一行 | 二d、Q4 | 2 | 03（同一次升版） |
| 05 | 手動貼上那一格講它少了什麼 | Q7 | 2 | 04 |
| 06 | 併進已確認那一天：原本談定的段不動，只有新的這一段待確認 | 第四 1、Q6 | 3 | |
| 07 | 拍 Abovee 的確認框走 `consequences.js`，跟其他入口講一樣的話 | 第三、Q10 | 3 | 06 |
| 08 | 拍 Abovee：預約狀態跟 app 對一次（只講不改） | 第三、Q5 | 3 | |

## 新 ADR（舊的一個字不改）

- **0114** 系統寫的說明、跟標題講同一件事的 `?` 直接不畫（01）—— 推翻 ADR-0072「一句都不會真的消失」那個前提的一部分
- **0115** 試算表也印「買過什麼」（04）—— ADR-0090 說第四種名字「只住在兩個地方」，試算表是第三個
- **0116** 拍 Abovee 拿預約狀態跟 app 對一次，只講不改（08）—— 延伸 ADR-0104 第 3 點（只照原字印）與 ADR-0056

## 這一輪不做（留給之後）

- 正式站：她說「先等全部健檢完後在說」。合 main 之前一定要先補 CI 正式服務帳號的 Service Usage Consumer
- staging 療程單存照片 403：修好之前 M 那一段與療程單比對走不了
- 9/17 審查留下的七件（Abovee「打名字找」、照片 >1.5MB、療程單新版品項蓋成空、訂購單「拆開」丟改動、孤兒照片、觸控區）
- 清單 C 的 `?`（她：「C先不用，目前感覺沒有影響版面」）

## 出貨（2026-09-24 晚）

三支都從 `develop`（`9904de8`）開，01–08 全部 done。每一支各自跑過 `npm run verify` 那一份（相關的 E2E 從**那一支自己的 worktree** 起模擬器跑）：
PR 1 32 支 260 條、PR 2 4 支 25 條、PR 3 17 支 —— 新寫的 E2E（29 S2c／S2d、41 A6）都先在舊的 code 上跑過一次確認是紅的。

- PR 1 `claude/asks-0924e-tips`：01 ＋ 這個資料夾 ＋ README 第 32 列。sw.js v140
- PR 2 `claude/asks-0924e-sheet`：02；03＋04（同一個 commit，同一次升版）；05。sw.js v141。**合進去之後她要重貼 `.gs`**
- PR 3 `claude/asks-0924e-abovee`：06、07、08。sw.js v142

三支合併時 `sw.js` 那一行會衝突，留數字大的那一個（v142）。CLAUDE.md 三支各改不同的列。
