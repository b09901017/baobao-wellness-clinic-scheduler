# 上線前整體健檢的修正（2026-09-23）

來源：2026-09-23 那一輪「上線前整體大健檢」（只讀不改）。**每一條都重現過一次**——
純邏輯的直接跑 app 自己的 domain 函式，畫面上的用本機模擬器＋Playwright 在真的 app 上點。
重現腳本當時放在 session 的暫存區，**沒有進 repo**；每一支 issue 的「怎麼重現」寫到
足夠照著寫出一支失敗測試（CLAUDE.md：修 bug 先寫一支重現得出來的失敗測試）。

基底：`develop`。PR 開進 `develop`，不是 `main`。

## 這一輪要做什麼

| | 題目 | 嚴重度 | 動到 domain 規則？ |
|---|---|---|---|
| 01 | 復原過的、系統收掉的任務被當成「做過了」 | 🔴 | 是 |
| 02 | 同一天取消一段再補排，新那一段不長掛號 | 🔴 | 是（要她確認一句） |
| 03 | 確認抽屜一次存好幾天，次數快取拿舊清單算 | 🔴 | 否 |
| 04 | 拿畫面上的舊來訪去改，會蓋掉別台剛存的 | 🔴 | 否 |
| 05 | 試算表推送在路上時又寫一筆，那一筆永遠推不上去 | 🔴 | 否 |
| 06 | 同名客戶在試算表上互相蓋掉 | 🔴 | 否（可能動 `SYNC_FORMAT`） |
| 07 | 清掉已完成的「約二返」後，它會長回來而且逾期 | 🟠 | 是 |
| 08 | 刪掉客戶後，他未來的來訪與待辦都還在 | 🟠 | 是（要她選） |
| 09 | 客戶改名字，日曆與待辦還是舊名字 | 🟠 | 否（要她選） |
| 10 | 按「復原」之後，開著的那一層還是舊資料 | 🟠 | 否 |
| 11 | 待辦分類頁的「已完成」分頁會黏住 | 🟠 | 否 |
| 12 | 確認抽屜的「全部退回未確認」其實是取消 | 🟠 | 否 |
| 13 | 「改這一段」改了已談定那一段的時間／課程，要提醒回去改 | 🟠 | 是（她 9/23 定了方向） |
| 14 | 試算表只看前後 400 天的來訪 | 🟡 | 否 —— **暫緩** |

## 建議的順序

1. **01 → 07**：同一個病（「被刪掉的任務算不算做過」），01 定下的規則 07 直接照用
2. **02 → 13**：13 最乾淨的做法是「改期＝取消＋重新排」，而那條路要 02 先讓掛號逐段長
3. **03、04、10**：都是「拿了舊的那一份去寫／去畫」，04 定下的防線 03 可以一起用
4. **05、06**：試算表兩條，06 可能要升 `SYNC_FORMAT`（她要回試算表重貼 `.gs`）
5. **12、11**：文案與一行重設，最小
6. **09、08**：要她先選方向，選完才動

## 她定的那幾件（2026-09-23）

逐字記在這裡，是底下幾支 issue 的前提。

- **13（改這一段改時間）**：「我希望會提醒回 Abovee／Examine／耀聖改時間以及回去取消已經掛好的號等等」
- **14（試算表 400 天）**：「這個暫時沒關係，但是可以在試算表標註起來或是用 tooltip 標註等等，暫時先沒關係」
  → issue 14 開著但 `Status: 暫緩`，**這一輪不做**
- **不修、以後也不用再報的兩件**：「刪除線讓『二』看起來像『三』、微調面板兩位數只顯示一個字 這兩個我完全不介意」
  （出處：`prod-manual-testing` 那一輪的 `.slotrow--no` 刪除線、`planTweak.js` 的 `.stepper__n` 裁字）

## 還沒定案、這一輪刻意不碰

- `slot-confirm-and-durations-2026-09-16/issues/11`：鎖定還是整天一起鎖（她那一輪說先不做）
- 壓表批次裡已刪除客戶的卡片（`boundary-checklist` 那一輪記過；08 做完之後再看還剩多少）
- `coverage-gaps/issues/04` 觸控區貼錯盒子、`pii-in-repo/issues/01` git 歷史裡的真名

## 這一輪看過、沒有問題的（不必再查）

- 一次存檔的來訪、次數、任務在同一個 commit；刪除全部是軟刪除＋稽核
- 「今天」「完成那天」都用裝置當地日期（`dates.js` 的 `todayISO()`／`dayOf()`），沒有早上 8 點前差一天
- 連點防護（`withSaveState` 的 `key`）、離線不說失敗
- `firestore.rules`：外人只寫得進自己那一份 `formResponses`

---

# 第二輪：PR #129 審查之後（2026-09-23 晚上）

來源：同一天用 `/matt-code-review` 審 PR #129（01～13）找到的問題，加上審查時新看到的一件（21）。
**全部接在同一個分支 `claude/prelaunch-audit-2026-09-23`、進 PR #129 一起合**（不另開、不疊 PR）。
可以一次做完，不用逐支停下來問。

## 這一輪要做什麼

| | 題目 | 來自 | 動到 domain 規則？ |
|---|---|---|---|
| 15 | 種子資料與改期確認框都問 `newRegistrations()` | 審查 1 | 否 |
| 16 | 改名：還掛著沒做完的事的那幾筆來訪，不管日期一起換 | 審查 2（09） | 是（她定了） |
| 17 | 刪客戶：還沒勾的隨手記也擋；「去批次取消」跳對月份 | 審查 3、G（08） | 是（她定了） |
| 18 | 存好幾筆存到一半失敗，按重試不會被自己擋 | D（03、04） | 否 |
| 19 | 日曆長按、簽療程單、確認抽屜、壓表：拿剛讀回來的那一份去套 | E（04 第一層） | 否 |
| 20 | 日曆編輯器按取消，重開的那一天抽屜是新的資料 | F（10） | 否 |
| 21 | 待辦那一列印出它掛的是哪一段 | K（新）＋ H（她定了維持兩張） | 否（畫面） |
| 22 | 新增時的確認框只講這次新加的那幾段會長什麼 | I | 否 |
| 23 | 改名時療程單那一頁跟著變，表單邀請不變 | J 延伸 | 否 |
| 24 | 文件、`related.js` 登記、審查點到的小整理 | J | 否 |

## 她這一輪的回答（逐字）

- 審查第 1 點（舊的掛號待辦沒有 `slotIndexes`）：「好像沒關係，畢竟現在都沒有真實資料上線，之後還會重新用skill merge一次，
  所以只要合併的skill有顧慮到這點就好」
  → 查過：合併檔**不帶任務**，任務是 app 匯入時由 `importedTasksFor()` 長的，而它走 `newRegistrations()`，
  每一張都帶 `slotIndexes`。**merge skill 不用改。** 剩下的是 staging 的種子與確認框那一句（15）
- 16：「還掛著沒做完的事」的那幾筆，不管日期一起換。那幾筆還不算歷史；已經結案、沒有待辦的，照樣留著當時的名字
- 17：這位客戶身上所有還沒勾的都擋，不管有沒有日期
- H：維持兩張。一次確認就是一趟去 Examine，勾掉的意思很清楚。配合下面的 K
- 21（K）：照建議 —— 帶 `slotIndexes` 的那幾張，只印那幾段的時間與課程
- 23（J 延伸）：照建議 —— 療程單那一頁改讀客戶本人的名字；客人填時間的表單邀請不換
- 出貨：全部接在同一個分支、進 PR #129 一起合

## 順序

19 → 18（同一段迴圈，先換成新的那一份再加「跳過已存」）；15 → 22（共用「這幾段談定了會長什麼」那一支）；
16、23 都是改名；24 最後（收各支的文件與連動表）。其餘互不相依。

## 這一輪刻意不做

- **`rebookSlot()` 不要改走 `applyStatus()`**（審查的 Standards 軸建議過）。實跑過：一天只有一段時，
  `applyStatus(…, 'cancelled', {slotIndex})` 會先把整筆推成「已取消」，接上來的新那一段跟著變成死的。
  現在直接標 `status: 'cancelled'` 再 `withExtraSlot()` 才是對的 —— 24 補一支測試把它釘住
- 舊掛號待辦（沒有 `slotIndexes`）的退路（`cancelSlotsOf()` 當成整天）**留著不動**：正式資料會重新匯入，
  匯入長出來的都帶 `slotIndexes`
- 14（試算表 400 天）照舊暫緩

## 出貨

- `public/sw.js` 的 `VERSION` 在這個 PR 已經是 v137、還沒上過 staging —— **不用再升**，
  除非這段期間 develop 升過（那就挑一個比兩邊都大的號）
- PR #129 的內文要補第二輪的手動驗收清單（寫的人要真的走過那條路）
