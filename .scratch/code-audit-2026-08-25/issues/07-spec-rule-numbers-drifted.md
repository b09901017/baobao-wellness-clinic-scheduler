# SPEC 第 7 節規則編號四處對不上

Status: done
來源：全庫掃描，2026-08-25（`../spec.md`）

## 症狀

`SPEC.md` 第 7 節 2026-08-23 那一輪多了規則 9「療程單」，後面兩條各往後推一格：

| 現在 | 是什麼 |
|---|---|
| 9 | 療程單 |
| 10 | 取消即釋出（**改期 = 取消 + 重新排**） |
| 11 | 壓表前確認（**已在 Abovee 確認診間無人使用？**） |

程式碼裡四處引用的還是舊編號：

- `domain/visits.js:119` 改期 → 寫「規則 9」，應為 10
- `ui/views/visitEditor.js:576` 改期 → 「規則 9」，應為 10
- `ui/views/visitEditor.js:525` 壓表前確認 → 「規則 10」，應為 11
- `ui/views/schedule.js:1436` 壓表前確認 → 「規則 10」，應為 11

（`domain/masterData.js:145` 的「規則 9」指療程單，是對的。）

不影響行為，但下一個人照著編號回去查 SPEC 會查到別條規則 ——
而這一份專案的整個做法就是「照著註解回去讀 SPEC」。

## 做了什麼

四處改成正確的編號。

## 驗證

`grep -rn "規則 [0-9]\+" public/js` 逐條對照 `SPEC.md` 第 7 節。
