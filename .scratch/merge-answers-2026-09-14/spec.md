# 2026-09-14 那一批舊表 × 行事曆：她回報之後要改的東西

她 2026-09-14 丟來新的 `all_client.xlsx`（28 位）與 `timetree.ics`（467 筆），跑 skill 之後產了一份 93 題的問卷，
她 2026-09-15 全部答完。**客戶真名與逐位的決定只在 `.local/references/`**（`合併回報-2026-09-14.json`、
`合併回報紀錄.md`、`merge-decisions.json`），這裡只寫規則。

## 她講的規則（去識別化）

| 題 | 她的答案 |
|---|---|
| 年份 | 這一批全部 2026 |
| 行事曆的 `.N` | **就是點滴 N**（`.5雪` = 點滴5、`.8（躺）` = 點滴8）。推翻 8/27 那一條「裸數字不認」 |
| 一個字的點滴品項 | 雪＝雪顏亮彩、肝＝護肝排毒、腸＝腸道修復 |
| `13健檢` | 「付 1 萬換 3 萬的健檢」，名字就叫 13健檢。不是 13 萬 |
| X光 | **是復健科門診的一部分**（看門診前先照，要不要照看醫師）。不是一段療程，不可以拿它的時間補別的課程 |
| 取消／預約／改／紀錄／約 | 一律不是來訪 |
| 超過 40 字的備註 | 照句號、驚嘆號、分號切 |
| 沒有分頁的人 | 同事請假請她帶的，**當行事備註** |
| 待辦的字 | 回電、告知、給、退款、影本、更新、排、包、澆水 都當待辦 |
| 物理治療師 | 多一位；另外幾種寫法是現有治療師的簡寫 |
| 行事曆上的「賴」 | 不是人，是提醒自己要加客人的 LINE |
| 舊表的 `SIS(60min)` | 單買一台 SIS（60） |
| `ILIB (60mins)`、`ILIB 30` | ILIB，時長照括號 |
| `復能(30min)` 配 B2「復能四選一(30)」 | 四選一（30） |
| `任選(30min)` | **還沒定**，之後個別問 |
| 同一天同一位客戶的復健門診 D 欄是 0 卻勾了 | 送的，照勾的次數建額度，備註寫「送的」 |
| 心臟門診被標成「～」 | 送的（買 EECP 之前一定要看心臟門診） |
| 行事曆有、舊表沒勾（沒有特別理由時） | **舊表沒勾就是沒做**；她：「理論上我試算表上有勾的就是有」 |

## 要改的程式（一支 issue 一件事）

- `01` 舊表的新寫法：B2 兩種讀漏、八種新列名、一個字的品項、日期欄裡被丟掉的字、長備註、警示與合作機構
- `02` 合併檔 v3：時長、警示、合作機構；app 收只有一台的擇一池
- `03` 行事曆比對：`.N`、只寫姓的事件、X光、取消類句子、待辦的字、`13健檢`；以及**決定檔**（她逐筆回答過的，下一批自動套用）

## 保留的問題

她標了「保留」的題目與還沒定的那幾件，另外做成一份問卷給她（在 `.local/references/`），
每一題都寫著**這次先照什麼做**。
