# 有六位真實客戶的姓名已經被 commit 進 repo 了

Status: open —— 第 1 件（換假名 + 測試）做完了，第 2 件（洗 git 歷史）等使用者決定
發現者：Claude，2026-08-20（比對她給的 `.xlsx` 與 repo 內容時掃到的）
動工前先讀：`SPEC.md` 第 10 節「資安」、`CLAUDE.md` 的文件分工那一段

## 症狀

`SPEC.md` 第 10 節寫著：

> **絕對不能 commit 的**：…任何含**真實客戶資料**的匯出檔、備份 JSON、截圖
>
> 一旦真實客戶資料被 commit 過，就算之後刪掉，**它仍然留在 git 歷史裡**。

`CLAUDE.md` 也寫著「`docs/legacy/` 與 `docs/adr/` 不放任何真實客戶姓名或健康資訊」。

但拿 2026-08-20 那份 `.xlsx` 的 21 位客戶姓名去掃被 git 追蹤的檔案，
**六位對得上，散在六個檔案裡**：

| 檔案 | 幾位 | 以什麼形式 |
|---|---|---|
| `.claude/skills/calendar-sheet-merge/SKILL.md` | 1 | 姓名比對的例子 |
| `.claude/skills/calendar-sheet-merge/references/findings.md` | 1 | 「照抄的話客戶會叫 ⋯⋯」的例子 |
| `.claude/skills/calendar-sheet-merge/references/shorthand.md` | 3 | 行事曆簡寫對照表的例子 |
| `.claude/skills/calendar-sheet-merge/scripts/merge.mjs` | 3 | 程式註解裡的例子 |
| `.scratch/followup-visit/issues/01-…` | 1 | 引用行事曆原文的例子 |
| `public/js/domain/legacyImport.js` | 1 | 註解裡的解析範例，**還帶著器材偏好** |

全部都是**當成範例寫進註解與文件**的，不是資料檔 —— 寫的人是為了把規則講清楚。
但 `docs/legacy/README.md` 同樣在講規則，那裡從第一天就一律寫「客戶A、客戶B」，
可見這條線本來就畫得出來。

**這不是這一輪動到的東西，是既有狀況。** 記下來是因為它違反的是專案自己的規矩，
而且時間拖越久、歷史越長，處理起來越麻煩。

## 想要的樣子

兩件事分開看，難度差很多：

### 1. 把工作區裡的名字換成匿名（容易，值得馬上做）

六個檔案的姓名換成 `客戶A`、`客戶B`，語意一個都不會掉 ——
那些例子講的是「去姓」「異體字」「分頁名和 A2 對不起來」這些**規則**，
規則不需要真名。`legacyImport.js` 那一筆還要順手把器材偏好一起改掉。

改完之後補一個測試，讓它掃不到真名再回來 —— 但**測試裡不能寫真名**
（否則就是又 commit 了一次）。作法可以是掃「中文姓名 + 三到四位數字」這種形狀，
或是讓她把名單放在一個 gitignore 的檔案裡由測試選讀。這一段要想清楚再動手，
`tests/no-secrets.test.js` 是現成的骨架（它掃的是私鑰特徵，同一個模式）。

### 2. 洗掉 git 歷史（麻煩，要她決定）

改工作區**不會**讓名字從歷史裡消失。真的要清掉得 `git filter-repo` 之類的東西
重寫歷史並強推，而那會讓所有既有的 clone 失效、PR 對不上。

**要不要做是她的決定，不是技術決定。** 判斷的依據是：

- repo 是 **private** 的，看得到的人目前只有她自己
- 洩漏的是**姓名**，沒有病歷、電話、地址；只有 `legacyImport.js` 那一筆
  把姓名和器材偏好放在一起
- 對照之下，真正會出事的是服務帳號金鑰，而那個有 `tests/no-secrets.test.js` 守著

我的建議是**先做第 1 件、把第 2 件當成「知道有這回事」**，
除非哪天這個 repo 要開放給同事或轉成公開 —— 那時第 2 件就變成必須做。

## 不要順手做的事

- 不要為了清歷史就去改別人已經合併的 commit，先問她。
- 不要在修的時候順便把那些例子刪掉。**例子要留著**，只是換成假名 ——
  那幾條規則（去姓、異體字、分頁名打錯）是真的踩過的坑，講清楚比乾淨重要。


## Comments

**2026-08-20 — 第 1 件做完了。** `npm test` 從 696 變成 698（697 過、1 跳過，見下）。

### 實際上是七處，不是六處

原本這一支表格上寫的是六個檔案六位。動工前拿姓氏開頭掃過全 repo，
`merge.mjs:309` 還有一處沒被點名（`8.15{真名}13`，只寫名字沒寫療程的例子）。
所以是**六個檔案、七處真名**，全部換掉了。

| 檔案 | 換了什麼 |
|---|---|
| `.claude/skills/calendar-sheet-merge/SKILL.md` | 去姓的例子 |
| `.../references/findings.md` | A2 沒清乾淨會變成什麼名字 |
| `.../references/shorthand.md` | 「名字」那張表三處（去姓、只寫中間一個字、分頁名打錯） |
| `.../scripts/merge.mjs` | 去姓的註解、`displayName()` 的註解、只寫名字沒寫療程的例子 |
| `.scratch/followup-visit/issues/01` | 行事曆上兩筆二返的原文 |
| `public/js/domain/legacyImport.js` | `nameExtras()` 的解析範例 |

### 假名的挑法：規則需要字數時才用假名，其餘照舊寫「客戶A」

這一支原本說「換成 `客戶A`、`客戶B`，語意一個都不會掉」。大部分成立，但
**有三條規則講的就是名字的字數與結構**，寫「客戶A」看不出來：

- 去姓（四個字、複姓兩個字 → 只寫後兩個字）
- 只寫中間一個字（三個字 → 中間那個）
- 分頁名打錯（兩個名字只差最後一個字）

所以那幾處改用**明顯虛構的假名**：`王陳小明`、`王小明`、`王小名`。
`王小明` 是中文的 John Doe，而且這個 repo 本來就在用它（ADR-0024、`issues/07`）。
`shorthand.md` 的那一段開頭加了一句「這幾個都是虛構的」，免得下一個人以為它是真的。

### 器材偏好沒有跟著改，理由寫在這裡

這一支原本寫著「`legacyImport.js` 那一筆還要順手把器材偏好一起改掉」。
實際看過之後**只換了名字，`(高能/sis)1234` 原樣留著**：

那個字串已經以匿名形式存在於 `docs/legacy/README.md:234`、
`.scratch/legacy-import/issues/01:27` 與 `tests/legacy-import.test.js:399`
三個地方（都接在「名字」或「客戶A」後面），是 repo 早就採用的例子。
**讓它變成個資的是那個名字，不是那串簡寫** —— 名字換掉之後，
`高能/sis` 就只是一個沒有主人的器材簡寫，而它本來就公開寫在 `shorthand.md` 的對照表上。
只改這一處會讓同一個例子在四個地方長得不一樣，換不到任何隱私。

`4321`（另一個病歷編號）順手換成 repo 既有的例子 `1234`，所以它也不在工作區了。

### 測試：兩條，互補，都不含真名

**① 形狀檢查（不需要名單，CI 上就跑得到）。** 掃「中文字直接黏著 2–4 位數字」，
也就是舊表 A2 的 `名字1234`。

**先試過「像不像人名」那條路，不可行**：拿一百多個姓氏開頭掃全 repo，
誤判的是「一**張**卡」「任**何**東西」「**高**能量雷射」「**顏**色」「一**方**面」
這種一般詞彙，數量遠多過真名 —— 那種測試紅個兩次就會被關掉，等於沒有。
黏著數字這個形狀掃出來全 repo 只有五種詞（`名字`、`點滴`、`點滴室`、`顧客會`、
以及新加的假名），全部進白名單，所以它紅的時候幾乎一定是真的。

**② 別名表交叉比對（有名單的機器上才驗得到）。** 讀 `.local/aliases.json`
（gitignore）的 `nicknames`，全名與行事曆上的叫法都拿去掃。檔案不在就 `t.skip()`
並講清楚為什麼 —— 所以在這個容器裡它是「1 skipped」。
只寫一個字的別名不掃（單字在中文裡到處都是）。命中時**只印檔名與命中幾個，
不印是哪個名字** —— 那行字會進 CI 的 log。

**測試本身一個真名都不能寫**，否則就是為了防止 commit 真名而 commit 一次真名。

兩條都故意改壞驗證過會紅：塞一個「姓名緊接著四位數字」的字串進 `dates.js`，形狀那條抓到並印出
檔名行號；放一份 `nicknames` 指向 `騰崴` 的 `.local/aliases.json`，第二條抓到 `SPEC.md`。

### 一起改的

- `CLAUDE.md`：原本只寫「`docs/legacy/` 與 `docs/adr/` 不放真名」，範圍太窄 ——
  這次漏出去的正好在 `.claude/`、`.scratch/` 與 `public/js/`。改成「任何進版控的檔案」。
- `SPEC.md` 第 10 節的「絕對不能 commit 的」多一條，並指到這支測試。

### 掃過但**沒有**動的

- `.scratch/ui-redesign/canvas/*.dc.html` 的 `陳先生`、`黃太太`、`林小姐` ——
  那是設計稿裡的示範資料，姓＋稱謂認不出任何人（等同於 mockup 上的 John D.）。
- `tests/progress.test.js` 的 `陳一`、`林二` —— 測試假資料。
- `config/staff` 的治療師與醫師（騰崴、芝寧、夏、許、李…）—— 他們是同事不是客戶，
  而且 `SPEC.md` 第 12 節本來就當種子資料列著。這一條的界線是**客戶**。

### 第 2 件（洗 git 歷史）沒有做

**這是使用者的決定，不是技術決定**，判斷依據原文寫在上面。工作區乾淨了，
但那七處真名**仍然留在 git 歷史裡**。要真的清掉得 `git filter-repo` 重寫歷史並強推，
代價是所有既有的 clone 失效、開著的 PR 對不上。

建議維持原判斷：repo 是 private、洩漏的是姓名沒有病歷內容，先當成「知道有這回事」。
**哪天這個 repo 要開放給同事或轉成公開，第 2 件就變成必須做。**
