# 候選清單照「來源」分，不照時間分；`ambiguous` 沒有任何地方看得到

Status: done
回報者：使用者，2026-08-21
動工前先讀：`ui/views/mergeImport.js` 的 `candidateCards()`、
`domain/mergeImport.js` 的 `validateFile()` 與 `eventDocs()`

## 症狀

她的原話：

> 日曆那邊，匯入的時候希望能區分兩個區塊，一個是以前一個是未來，讓我可以自己勾選要不要匯入
> （目前有勾選匯入，但是我要一個一個看日期，希望直接分成兩個區塊，一樣勾選）

## 為什麼

`candidateCards()` 現在的三張卡是照**這筆資料哪裡來的**分的：

| 卡片 | 來源 | 她那份檔案裡有幾筆 |
|---|---|---|
| 行事曆上有，試算表沒勾 | `missingFromSheet` | 27 |
| 未來的預約 | `futureVisits` | **1** |
| 對不到客戶的行事曆事件 | `eventCandidates` | **198** |

問題有三個：

1. **198 筆混在一起，過去和未來交錯排列。** 她要一筆一筆看日期才知道哪些還沒發生。
2. **「未來的預約」那張卡名字騙人。** 它只有 1 筆，但真正在未來的來訪有 11 筆 ——
   另外 10 筆藏在 `customers[].visits` 裡（見 `issues/03`）。
   一張叫「未來的預約」的卡只列出十一分之一，比沒有這張卡還糟。
3. **`customers[].visits` 那 67 筆根本沒有勾選介面**，是直接寫進去的。
   她說「一樣勾選」，指的很可能包含這一塊。

## 另一件沒人看得到的事

合併檔裡有 `ambiguous` —— 同一筆行事曆事件配得上兩位客戶，skill 不敢猜，
所以列出來讓她判斷。她那份裡有 1 筆（6/30 的靜脈，兩位客戶都可能）。

**`ambiguous` 在整個 `public/js/` 底下出現 0 次。** `validateFile()` 不看它，
`candidateCards()` 不列它，摘要卡不數它。skill 那側花力氣不猜，
結果 app 這側連提都沒提 —— 那一筆來訪就這樣消失了，兩邊都沒有錯，但東西不見了。

## 想要的樣子

### 候選清單改成先分時間，再分來源

```
▸ 還沒發生的（N 筆）           ← 預設全部勾起來
    來訪（會建成「已確認」，會長出登記待辦）
    個人行程
▸ 已經發生的（N 筆）           ← 預設一筆都不勾
    行事曆上有、試算表沒勾的來訪
    個人行程
```

兩個區塊各自一顆「全選 / 全不選」。

### 預設值要反過來（2026-08-21 使用者拍板）

**未來的預設勾起來、已經發生的預設不勾。** 她 2026-08-21 直接確認了這一條。

現在是「三份清單一律 `include: false`，她說要自己一筆一筆決定」。
那條規則對**過去**的資料是對的（補歷史要謹慎）。

但她 2026-08-21 說的是：

> 未來的就算不是客人相關也都要匯入，因為是現在紀錄的

**未來的東西預設勾起來。** 那些是她現在正在管理的事，不是考古。
她還是可以取消勾選，只是不用勾 198 次。

### `ambiguous` 要有地方看

不做成可勾選的（勾了也不知道要算給誰）。做成一張**唯讀的提醒卡**：
「有 N 筆行事曆事件對得上兩位以上的客戶，沒有匯入。匯完到日曆上自己補。」
列出日期、原文、可能是哪幾位。

`validateFile()` 也該把它算進 warnings，理由和現在那條
「有 N 筆候選的客戶名字不在這份檔案裡」一樣。

## 不要順手做的事

- **不要把「未來 / 過去」的界線寫進檔案。** 理由見 `issues/03` ——
  界線在匯入的那一刻。UI 這一層用 `todayISO()` 分組就好。
- **不要因為 `ambiguous` 沒地方放就讓 skill 自己挑一個。**
  不猜是對的，缺的是轉述。


## Comments

**2026-08-21 — 做完了。** `npm test` 從 715 變成 722 全過。

候選清單改成兩張卡：**還沒發生的**、**已經發生的**，每一張底下再分「來訪」與
「個人行程」兩段，各自一顆「全選 / 全不選」。分組與預設值都在
`domain/mergeImport.js`（`groupCandidates()` / `defaultPicks()`），UI 只負責畫 ——
「哪些算還沒發生」是判斷，判斷不寫在 UI 事件處理器裡。

| 在哪裡 | 做什麼 |
|---|---|
| `domain/mergeImport.js` | `groupCandidates(json, today)`：三份清單攤平成 `{ kind, index, date, item }`，依日期分兩區。`index` 記的是原本清單裡的位置，所以勾選狀態原封不動還是那三個 Set |
| 同上 | `defaultPicks(json, today)`：還沒發生的全選、已經發生的不選 |
| 同上 | `validateFile()` 把 `ambiguous` 算進 warnings |
| `ui/views/mergeImport.js` | `candidateCards()` 整段重寫；勾選預設值改從 `defaultPicks()` 來；多一組 `[data-all]` 的全選 / 全不選 |
| `docs/adr/0030-*` | 預設值反轉的理由 |
| `tests/merge-import.test.js` | 七條 |

### 排序

「離今天多遠」：**還沒發生的近的在前，已經發生的最近做的在前。**
兩邊都用日期升冪的話，198 筆裡最近做的那幾筆會沉到最底下。

### `ambiguous` 做成唯讀卡

「這 N 筆兩位客戶都可能」，列日期、原文、療程、可能是哪幾位，**沒有勾選框** ——
勾了也不知道要算給誰。同一件事在 `validateFile()` 也講一次，理由跟現有那條
「有 N 筆候選的客戶名字不在這份檔案裡」一樣：檔案剛貼進來的那一刻就要知道。

### 那 11 筆未來來訪的下落

「還沒發生的」那張卡底下多一句：「另外有 N 筆未來的來訪寫在客戶的療程紀錄裡，
那些一定會匯入，不用勾」。

**沒有替 `customers[].visits` 做勾選介面。** issue 裡說她講的「一樣勾選」
「很可能包含這一塊」，但 `issues/03` 的「不要順手做的事」寫著
**「不要因為日期在未來就跳過不匯 —— 那是她已經約好的事，日曆上必須看得到」**，
兩件事對不起來時以已經定案的那條為準。真的要一筆一筆決定的話，
那是「匯入前先編輯這份檔案」，是另一支票，不是這一支的分區問題。
所以這一支的做法是**把筆數講出來**：那張卡不再只列十一分之一。

### 做了 issue 上沒寫的事

**「已經發生的」那一區裡，檔案標成 `confirmed` 的那幾筆會多一行警告。**
`futureVisits` 是產檔那一刻的未來，她隔幾天才貼的話那個日期可能已經過了。
`issues/03` 定的是「檔案說 confirmed 就聽它，不管日期」，所以那一筆會建成已確認，
而它的登記待辦一長出來就是逾期的紅字（見 `issues/04` 的 Comments 第 2 點）。
不講的話她要到待辦中心才會發現。

### 沒有做的

- **`merge.mjs` 的 `include: false` 沒有改成依日期給值。** app 這一側從來沒有讀過
  那個欄位（勾選狀態是 UI 自己的），而「未來」的界線在匯入的那一刻 ——
  寫進檔案就是把一個會過期的判斷冷凍起來。只改了那段檔頭註解，
  它描述的是一件不再成立的事。
