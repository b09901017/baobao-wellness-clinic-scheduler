# 第一次拿真資料跑完整條匯入路

Status: 待動工（2026-08-21 梳理，尚未改任何程式碼）
來源：使用者，2026-08-21
動工前先讀：`SPEC.md` 第 4.1、4.2、4.8 節、`CONTEXT.md`、
`docs/adr/0011`、`0013`、`0015`、`0024`、`0027`、`.scratch/visit-lifecycle/spec.md`

2026-08-21 使用者照著建議的順序跑了一次完整流程：
開新試算表 → 貼 `.gs` → 清空 Firebase → 載入種子資料 → 設好推送網址 → 貼合併檔匯入。

**流程本身是對的，但四個地方在真資料上壞掉了。** 逐項拆在 `issues/` 底下，一支一件事。

---

## 這一輪發現什麼

| # | 症狀（她看到的） | 真正的原因 |
|---|---|---|
| 1 | 試算表只長出一位客戶，而且那一張看起來沒畫完 | `.gs` 的 `setFrozenColumns(1)` 切到合併儲存格，第一張畫完就丟例外 → `issues/01` |
| 2 | 沒有任何錯誤訊息 | 自動推送失敗完全靜音 → `issues/02` |
| 3 | 未來的預約被當成已完成 | 合併檔 67 筆來訪全是 `done`，其中 11 筆在今天之後 → `issues/03`、`issues/04` |
| 4 | 候選清單要一筆一筆看日期才知道是過去還是未來 | 三張卡是照「來源」分的，不是照時間 → `issues/05` |
| 5 | 跨天的行事曆事件匯不進來 | skill 的 `parseIcs()` 從頭到尾沒讀過 `DTEND` → `issues/06` |

## 一條貫穿全部的觀察

**這一輪五件事有四件的共同點是「東西不見了，而畫面上什麼都沒說」。**

`issues/01` 的例外存在多久沒人知道，因為 `issues/02` 把它藏起來了；
`issues/03` 的 11 筆未來預約被無聲改寫成已完成；
`issues/06` 的跨天事件無聲塌成一天；
合併檔裡的 `ambiguous`（同一筆事件兩個人都可能）app 根本沒有畫面讀它。

**動工時的鐵則：對不到、算不出、做不到的東西一律講出來，不要安靜地挑一個。**
這條規矩 `domain/legacyImport.js` 已經做得很好（`problems` / `skippedRows` 逐筆列出來），
壞掉的是它下游那幾段。

## 資料

使用者提供的 `import.json`（`baobao-merge/v1`，2026-08-19 產生）：
21 位客戶、67 筆來訪、107 個時段、`futureVisits` 1 筆、`missingFromSheet` 27 筆、
`eventCandidates` 198 筆、`ambiguous` 1 筆。

**原始檔含真實姓名與健康資訊，一律留在暫存區，不進 repo**（`SPEC.md` 第 10 節）。
底下所有的例子都用「客戶A」或假名。
