# 邊界測試清單

上線前要**親手點過**的極端情境。

**這裡刻意沒有 happy path。** 正常的動線由端對端測試自動盯著
（`tests-e2e/`），人再點一遍沒有換到任何資訊。這一份列的是
**測試不容易做、而且踩中會留下壞資料**的那幾種 —— 大部分是
「不熟悉操作的人第一天會做的事」。

## 一條貫穿全部的判準（2026-09-04 加的）

> **每一個會改變狀態的動作，走兩次：做一次，再反悔一次。**
> 反悔之後的畫面要跟做之前一模一樣（除了稽核紀錄）。

那一輪回報的四個問題裡，最嚴重的一個就是從這個縫裡掉出去的：
勾掉「追蹤健檢報告」→ 長出兩張 → 再勾回去 → **那一張報告自己被軟刪除了**。
去程有測試盯著，回程一支都沒有。

## 怎麼用這一份

每一條四欄：**怎麼做 → 預期行為 → 驗收標準 → 對到哪一條規則**。

- **驗收標準**寫的是「怎麼算過」。看不出來就是沒過 ——
  「應該沒問題吧」不算過。
- 在 **staging** 上做，不要在正式環境上做（怎麼分見 [STAGING.md](./STAGING.md)）。
- 踩到跟預期不一樣的，記到 `.scratch/` 開一支 issue，格式見
  `docs/agents/issue-tracker.md`。

---

## A. 連點與競態

一個人在手機上會做的第一件事：**沒反應就再按一次**。

| # | 怎麼做 | 預期行為 | 驗收標準 |
|---|---|---|---|
| A1 | 「問這輪的時間」→ 對同一位客戶**連按兩下**「產生連結」 | 只產生**一條** | 設定 → 稽核紀錄裡只有一筆 `create`；那一位客戶只出現一張訊息卡 |
| A2 | 「依客戶」抽屜裡**連點兩下**同一張待辦 | 只寫一次 | 那一列只變一次（勾起來就停住，不會勾起來又彈回去）；稽核紀錄只有一筆 |
| A3 | 壓表的記錄面板**連按兩下**「加這一筆」 | 只有一筆來訪 | 日曆上那一天只有一段，不是兩段 |
| A4 | 兩台裝置（或兩個分頁）**同時**勾同一張待辦 | 兩邊最後都是「已完成」 | 重新整理兩邊，狀態一致；不會一邊勾起來一邊反勾 |
| A5 | 加購那張表**連按兩下**送出 | 只加一筆額度 | 客戶詳情那一排卡片沒有兩張一模一樣的 |
| A6 | 「簽療程單」的收尾面板連按兩下送出 | 次數只扣一次 | 客戶詳情的「已完成」只 +1 |
| A7 | 表單那一頁（客戶端）連按兩下「送出」 | 只有一份答案 | 收件匣裡只有一筆 |

> 對到的規則：`toast.withSaveState()` 的 `key`（防連點），
> `tests/save-guards.test.js` 會掃出漏傳的地方。

---

## B. 日期的邊界

這一組全部要靠**改裝置的日期**或**挑遠一點的月份**才碰得到。

| # | 怎麼做 | 預期行為 | 驗收標準 |
|---|---|---|---|
| B1 | 客戶詳情 → 不能的時間 → 記一次 → **選這個月**（不是下個月） | 記得下去 | 「新增」那一排的第一顆就是**當月**；存完之後壓當月的表用得到它 |
| B2 | 在**已經過去的日期**上記一筆隨手記 | 記得下去 | 日曆上那一天看得到它，字是變色的，但**不進「逾期」那顆數字** |
| B3 | 8/31 排一筆、9/1 排一筆，然後去壓 9 月的表 | 各自吃到各自那個月的「不能的時間」 | 卡片上那一塊的抬頭寫著「9月不能的時間」；如果九月沒問過，寫的是「還沒問9月的時間」而**不是拿八月那份硬算** |
| B4 | 月底最後一天發出的表單連結，**隔天**再打開 | 還打得開（刻意寬鬆） | 客戶那一頁正常顯示，不是「這個連結找不到」 |
| B5 | 表單連結放到**下下個月**再打開 | 打不開 | 顯示「這個連結已經過期」之類的訊息，而不是一片空白 |
| B6 | 日曆翻到 **2/29**（閏年）與 **12/31** | 兩天都畫得出來、點得下去 | 格子在、點下去抽屜出得來 |
| B7 | 「問這輪的時間」從 1 月往前一格 | 換到**上一年的 12 月** | 抬頭寫著 12 月，名單跟著換 |
| B8 | 死線跨月：來訪排在 **9/1**，任務的死線應該是 **8/31** | 死線是來訪日的**前一天** | 那一列寫的是 8/31，不是 9/1 也不是 9/2 |
| B9 | 跨過午夜（把裝置時間調到隔天）再打開 app | 「今天／明天」那兩顆數字跟著換 | 昨天的「今天」變成「逾期」 |

---

## C. n返（三返、四返…）

這是這一輪新的東西，一筆舊資料都沒有，所以每一條都要點過。

| # | 怎麼做 | 預期行為 | 驗收標準 |
|---|---|---|---|
| C1 | 一位**還沒有任何健檢**的客戶，去壓表看那一排 | 「＋ n返」**整顆不出現** | 不是灰掉按不下去，是根本沒有那一顆 |
| C2 | 健檢**排了但還沒結案**的客戶，選「＋ n返」 | 講出來為什麼沒有候選 | 「這是哪一次健檢的」底下寫著「還沒有做完的健檢可以接」，**不是一片空白** |
| C3 | 同一次健檢**連續加三場** | 返數預設一路變成三、四、五返 | 每次點開「＋ n返」，被按下去的那一顆往後跳一格 |
| C4 | 同一次健檢加**兩場三返** | 只**提醒不擋** | 存檔前的警告寫著「這一次健檢的三返已經約在 X 了」，而且**存得下去** |
| C5 | 加了三返之後，回客戶詳情看次數 | **一個數字都沒有變** | 健檢與二返兩張卡上的「剩 N」跟加之前一模一樣 |
| C6 | 加了三返之後，去看那位客戶的「約二返」待辦 | **完全沒有被影響** | 該在的還在、該收的收了，跟加之前一樣 |
| C7 | 加了三返之後，**把那一次健檢取消掉** | 資料健檢看得見這件事 | 設定 → 資料健檢列出那個孤兒（三返指到一筆不再是「已完成」的健檢） |
| C8 | **一位什麼次數都用完**的客戶，加一場三返 | 走日曆那條路加得了 | 壓表牆上沒有他（正常）；日曆 → 那一天 → ＋ → 新增來訪 → 選他 → 選得到「＋ n返」 |
| C9 | 選了「＋ n返」之後**改回一般的額度** | 返數要被清乾淨 | 存進去的那一段**沒有** `followupNth`；不會跳出「不可以同時指定額度」 |
| C10 | 同一天同一筆來訪裡放**二返＋三返** | 兩段都存得下去、各自正確 | 二返扣了一次額度，三返一次都沒扣 |
| C11 | 加了三返之後看試算表 | 接在那一次健檢的欄位底下，**同一格兩行** | `8/8 二返(醫師甲)` 換行 `9/20 三返(醫師乙)`；矩陣**沒有多一列** |
| C12 | 三返的醫師先不選，存起來 | 存得下去，只提醒 | 試算表上印 `9/20 三返()` —— 空括號代表「約了但醫師還沒定」 |

---

## C2. 臨床提醒（血管難打）

這一輪新的一層。它最容易壞的地方是**看起來標好了，其實那個字不算數**。

| # | 怎麼做 | 預期行為 | 驗收標準 |
|---|---|---|---|
| C2-1 | 把「血管難打」四個字打進**「其他限制」**那一欄，存檔 | 客戶詳情上看得到，**壓表卡片牆上不畫** | 這是對的行為，不是 bug。第三層本來就不畫 |
| C2-2 | 改用上面那一排丸子點一次，存檔 | 壓表卡片牆上出現茶色外框的那一顆 | 卡片牆與待辦的「壓表登記」**兩頁都要有**，而且長得一樣 |
| C2-3 | 一位客戶身上**十二則備註 + 三個臨床提醒 + 兩個禁忌** | 卡片牆那一列不爆版 | 姓名那一行不被擠掉，丸子換行不蓋到旁邊的「還沒壓」徽章 |
| C2-4 | 設定 → 臨床提醒 → 把「血管難打」**停用** | 已經標過的那幾位客戶身上那一顆**掉回第三層** | 卡片牆上不再畫它；客戶詳情上還看得到（畫成灰的） |
| C2-5 | 設定 → 臨床提醒 → 把「血管難打」**改名**成別的字 | 已經標過的客戶身上**還是舊的那個字** | 不自動搬是刻意的。要到那個人身上重選一次 |
| C2-6 | 只標「血管難打」（沒有醫療禁忌）的客戶，去壓表 | 擇一池**三台照樣三台** | 卡片上沒有「只能 ⋯⋯」或「不能用 ⋯⋯」那一句；記錄面板三顆器材都點得下去 |
| C2-7 | 同一個字同時登記成器材的醫療禁忌**與**臨床提醒 | **禁忌贏** | 卡片牆上是實心紅那一顆，只有一顆 |

---

## C3. 備忘錄 / SOP

| # | 怎麼做 | 預期行為 | 驗收標準 |
|---|---|---|---|
| C3-1 | 寫一份**二十節、每節兩千字**的備忘錄 | 存得下去、讀得動 | 閱讀頁捲得順；展開收合不卡在半路；再加第 21 節時「加一節」按不下去 |
| C3-2 | 打到一半按「加一節」 | **已經打的字不會消失** | 上面那幾格的內容原封不動 |
| C3-3 | 一份備忘錄掛的課程**在主檔被刪掉** | 那一份照樣打得開 | 標題底下那一行淡字少一個名字，其餘不變；不會白屏 |
| C3-4 | 一節都沒標時機的那一份 | **不畫那條脊線** | 沒有節點小圓點，就是一串平的清單 |
| C3-5 | 只寫了「當天」那一節，去日曆點**下週**的那一筆來訪 | **整塊不畫** | 不是留一個空的框，是根本沒有那一塊 |
| C3-6 | **離線**存一份備忘錄 | 那一句**不可以說「失敗」** | 逾時之後畫面上寫的是「還沒送出去 —— 已經存在這台裝置上了」 |
| C3-7 | 刪掉一份，去設定 → 已刪除項目 | 還原得回來 | 還原之後備忘錄清單上又看得到，章節一節都沒少 |
| C3-8 | 同一天兩段**同樣的課程**（例如兩段點滴） | 那一份備忘錄**只畫一次** | 日曆的讀取卡片上不會出現兩塊一模一樣的 |

---

## C4. 健檢那條鏈的分岔

| # | 怎麼做 | 預期行為 | 驗收標準 |
|---|---|---|---|
| C4-1 | 勾掉「追蹤健檢報告」 | 同一刻長出**兩張** | 「寄報告給醫師」與「約二返」，死線一樣 |
| C4-2 | 把「約二返」勾掉，**不要**動寄報告 | 寄報告那一張**還在** | 這是這一輪最重要的一格。它消失就是回歸了 |
| C4-3 | 把「追蹤健檢報告」**拿回來**（取消勾選） | 另外兩張都收起來 | 報告那一張留著、變回未完成 |
| C4-4 | 勾掉 → 拿回來 → 再勾掉 | **不會變成四張** | 兩張就是兩張 |
| C4-5 | 同一天**兩個健檢**（理論上不會，資料上做得到） | 不會少算 | 那一天只認得出一次，而資料健檢會把它列出來 |

---

## C5. 「寫紀錄」那一族

| # | 怎麼做 | 預期行為 | 驗收標準 |
|---|---|---|---|
| C5-1 | 把一場**二返**結案 | 長出「寫紀錄」，死線是**來訪那一天** | 不是前一天。那一列上印得出「二返」 |
| C5-2 | 結案的那位客戶身上還有**沒做完的 Examine** | Examine **一張都不會被收掉** | 結案不可以把還沒做完的掛號洗掉 |
| C5-3 | 把一場二返標成**未到** | **一張都不長** | 人沒來，沒有紀錄要寫 |
| C5-4 | 設定 → 課程 → 二返 → **關掉**「客人走了之後要補一份紀錄」，再結一場 | 不再長那一張 | 已經長出來、還沒勾的那幾張會被收掉；勾掉的留著 |
| C5-5 | 一場來訪裡**兩個都要寫紀錄**的課程 | 只長**一張** | 不是兩張。那一列講得出兩個課程名 |

---

## D. 網路與離線

打開 DevTools 的 Network → Offline，或者直接開飛航模式。

| # | 怎麼做 | 預期行為 | 驗收標準 |
|---|---|---|---|
| D1 | 離線狀態下勾一張待辦 | 八秒後換一句話 | 那句話**不可以有「失敗」兩個字** —— 資料已經在本機了，說失敗你會再存一次 |
| D2 | 離線狀態下在抽屜裡勾三筆，然後恢復連線 | 三筆都送出去了 | 重新整理之後三筆都還是「已完成」 |
| D3 | 離線狀態下打開 app（冷啟動） | 畫面畫得出來 | 不是白屏；最上面有一條常駐的離線提示 |
| D4 | 弱網（DevTools → Slow 3G）切換「問這輪的時間」的月份 | 不會出現半份名單 | 要嘛還在載入，要嘛整份換好，不會一半是上個月一半是這個月 |
| D5 | 弱網下按「產生連結」，在轉圈的時候**再按一次** | 只產生一條 | 同 A1 |
| D6 | 離線 → 勾一筆 → **直接關掉 app** → 恢復連線 → 重開 | 那一筆有送出去 | 待辦上那一筆是「已完成」 |

---

## E. 資料看起來壞掉的時候

這一組模擬「主檔被動過」與「資料是舊的」，都是真的發生過的形狀。

| # | 怎麼做 | 預期行為 | 驗收標準 |
|---|---|---|---|
| E1 | 設定 → 課程主檔停用一個**已經被排過**的課程 | 用過它的客戶**不會從清單上消失** | 「壓表登記」那一頁照樣列得到他；日曆上舊的那幾筆照樣印得出課程名 |
| E2 | 刪掉一筆**已經被來訪用到**的額度 | 資料健檢列出孤兒 | 設定 → 資料健檢 → 看得到「這個時段指向的額度已被刪除」 |
| E3 | 客戶**填完表單之後**才重發一條新連結 | 舊的那份答案還在收件匣 | 「客戶填好的時間」看得到那一份；新連結那一位在「等待回覆」 |
| E4 | 同一位客戶同一個月**兩份**「不能的時間」（拿舊資料） | 資料健檢列出來，**不自動合併** | 兩份的摘要都看得到，你自己刪掉不要的那一份 |
| E5 | 把一位客戶**停用** | 他從壓表牆與「問這輪的時間」消失 | 客戶總覽的「看已停用的」找得回來；他既有的來訪還在日曆上 |
| E6 | 一筆**匯入的舊來訪**（沒有時間、沒有器材） | 存得回去 | 打開來訪編輯器改一個欄位、按儲存，不會被「時間格式不對」擋住 |
| E7 | 額度的計數欄位手動改壞（或拿有問題的舊資料） | 對帳列出差異 | 設定 → 資料健檢 → 次數對帳看得到；按「修正」之後兩邊一致，而且稽核紀錄有一筆 |
| E8 | 有**醫療禁忌**（例：體內金屬）的客戶，排一段用超磁場的復能 | **硬性擋下來** | 那顆器材丸子是劃掉的、按不下去；這是全系統唯一真的會擋你的地方 |

---

## F. 反覆與收尾

| # | 怎麼做 | 預期行為 | 驗收標準 |
|---|---|---|---|
| F1 | 勾掉「追蹤健檢報告」→ 到「已完成」把它**拿回來** | 退回上一站 | 「約二返」跟著收起來，「追蹤健檢報告」回到未完成 —— **不是把它刪掉** |
| F2 | 「依客戶」抽屜裡勾一筆 → **往下甩掉抽屜** → 再點開同一位 | 狀態是對的 | 那一筆還是勾掉的（在抽屜裡劃著線），底下卡片的數字也對 |
| F3 | 抽屜開著的時候按**手機的返回鍵** | 關掉抽屜，**不換頁** | 還在待辦中心，而且還在「依客戶」那一格 |
| F4 | 客戶詳情按「編輯」，打到一半按返回鍵 | 回到那個人的詳情頁 | **不是整個離開這位客戶** |
| F5 | 取消一筆**已經去 Examine 掛過號**的來訪 | 產生一張「取消 Examine」 | 待辦的「改時間／取消」那一段看得到它 |
| F6 | 一次清掉「已完成」的十幾筆 | 二次確認，講出筆數 | 確認框寫著「清掉 N 筆」與「還原得回來」；清完之後設定 → 已刪除項目找得回來 |
| F7 | 收工前開「看今天做了什麼」 | 今天做的每一件都在 | 兩種分組（照人／照流程）切換之後，**兩邊的筆數一樣** —— 一則都不可以被丟掉 |

---

## H. 反悔（2026-09-04）

**做一次，再反悔一次。** 這一組是上面那條判準的實作。

| # | 怎麼做 | 預期行為 | 驗收標準 |
|---|---|---|---|
| H1 | 勾掉「追蹤健檢報告」→ 去壓一場二返 → 回頭把報告從「已完成」點回來 | 報告回到**未完成**，另外兩張收起來 | 待辦 → 追蹤健檢報告 → 「未完成」那一格看得到它；設定 → 已刪除項目裡**沒有**它 |
| H2 | 勾掉「追蹤健檢報告」→ 勾掉「約二返」→ 再把報告點回來 | 同上 | 同上 |
| H3 | 那一筆健檢改成**已取消**（不是反悔，是真的沒發生） | 掛在它身上的三張全部收掉 | 稽核紀錄上三筆的理由都是「那一筆健檢不是已完成了」 |
| H4 | 備忘錄 → 鉛筆 → 改了幾行 → **取消** | 卡片上還是舊的字 | 重新整理之後也還是舊的 |
| H5 | 排來訪時把品項換成別款 → **不存檔**，直接返回 | 那一筆沒有變 | 日曆點開那一筆，品項還是原來那一款 |

---

## I. 那幾格數字（2026-09-04）

**打得出正確的數字卻存不下去**是最難查的一種：瀏覽器擋在存檔之前，
所以程式裡的驗證跑都沒跑到，而錯誤訊息看起來跟那個欄位沒有關係。

| # | 怎麼做 | 預期行為 | 驗收標準 |
|---|---|---|---|
| I1 | 設定 → 課程 → 二返，時長填 **30** | 存得起來 | 重新打開那一頁還是 30 |
| I2 | 同上，填 **7**、**23**、**90** | 都存得起來 | 三次都要成功 |
| I3 | 同上，填 **0** 或 **-5** | **擋下來** | 欄位不可以比規則鬆 |
| I4 | 設定 → 排序權重，w1 填 **1.25** | 存得起來 | 重新打開還是 1.25 |
| I5 | 來訪編輯器上把時間打成 **10:32**（不是 5 的倍數） | 存得起來 | 日曆上那一筆顯示 10:32 |

---

## J. 貼上與 emoji（2026-09-04）

**剪貼簿是這個 app 唯一一條「別人的資料進得來」的路。**
她的筆記記在 LINE 裡，貼過來時帶著 `(emoji)`、`(加1)` 這種代碼。

| # | 怎麼做 | 預期行為 | 驗收標準 |
|---|---|---|---|
| J1 | 備忘錄 → 鉛筆 → 貼一段 60 行以上的字 | 框裡**捲得動**，而且「存起來」那顆還在畫面上 | 手指在框裡上下滑得動，最後一行看得到 |
| J2 | 同上，鍵盤叫出來的時候 | 存檔鈕還在畫面上 | iPad 與手機各試一次 |
| J3 | 貼一段超過 5000 字的 | 截在 5000，而且**講出丟掉幾個字** | 看到那句提示，不是安靜地少一段 |
| J4 | 貼她自己那段 LINE 筆記 | `(emoji)` → 🐻、`(加1)`/`(1)` → 1️⃣ | 畫面上找不到 `(emoji)` 三個字 |
| J5 | 那一段裡的全形括號（乾淨度）、（通知客人） | **一個字都沒被改** | 逐字比對一次 |
| J6 | 自己**打**（不是貼）一個 `(2)` | 不換 —— 打字不清洗 | 存起來還是 `(2)` |
| J7 | 游標放在中間 → 點一顆 emoji | 插在**游標處**，焦點留在框裡 | 不用再點一次輸入框就能繼續打 |
| J8 | 存起來 → 日曆點開那個課程的來訪 | 浮出來的前幾行**emoji 顯示得出來、不破版** | 那一塊沒有橫向捲動條 |
| J9 | 貼**全形**的「（emoji）（加1）」 | 一樣換掉 | LINE 在不同裝置上複製出來的括號不一定是半形的 |
| J10 | 打開一份**舊的**、裡面本來就有代碼的備忘錄 | emoji 那一排最前面出現「清掉貼圖代碼」 | 按了才清 —— 開檔不會自動改她的字 |
| J11 | 打開一份沒有代碼的 | 那一顆**不出現** | 一顆永遠在的按鈕會變成裝飾 |
| J12 | 編輯一份很長的備忘錄 | **整張卡不用捲**：emoji 那一排與「存起來」都看得到，只有輸入框會捲 | 這是 2026-09-04 回報的 c 與 d |

---

## K. 拿回來與這一場的待辦（2026-09-04）

| # | 怎麼做 | 預期行為 | 驗收標準 |
|---|---|---|---|
| K1 | 勾掉「追蹤健檢報告」→ 從已完成點回來 | **跳一道確認**，列出會被收走的那兩張 | 框裡看得到「寄報告給醫師」與「約二返」 |
| K2 | 同上，先去壓好一場二返再拿回來 | 框裡講得出那一場的日期，**而且說「那一筆來訪不會被動到」** | 按下去之後，日曆上那一筆二返還在，狀態沒變 |
| K3 | 同上，按「取消」 | 什麼都沒發生 | 報告還是已完成，那兩張還在 |
| K4 | 拿回 Examine／耀聖／寫紀錄 | **不跳確認** | 一按就回到未完成 |
| K5 | 取消一筆**健檢** | 確認框印的是「取消 Examine」，不是三個系統並列 | 框裡找不到 Abovee |
| K6 | 取消一筆同時有健檢與復能的來訪 | 兩個系統都講 | 框裡兩個都在 |
| K7 | 日曆／客戶詳情／待辦中心／進度追蹤，各點開同一筆來訪 | 「這一場的待辦」那一塊**四頁一模一樣** | 四頁列出來的順序與勾法都相同 |
| K7b | 點開一筆**已經結案**的來訪 | 「跟客人確認時間」「簽療程單」**還在**，畫成淡掉＋劃掉 | 做完的不消失 —— 消失了分不出「做完了」跟「沒有這一件」 |
| K8 | 點那一塊裡的任何一列 | **點不動**（只給看） | 沒有勾選框、沒有按鈕 |
| K9 | 點開一筆什麼待辦都沒有的來訪 | 那一塊**整個不出現** | 不是一個空的框 |
| K10 | 日曆抽屜看一天 | 有「記的話」的那一列有記事本圖示；掛得到備忘錄的有書的圖示 | 兩顆都在的那一列，狀態徽章沒被擠掉 |

---

## L. LINE 回覆模板（2026-09-04）

| # | 怎麼做 | 預期行為 | 驗收標準 |
|---|---|---|---|
| L1 | 設定 → LINE 回覆模板，改「問壓好的時間可不可以」→ 存 | 待辦 →「跟客人確認時間」那顆複製鈕給的是新的字 | 兩邊逐字比對 |
| L1b | 一位客戶同一天壓兩段、外加另一天一段 | 確認訊息裡**三段各一行**，都有課程名 | 例：`9/17 10:00 營養點滴` 與 `9/17 11:00 復能` 兩行都在 |
| L2 | 把 `{name}` 打成 `{Name}` → 存 → 去看訊息 | 那五個字**原樣印在訊息裡** | 看得到就知道自己打錯了 |
| L3 | 一則刪光 → 存 | **存不下去**，而且講得出是哪一則 | 錯誤訊息上有那一則的名字 |
| L4 | 逐則「回復預設」 | 那一格變回原本的字（不問） | 存回去之後「已改過」那顆徽章消失 |
| L5 | 「全部回復預設」 | **問一次**，按確定之後六則都回去 | 設定 → 稽核紀錄看得到那一筆寫入 |
| L6 | 改完之後**換另一台裝置** | 那一台也是新的字 | 模板存在雲端，不是這一台的 localStorage |

---

## G. 上線前最後一次

這幾條是每次要推上正式環境之前跑一遍的，不是每天做的。

- [ ] `npm test`（單元與靜態守衛）全綠
- [ ] `npm run test:rules`（Security Rules 跑在模擬器上）全綠
- [ ] `npm run test:e2e`（端對端，約二十幾分鐘）全綠
- [ ] 在 **staging** 上把上面 A–F、H、I 每一組至少各挑兩條點過
- [ ] 備份**還原過一次**（不是只有匯出，是真的還原回去看資料在不在）——
      做法見 [STAGING.md](./STAGING.md)
- [ ] 試算表那一側的指令碼版本對得上（推一次，看試算表真的動了）
- [ ] 手機上冷啟動一次（第一次開、沒有快取）
- [ ] 深色模式下把每一頁掃一遍（有沒有哪一塊還是淺的）
