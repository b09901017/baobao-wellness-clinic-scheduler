# 上線前健檢報告的重現測試（2026-09-16）

**這幾支是刻意會紅的，而且刻意放在 `tests/` 的子目錄裡** ——
`npm test` 只掃 `tests/*.test.js`（不遞迴），所以它們不會擋住日常驗收。

自己跑：

```
node --test "tests/prelaunch-repro/*.test.js"
```

一支對應報告的一節，斷言寫的是**文件說它應該怎樣**，紅的那一行就是報告講的那件事。
修好之後這幾支會轉綠 —— 那時候再決定要把哪幾支搬進 `tests/` 正式的那一層。
