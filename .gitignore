# === 絕對不能進版控（SPEC.md 第 10 節）===
# Service account 金鑰是萬能鑰匙，**它繞過所有 Security Rules**。
#
# 兩層擋法，刻意重複：
#
#   1. 檔名 —— Console 下載下來的預設名字就長這樣，改到別的資料夾也照樣擋
#   2. 整個 keys/ 資料夾 —— 擋掉「她把檔名改短了」的那種情況，
#      而那正是第 1 條唯一漏得掉的破口
#
# 少了任何一條都還有另一條在。驗證方式：
#   git check-ignore -v keys/你的金鑰.json
*-firebase-adminsdk-*.json
serviceAccountKey.json
*serviceAccount*.json
keys/

# 含真實客戶健康資訊的匯出檔與備份
/backups/
/exports/

# 環境變數（Firebase 前端 config 不是密鑰，可以直接 commit，不需要放這裡）
.env*

# === Firebase 工具產物 ===
.firebase/
firebase-debug.log*
firestore-debug.log*
database-debug.log*
pubsub-debug.log*
ui-debug.log*
firestore-export/

# === 一般 ===
node_modules/
.DS_Store
Thumbs.db
*.swp

# === 刻意「不」忽略的東西，不要加進來 ===
# .claude/    → agent skills，必須進版控（手機開 session 靠這個）
# .scratch/   → 本專案的 issue tracker（見 docs/agents/issue-tracker.md）
# .firebaserc → 專案設定，不是密鑰
# firebase.json / firestore.rules / firestore.indexes.json → 必須進版控

# Python
__pycache__/

# === 設計稿 ===
# .scratch/ui-redesign/canvas/*.dc.html 與 canvas.json 是原始檔，要進版控。
# 下面這個是把它們封進 Claude Design 編輯器之後的產出物（約 2.4MB，其中
# 絕大部分是編輯器本身的程式碼），改了原始檔重跑 seed 就會重新產生一份。
.scratch/ui-redesign/canvas/scheduler-ui-redesign.html

# === 舊資料合併用的本機檔案（.claude/skills/calendar-sheet-merge）===
# 別名表裡是真實客戶姓名，跟匯出的 TSV、報告一樣不進版控。
# 遠端容器會被回收，所以每次跑完要把 .local/ 裡的東西交還給使用者自己留著。
/.local/

# graphify 產出的圖（build artifact，不進版控）
graphify-out/

# === E2E 的產出（Playwright + 模擬器）===
# 報告、截圖、錄影、trace 與模擬器日誌。一次全量跑完是 100MB 起跳，
# 而且每一次的內容都不一樣 —— 它是那一次的證據，不是原始碼。
# **腳本本身要進版控**：tests-e2e/ 是回歸測試的護城河，不在這裡面。
/.artifacts/

# === 編輯器的個人設定 ===
# 「這台機器怎麼開專案」，不是專案本身。她兩台裝置的設定本來就該各自不同，
# 同 ADR-0055 對主題的判斷。
.vscode/
